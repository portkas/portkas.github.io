<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Anaconda</title>
    <url>/2024/10/31/Anaconda/</url>
    <content><![CDATA[<span id="more"></span>
<h1 id="常用指令"><a href="#常用指令" class="headerlink" title="常用指令"></a>常用指令</h1><h2 id="命令行窗口"><a href="#命令行窗口" class="headerlink" title="命令行窗口"></a>命令行窗口</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 查看版本号</span><br><span class="line">&gt; conda --version</span><br><span class="line"></span><br><span class="line"># 查看目前的镜像</span><br><span class="line">&gt; conda config </span><br><span class="line"></span><br><span class="line"># 添加路径</span><br><span class="line">&gt; conda config --add channels http://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/</span><br><span class="line">&gt; conda config –-add channels http://mirrors.ustc.edu.cn/anaconda/pkgs/free/</span><br><span class="line">&gt; conda config --add channels http://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge/</span><br><span class="line">&gt; conda config --set show_channel_urls yes</span><br><span class="line"></span><br><span class="line"># 升级工具包</span><br><span class="line">&gt; conda upgrade --all</span><br></pre></td></tr></table></figure>

<h2 id="Anaconda-Prompt"><a href="#Anaconda-Prompt" class="headerlink" title="Anaconda Prompt"></a>Anaconda Prompt</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 创建虚拟环境</span><br><span class="line">&gt; conda create -n &lt;env-name&gt; python=3.6</span><br><span class="line"></span><br><span class="line"># 列出所有可用的conda环境</span><br><span class="line">&gt; conda info --envs</span><br><span class="line"></span><br><span class="line"># 进入虚拟环境</span><br><span class="line">&gt; activate &lt;env-name&gt;</span><br><span class="line"></span><br><span class="line"># 退出虚拟环境</span><br><span class="line">&gt; deactivate</span><br><span class="line"></span><br><span class="line"># 安装package</span><br><span class="line">&gt; conda install &lt;package-name&gt;</span><br><span class="line"></span><br><span class="line"># 查看当前环境下安装的packages</span><br><span class="line">&gt; conda list</span><br><span class="line"></span><br><span class="line"># 安装package</span><br><span class="line">&gt; pip install -i http://pypi.tuna.tsinghua.edu.cn/simple --trusted-host pypi.tuna.tsinghua.edu.cn  matplotlib</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="ps"><a href="#ps" class="headerlink" title="ps"></a>ps</h2><p>Anaconda Prompt可以看作是命令行窗口（CMD）的增强版，专门为Anaconda用户设计，以支持Python开发和数据科学工作。</p>
<ul>
<li>Anaconda Prompt完全兼容CMD中的所有命令。</li>
<li>Anaconda Prompt提供了conda命令，用于管理Anaconda环境和包。</li>
<li>Anaconda Prompt预装了许多数据科学和机器学习相关的库</li>
<li>Anaconda Prompt允许用户创建、激活和删除不同的Python环境（称为conda环境）</li>
</ul>
<h1 id="reference"><a href="#reference" class="headerlink" title="reference"></a>reference</h1><p><a href="https://blog.csdn.net/Inochigohan/article/details/120400990">anaconda安装</a><br><a href="https://blog.csdn.net/qq_39220334/article/details/121961742">清华镜像源</a><br><a href="https://www.cnblogs.com/lab-zj/p/16975410.html">conda常用命令</a><br><a href="https://blog.csdn.net/JineD/article/details/124774570">pip安装package</a></p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
  </entry>
  <entry>
    <title>CMake嵌套写法</title>
    <url>/2024/10/31/CMake%E5%B5%8C%E5%A5%97%E5%86%99%E6%B3%95/</url>
    <content><![CDATA[<p>[ ] CMake嵌套写法</p>
<p>动态库：源文件非常多，生成的可执行文件比较小，发布的时候不仅需要发布可执行文件，还需要发布动态库。<br>静态库：源文件比较少，生成的可执行文件比较大，发布的时候只需要发布可执行文件就可以了。</p>
<span id="more"></span>

<p><code>CMAKE_CURRENT_SOURCE_DIR</code>:指向当前正在解析的CMakeLists.txt文件所在目录，它的值会随着CMake处理不同的CMakeLists.txt而变化。<br><code>PROJECT_SOURCE_DIR</code>:指向项目的根目录，即包含顶层CMakeLists.txt文件所在的目录。这个变量只在项目的顶层CMakeLists.txt文件中有效，即在调用project()命令之后。<br><code>CMAKE_SOURCE_DIR</code>：总是指向整个项目的根目录，这个变量在项目的顶层CMakeLists.txt文件中等同于<code>PROJECT_SOURCE_DIR</code>，并且在子目录中也是可以使用的。<br><code>PROJECT_BINARY_DIR</code>:指向项目的二进制文件，即CMake生成的构建文件（如Makefile）所在的目录。</p>
<p>父节点里的变量可以在子节点中用（继承）<br>子节点中定义的变量不能再父节点中用</p>
<h1 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h1><h2 id="文件结构"><a href="#文件结构" class="headerlink" title="文件结构"></a>文件结构</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ tree </span><br><span class="line">.</span><br><span class="line">├── CMakeLists.txt</span><br><span class="line">├── calc</span><br><span class="line">│   ├── CMakeLists.txt</span><br><span class="line">│   ├── add.cpp</span><br><span class="line">│   ├── div.cpp</span><br><span class="line">│   ├── mult.cpp</span><br><span class="line">│   └── sub.cpp</span><br><span class="line">├── include</span><br><span class="line">│   ├── calc.h</span><br><span class="line">│   └── sort.h</span><br><span class="line">├── sort</span><br><span class="line">│   ├── CMakeLists.txt</span><br><span class="line">│   ├── bubbleSort.cpp</span><br><span class="line">│   ├── insertionSort.cpp</span><br><span class="line">│   └── selectionSort.cpp</span><br><span class="line">├── test01</span><br><span class="line">│   ├── CMakeLists.txt</span><br><span class="line">│   └── sort.cpp</span><br><span class="line">└── test02</span><br><span class="line">    ├── CMakeLists.txt</span><br><span class="line">    └── calc.cpp</span><br><span class="line"></span><br><span class="line">5 directories, 16 files</span><br></pre></td></tr></table></figure>

<h2 id="根目录CMakeLists-txt文件"><a href="#根目录CMakeLists-txt文件" class="headerlink" title="根目录CMakeLists.txt文件"></a>根目录CMakeLists.txt文件</h2><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">cmake_minimum_required</span>(VERSION <span class="number">3.10</span>)</span><br><span class="line"><span class="keyword">project</span>(Demo_r)</span><br><span class="line"><span class="comment"># 定义变量</span></span><br><span class="line"><span class="comment"># 静态库生成路径</span></span><br><span class="line"><span class="keyword">set</span>(LIBPATH <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/lib)</span><br><span class="line"><span class="comment"># 可执行程序存储目录</span></span><br><span class="line"><span class="keyword">set</span>(EXECPATH <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/bin)</span><br><span class="line"><span class="comment"># 头文件路径</span></span><br><span class="line"><span class="keyword">set</span>(HEADPATH <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/<span class="keyword">include</span>)</span><br><span class="line"><span class="comment"># 库文件的名字</span></span><br><span class="line"><span class="keyword">set</span>(CALCLIB calc)</span><br><span class="line"><span class="keyword">set</span>(SORTLIB sort)</span><br><span class="line"><span class="comment"># 可执行程序的名字</span></span><br><span class="line"><span class="keyword">set</span>(APPNAME1 app1)</span><br><span class="line"><span class="keyword">set</span>(APPNAME2 app2)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 给当前节点添加子目录</span></span><br><span class="line"><span class="keyword">add_subdirectory</span>(calc)</span><br><span class="line"><span class="keyword">add_subdirectory</span>(sort)</span><br><span class="line"><span class="keyword">add_subdirectory</span>(test01)</span><br><span class="line"><span class="keyword">add_subdirectory</span>(test02)</span><br></pre></td></tr></table></figure>

<h2 id="calc目录中CMakeLists-txt文件"><a href="#calc目录中CMakeLists-txt文件" class="headerlink" title="calc目录中CMakeLists.txt文件"></a>calc目录中CMakeLists.txt文件</h2><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">cmake_minimum_required</span>(VERSION <span class="number">3.10</span>)</span><br><span class="line"><span class="keyword">project</span>(calc)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搜索源文件</span></span><br><span class="line"><span class="keyword">aux_source_directory</span>(./ SRC)</span><br><span class="line"><span class="comment"># 指定包含的头文件</span></span><br><span class="line"><span class="keyword">include_directories</span>(<span class="variable">$&#123;HEADPATH&#125;</span>)</span><br><span class="line"><span class="comment"># 指定静态库生成的路径</span></span><br><span class="line"><span class="keyword">set</span>(LIBRARY_OUTPUT_PATH <span class="variable">$&#123;LIBPATH&#125;</span>)</span><br><span class="line"><span class="comment"># 生成静态库</span></span><br><span class="line"><span class="keyword">add_library</span>(<span class="variable">$&#123;CALCLIB&#125;</span> STATIC <span class="variable">$&#123;SRC&#125;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="sort目录中CMakeLists-txt文件"><a href="#sort目录中CMakeLists-txt文件" class="headerlink" title="sort目录中CMakeLists.txt文件"></a>sort目录中CMakeLists.txt文件</h2><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">cmake_minimum_required</span>(VERSION <span class="number">3.10</span>)</span><br><span class="line"><span class="keyword">project</span>(sort)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搜索源文件</span></span><br><span class="line"><span class="keyword">aux_source_directory</span>(./ SRC)</span><br><span class="line"><span class="comment"># 指定包含的头文件</span></span><br><span class="line"><span class="keyword">include_directories</span>(<span class="variable">$&#123;HEADPATH&#125;</span>)</span><br><span class="line"><span class="comment"># 指定静态库生成的路径</span></span><br><span class="line"><span class="keyword">set</span>(LIBRARY_OUTPUT_PATH <span class="variable">$&#123;LIBPATH&#125;</span>)</span><br><span class="line"><span class="comment"># 生成静态库</span></span><br><span class="line"><span class="keyword">add_library</span>(<span class="variable">$&#123;SORTLIB&#125;</span> STATIC <span class="variable">$&#123;SRC&#125;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="test01目录中CMakeLists-txt文件"><a href="#test01目录中CMakeLists-txt文件" class="headerlink" title="test01目录中CMakeLists.txt文件"></a>test01目录中CMakeLists.txt文件</h2><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">cmake_minimum_required</span>(VERSION <span class="number">3.10</span>)</span><br><span class="line"><span class="keyword">project</span>(test01)</span><br><span class="line"></span><br><span class="line"><span class="keyword">aux_source_directory</span>(./ SRC)</span><br><span class="line"><span class="keyword">include_directories</span>(<span class="variable">$&#123;HEADPATH&#125;</span>)</span><br><span class="line"><span class="keyword">link_directories</span>(<span class="variable">$&#123;LIBPATH&#125;</span>)</span><br><span class="line"><span class="keyword">link_libraries</span>(<span class="variable">$&#123;SORTLIB&#125;</span>)</span><br><span class="line"><span class="keyword">set</span>(EXECUTABLE_OUTPUT_PATH <span class="variable">$&#123;EXECPATH&#125;</span>)</span><br><span class="line"><span class="keyword">add_executable</span>(<span class="variable">$&#123;APPNAME1&#125;</span> <span class="variable">$&#123;SRC&#125;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="test02目录中CMakeLists-txt文件"><a href="#test02目录中CMakeLists-txt文件" class="headerlink" title="test02目录中CMakeLists.txt文件"></a>test02目录中CMakeLists.txt文件</h2><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">cmake_minimum_required</span>(VERSION <span class="number">3.10</span>)</span><br><span class="line"><span class="keyword">project</span>(test02)</span><br><span class="line"></span><br><span class="line"><span class="keyword">aux_source_directory</span>(./ SRC)</span><br><span class="line"><span class="keyword">include_directories</span>(<span class="variable">$&#123;HEADPATH&#125;</span>)</span><br><span class="line"><span class="keyword">link_directories</span>(<span class="variable">$&#123;LIBPATH&#125;</span>)</span><br><span class="line"><span class="keyword">link_libraries</span>(<span class="variable">$&#123;CALCLIB&#125;</span>)</span><br><span class="line"><span class="keyword">set</span>(EXECUTABLE_OUTPUT_PATH <span class="variable">$&#123;EXECPATH&#125;</span>)</span><br><span class="line"><span class="keyword">add_executable</span>(<span class="variable">$&#123;APPNAME2&#125;</span> <span class="variable">$&#123;SRC&#125;</span>)</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>CMake</category>
      </categories>
  </entry>
  <entry>
    <title>Github的SSH配置</title>
    <url>/2024/09/02/Github%E7%9A%84SSH%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<h1 id="1-创建公私密钥"><a href="#1-创建公私密钥" class="headerlink" title="1.创建公私密钥"></a>1.创建公私密钥</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ssh-keygen -t rsa -C <span class="string">&quot;Github注册的邮箱地址&quot;</span></span><br><span class="line">$ <span class="built_in">cat</span> ～/.ssh/id_rsa.pub</span><br><span class="line"><span class="comment"># ps.在windows系统中.ssh文件在C:\Users\你的用户名\.ssh目录下</span></span><br></pre></td></tr></table></figure>

<span id="more"></span>
<h1 id="2-配置公钥"><a href="#2-配置公钥" class="headerlink" title="2.配置公钥"></a>2.配置公钥</h1><p>1.github -&gt; 个人头像 -&gt; setting -&gt; SSH and GPG keys -&gt; New SSH key<br>2.复制id_rsa.pub的内容到Key中<br>3.Title的内容自定义</p>
<h1 id="3-验证是否设置成功"><a href="#3-验证是否设置成功" class="headerlink" title="3.验证是否设置成功"></a>3.验证是否设置成功</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ssh -T git@github.com</span><br><span class="line">Hi portkas! You<span class="string">&#x27;ve successfully authenticated, but GitHub does not provide shell access.</span></span><br></pre></td></tr></table></figure>

<h1 id="4-Q-A"><a href="#4-Q-A" class="headerlink" title="4.Q&amp;A"></a>4.Q&amp;A</h1><p>SSH登录安全性由非对称加密保证，产生密钥时，一次产生两个密钥，一个公钥，一个私钥，在git中一般命名为id_rsa.pub, id_rsa。</p>
<h2 id="如何使用生成的一个私钥一个公钥进行验证呢？"><a href="#如何使用生成的一个私钥一个公钥进行验证呢？" class="headerlink" title="如何使用生成的一个私钥一个公钥进行验证呢？"></a>如何使用生成的一个私钥一个公钥进行验证呢？</h2><p>本地生成一个密钥对，其中公钥放到远程主机，私钥保存在本地，当本地主机需要登录远程主机时，本地主机向远程主机发送一个登录请求，远程收到消息后，随机生成一个字符串并用公钥加密，发回给本地。本地拿到该字符串，用存放在本地的私钥进行解密，再次发送到远程，远程比对该解密后的字符串与源字符串是否等同，如果等同则认证成功。</p>
<h2 id="每使用一台主机都要配？"><a href="#每使用一台主机都要配？" class="headerlink" title="每使用一台主机都要配？"></a>每使用一台主机都要配？</h2><p>每使用一台新主机进行git远程操作，想要实现无密，都需要配置。并不是说每个账号配一次就够了，而是每一台主机都需要配。</p>
<h2 id="配了为什么就不用密码了？"><a href="#配了为什么就不用密码了？" class="headerlink" title="配了为什么就不用密码了？"></a>配了为什么就不用密码了？</h2><p>因为配置的时候是把当前主机的公钥放到了你的github账号下，相当于当前主机和你的账号做了一个关联，你在这台主机上已经登录了你的账号，此时此刻github认为是该账号主人在操作这台主机，在配置ssh后就信任该主机了。所以下次在使用git的时候即使没有登录github，也能直接从本地push代码到远程了。</p>
]]></content>
      <categories>
        <category>Git</category>
      </categories>
  </entry>
  <entry>
    <title>Git分支管理Hexo</title>
    <url>/2024/09/02/Git%E5%88%86%E6%94%AF%E7%AE%A1%E7%90%86Hexo/</url>
    <content><![CDATA[<h1 id="1-本地初始电脑"><a href="#1-本地初始电脑" class="headerlink" title="1.本地初始电脑"></a>1.本地初始电脑</h1><p>1.初始化Git仓库</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cd</span> blog</span><br><span class="line">$ git init  <span class="comment"># 创建.git子目录</span></span><br></pre></td></tr></table></figure>
<span id="more"></span>
<p>2.添加远程仓库</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git remote add origin git@github.com:yourname/yourname.github.io.git</span><br></pre></td></tr></table></figure>

<p>3.拉取远端仓库所有内容及分支</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git fetch origin</span><br></pre></td></tr></table></figure>

<p>5.新建分支</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout -b master</span><br></pre></td></tr></table></figure>

<p>4.在<code>./source/_post/</code>中添加.md博客并生成</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo clean</span><br><span class="line">$ hexo g</span><br><span class="line">$ hexo d</span><br></pre></td></tr></table></figure>

<p>5.将本地仓库推送至远端</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git push origin master</span><br></pre></td></tr></table></figure>

<h1 id="2-其他电脑"><a href="#2-其他电脑" class="headerlink" title="2.其他电脑"></a>2.其他电脑</h1><p>1.安装Node.js</p>
<p>2.拉取远端仓库并进入文件夹</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">clone</span> git@github.com:yourname/yourname.github.io.git blogname</span><br><span class="line">$ <span class="built_in">cd</span> blogname</span><br></pre></td></tr></table></figure>

<p>3.拉取远端仓库分支并切换至master分支（默认分支是main，master分支是自己建的）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git fetch origin</span><br><span class="line">$ git checkout master</span><br></pre></td></tr></table></figure>

<p>4.安装Hexo及相关依赖</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ npm install hexo	<span class="comment"># 安装hexo</span></span><br><span class="line">$ npm install hexo-cli -g <span class="comment"># 重新安装(如果上述安装不起作用）</span></span><br><span class="line">$ npm install	<span class="comment"># 安装依赖</span></span><br></pre></td></tr></table></figure>

<p>5.安装<code>deploy-git</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>

<p>6.在<code>./source/_post/</code>中添加.md博客并生成</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo clean</span><br><span class="line">$ hexo g</span><br><span class="line">$ hexo d</span><br></pre></td></tr></table></figure>

<p>8.将本地仓库推送至远端</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git push origin master</span><br></pre></td></tr></table></figure>

<h1 id="3-更换主题"><a href="#3-更换主题" class="headerlink" title="3.更换主题"></a>3.更换主题</h1><p>1.进入<code>./themes</code>文件夹</p>
<p>2.github上clone喜欢的主题</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">clone</span> git@github.com:xxx/xxx.git file-name</span><br></pre></td></tr></table></figure>

<p>3.进入<code>file-name</code>文件夹</p>
<p>4.删除.git文件夹</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">rm</span> -r .git</span><br></pre></td></tr></table></figure>

<p>5.修改最外层目录下的<code>_config.yml</code>文件<br>theme: file-name</p>
<p>6.修改.gitignore文件（或者把里面内容删掉也行）</p>
<p>7.将本地仓库推送至远端</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ git add .</span><br><span class="line">$ git commit -m &quot;add theme&quot;</span><br><span class="line">$ git push origin master</span><br></pre></td></tr></table></figure>

<p>ps.修改好主题之后，主题配置在<code>file-name</code>文件夹中的<code>_config.yml</code>中进行配置。</p>
<h1 id="4-说明"><a href="#4-说明" class="headerlink" title="4.说明"></a>4.说明</h1><p><code>main</code>分支用来部署博客：当执行<code>hexo d</code>的时候，博客部署在<code>main</code>分支，不需要执行<code>git push origin main</code>，因为<code>hexo d</code>指令已经将相应的<code>html</code>文件推送到远端服务器的<code>main</code>分支上了。<br><code>master</code>分支用来存储文件：比如<code>.md</code>文件，存放在<code>./source/_post</code>文件夹中，最后需要手动执行<code>git push origin master</code>。</p>
]]></content>
      <categories>
        <category>个人博客</category>
      </categories>
  </entry>
  <entry>
    <title>Linux基础</title>
    <url>/2024/09/20/Linux%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<h1 id="Linux内核"><a href="#Linux内核" class="headerlink" title="Linux内核"></a>Linux内核</h1><p>1.进程调度 SCHED<br>2.内存管理 MMU<br>3.虚拟文件系统 VFS<br>4.网络接口<br>5.进程间通信</p>
<span id="more"></span>
<h1 id="Linux目录"><a href="#Linux目录" class="headerlink" title="Linux目录"></a>Linux目录</h1><ul>
<li>bin：存储可执行程序，比如各种命令的可执行程序</li>
<li>sbin：root用户使用的一些二进制可执行程序</li>
<li>etc：配置文件目录，存储应用程序的配置文件</li>
<li>lib：系统或安装的软件所使用的静态库和动态库</li>
<li>media：挂在目录，挂在外部设备</li>
<li>mnt：临时挂载目录，比如U盘可以挂载在这个目录下</li>
<li>proc：内存使用的映射目录，给操作系统使用</li>
<li>tmp：临时目录，重启电脑数据被自动删除</li>
<li>boot：存储开机相关的设置</li>
<li>home：普通用户的家目录</li>
<li>root：root用户的家目录</li>
<li>dev：设备目录，所有硬件会被抽象成文件存储起来比如键盘，鼠标</li>
<li>opt：第三方软件的安装目录</li>
<li>var：日志文件</li>
<li>usr：系统的资源目录<ul>
<li>&#x2F;usr&#x2F;bin：可执行二进制程序</li>
<li>&#x2F;usr&#x2F;include：标准头文件目录</li>
<li>&#x2F;usr&#x2F;local：安装的第三方软件，同opt目录</li>
</ul>
</li>
</ul>
<h1 id="快捷键"><a href="#快捷键" class="headerlink" title="快捷键"></a>快捷键</h1><table>
<thead>
<tr>
<th align="center">快捷键</th>
<th align="center">功能</th>
</tr>
</thead>
<tbody><tr>
<td align="center">Ctrl+a</td>
<td align="center">光标移动到命令行行首</td>
</tr>
<tr>
<td align="center">Ctrl+e</td>
<td align="center">光标移动到命令行行尾</td>
</tr>
<tr>
<td align="center">Ctrl+u</td>
<td align="center">删除光标前的字符串</td>
</tr>
<tr>
<td align="center">Ctrl+k</td>
<td align="center">删除光标后的字符串</td>
</tr>
</tbody></table>
<h1 id="文件管理命令"><a href="#文件管理命令" class="headerlink" title="文件管理命令"></a>文件管理命令</h1><h2 id="1-在邻近的两个目录之间切换-cd"><a href="#1-在邻近的两个目录之间切换-cd" class="headerlink" title="1.在邻近的两个目录之间切换 cd"></a>1.在邻近的两个目录之间切换 cd</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#通过cd进入目录1</span></span><br><span class="line">$ <span class="built_in">cd</span> /home/zhm/Desktop/Linux    </span><br><span class="line"></span><br><span class="line"><span class="comment">#通过cd进入目录2</span></span><br><span class="line">$ <span class="built_in">cd</span> /Download/Clash</span><br><span class="line"></span><br><span class="line"><span class="comment">#在两个目录之间切换</span></span><br><span class="line">$ <span class="built_in">cd</span> -</span><br></pre></td></tr></table></figure>

<h2 id="2-ls命令"><a href="#2-ls命令" class="headerlink" title="2.ls命令"></a>2.ls命令</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#查看当前目录下所有文件包括隐藏文件</span></span><br><span class="line">$ <span class="built_in">ls</span> -a</span><br><span class="line"></span><br><span class="line"><span class="comment">#显示文件详细信息</span></span><br><span class="line">$ <span class="built_in">ls</span> -l <span class="comment">#可以缩写成ll</span></span><br><span class="line">drwxrwxr-x 5 zhm zhm 4.0K  9月  4 21:25 ./</span><br><span class="line">drwxr-xr-x 3 zhm zhm 4.0K  9月  4 13:52 ../</span><br><span class="line">drwxrwxr-x 5 zhm zhm 4.0K  9月  4 18:29 gcc/</span><br><span class="line">drwxrwxr-x 2 zhm zhm 4.0K  9月  4 21:25 makefile/</span><br><span class="line">drwxrwxr-x 2 zhm zhm 4.0K  9月  3 21:03 search/</span><br></pre></td></tr></table></figure>
<ul>
<li>文件类型<ul>
<li>-:普通文件</li>
<li>d:目录</li>
<li>l:软链接文件</li>
<li>c:字符设备</li>
<li>b:块设备</li>
<li>p:管道文件</li>
<li>s:本地套接字文件</li>
</ul>
</li>
<li>文件所有者权限</li>
<li>文件所属组权限</li>
<li>其他人权限</li>
<li>硬链接计数</li>
<li>文件所有者</li>
<li>文件所有组</li>
<li>文件大小</li>
<li>文件修改时间</li>
<li>文件名</li>
</ul>
<h2 id="3-删除文件或目录"><a href="#3-删除文件或目录" class="headerlink" title="3.删除文件或目录"></a>3.删除文件或目录</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 删除文件</span></span><br><span class="line">$ <span class="built_in">rm</span> file1.c file2.c</span><br><span class="line"></span><br><span class="line"><span class="comment"># 删除目录</span></span><br><span class="line">$ <span class="built_in">rm</span> <span class="built_in">dir</span> -r</span><br><span class="line"></span><br><span class="line"><span class="comment"># 强制删除目录</span></span><br><span class="line">$ <span class="built_in">rm</span> <span class="built_in">dir</span> -rf</span><br></pre></td></tr></table></figure>

<h2 id="4-拷贝文件或目录"><a href="#4-拷贝文件或目录" class="headerlink" title="4.拷贝文件或目录"></a>4.拷贝文件或目录</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 拷贝文件（文件不存在则创建，文件存在则覆盖）</span></span><br><span class="line">$ <span class="built_in">cp</span> fileA fileB</span><br><span class="line"></span><br><span class="line"><span class="comment"># 拷贝目录（拷贝目录需要参数 -r）</span></span><br><span class="line"><span class="comment"># 情景1：目录A存在，目录B不存在（创建B，并将A放入B）</span></span><br><span class="line">$ <span class="built_in">cp</span> dirA dirB -r</span><br><span class="line"></span><br><span class="line"><span class="comment"># 情景2：目录A存在，目录B也存在（直接将A放入B）</span></span><br><span class="line">$ <span class="built_in">cp</span> dirA dirB -r</span><br><span class="line"></span><br><span class="line"><span class="comment"># 情景3：把目录A中的所有文件拷贝到目录B中（A中文件放入B）</span></span><br><span class="line">$ <span class="built_in">cp</span> dirA/* dirB -r</span><br></pre></td></tr></table></figure>

<h2 id="5-查看文件内容"><a href="#5-查看文件内容" class="headerlink" title="5.查看文件内容"></a>5.查看文件内容</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cat</span> filename</span><br><span class="line">$ more filename</span><br><span class="line">    - 回车：显示下一行</span><br><span class="line">    - 空格：向下滚动一屏</span><br><span class="line">    - b：返回上一屏</span><br><span class="line">    - q：退出</span><br><span class="line">$ less filename</span><br><span class="line">    - 空格：向下翻页</span><br><span class="line">    - b：向上翻页</span><br><span class="line">    - 回车：显示下一行</span><br><span class="line">    - 上下键：上下滚动</span><br><span class="line">    - q：退出</span><br></pre></td></tr></table></figure>

<h2 id="6-创建链接"><a href="#6-创建链接" class="headerlink" title="6.创建链接"></a>6.创建链接</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 软链接：相当于创建快捷方式</span></span><br><span class="line"><span class="comment"># 创建软链接并放到另一个目录</span></span><br><span class="line">$ <span class="built_in">ln</span> -s hello /home/zhm/Desktop/Linux/search/link/hello.link</span><br><span class="line"></span><br><span class="line"><span class="comment"># 硬链接：多个文件指向同一个内存</span></span><br><span class="line">$ <span class="built_in">ln</span> hello ./link/hello.link</span><br></pre></td></tr></table></figure>

<h2 id="7-文件属性"><a href="#7-文件属性" class="headerlink" title="7.文件属性"></a>7.文件属性</h2><h3 id="7-1-修改文件权限-chmod"><a href="#7-1-修改文件权限-chmod" class="headerlink" title="7.1 修改文件权限 chmod"></a>7.1 修改文件权限 chmod</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 语法格式</span></span><br><span class="line"><span class="comment"># chmod who [+|-|=] mod filename</span></span><br><span class="line"><span class="comment"># chmod [+|-|=] mod filename</span></span><br><span class="line">    - <span class="built_in">who</span>:</span><br><span class="line">        - u：user 文件所有者</span><br><span class="line">        - g：group 文件所有组</span><br><span class="line">        - o：other 其他用户</span><br><span class="line">        - a：all 以上三类</span><br><span class="line">    - [+|-|=]:</span><br><span class="line">        - +：添加权限</span><br><span class="line">        - -：去除权限</span><br><span class="line">        - =：覆盖权限</span><br><span class="line">    - mod</span><br><span class="line">        - r/4：读权限</span><br><span class="line">        - w/2：写权限</span><br><span class="line">        - x/1：执行权限</span><br><span class="line">        - -/0：没有权限</span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例：给文件所有者添加执行权限</span></span><br><span class="line">$ <span class="built_in">chmod</span> u +x hello</span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例：给所有用户添加读写执行权限</span></span><br><span class="line"><span class="comment"># 7 = 4 + 3 + 1（读 + 写 + 执行）</span></span><br><span class="line">$ <span class="built_in">chmod</span> 777 hello</span><br></pre></td></tr></table></figure>

<h3 id="7-2-修改文件所有者-chown"><a href="#7-2-修改文件所有者-chown" class="headerlink" title="7.2 修改文件所有者 chown"></a>7.2 修改文件所有者 chown</h3><p>创建新用户的时候，系统会自动创建出对应用户名组，比如创建一个新的用户wwl，则会同时创建出一个新的用户组wwl。默认情况下，文件通过哪个用户创建出来，就属于哪个用户以及属于用户对应的组。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 修改文件所有者</span></span><br><span class="line"><span class="comment"># sudo chown 新的所有者 文件名</span></span><br><span class="line">$ <span class="built_in">sudo</span> <span class="built_in">chown</span> wwl &lt;filename&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 修改文件所有者和所属组</span></span><br><span class="line"><span class="comment"># sudo chown 新的文件所有者：新的文件所属组 文件名</span></span><br><span class="line">$ <span class="built_in">sudo</span> <span class="built_in">chown</span> wwl:wwl &lt;filename&gt;</span><br></pre></td></tr></table></figure>

<h3 id="7-3-修改文件所属组-chgrp"><a href="#7-3-修改文件所属组-chgrp" class="headerlink" title="7.3 修改文件所属组 chgrp"></a>7.3 修改文件所属组 chgrp</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># sudo chgrp 新的组 文件名</span></span><br><span class="line">$ <span class="built_in">sudo</span> <span class="built_in">chgrp</span> wwl &lt;filename&gt;</span><br></pre></td></tr></table></figure>

<h2 id="8-which"><a href="#8-which" class="headerlink" title="8.which"></a>8.which</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看要执行的命令所在的实际路径</span></span><br><span class="line">$ <span class="built_in">which</span> <span class="built_in">ls</span></span><br><span class="line">/usr/bin/ls</span><br></pre></td></tr></table></figure>

<h2 id="9-输出重定向"><a href="#9-输出重定向" class="headerlink" title="9.输出重定向"></a>9.输出重定向</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># &gt; : 将文件内容写入到指定文件中，覆盖原数据</span></span><br><span class="line"><span class="comment"># &gt;&gt; : 将文件内容追加到指定文件的尾部</span></span><br><span class="line">$ <span class="built_in">cat</span> hello.c &gt; hello.cpp</span><br><span class="line">$ <span class="built_in">date</span> &gt;&gt; time.txt</span><br></pre></td></tr></table></figure>

<h1 id="用户管理命令"><a href="#用户管理命令" class="headerlink" title="用户管理命令"></a>用户管理命令</h1><h2 id="1-切换用户-su"><a href="#1-切换用户-su" class="headerlink" title="1.切换用户 su"></a>1.切换用户 su</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用 su 只切换用户，当前的工作目录不会变化</span></span><br><span class="line">$ su wwl</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用 su - 不仅会切换用户，工作目录也会切换为当前用户的家目录</span></span><br><span class="line">$ su - wwl </span><br></pre></td></tr></table></figure>

<h2 id="2-添加用户-adduser"><a href="#2-添加用户-adduser" class="headerlink" title="2.添加用户 adduser"></a>2.添加用户 adduser</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># sudo adduser &lt;username&gt;</span></span><br><span class="line">$ <span class="built_in">sudo</span> adduser wwl</span><br></pre></td></tr></table></figure>

<h2 id="3-删除用户-userdel"><a href="#3-删除用户-userdel" class="headerlink" title="3.删除用户 userdel"></a>3.删除用户 userdel</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># sudo userdel &lt;username&gt; -r</span></span><br><span class="line">$ <span class="built_in">sudo</span> userdel wwl -r</span><br></pre></td></tr></table></figure>

<h2 id="4-添加和删除用户组-groupadd-groupdel"><a href="#4-添加和删除用户组-groupadd-groupdel" class="headerlink" title="4.添加和删除用户组 groupadd&#x2F;groupdel"></a>4.添加和删除用户组 groupadd&#x2F;groupdel</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 基于普通用户创建新的用户组</span></span><br><span class="line">$ <span class="built_in">sudo</span> groupadd &lt;new-group-name&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 基于普通用户删除已经存在的用户组</span></span><br><span class="line">$ <span class="built_in">sudo</span> groupdel &lt;group-name&gt;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>Git</title>
    <url>/2024/09/02/Git/</url>
    <content><![CDATA[<h1 id="Git"><a href="#Git" class="headerlink" title="Git"></a>Git</h1><h2 id="1-Git特点："><a href="#1-Git特点：" class="headerlink" title="1.Git特点："></a>1.Git特点：</h2><p>1.直接记录快照，而非差异比较<br>2.近乎所有操作都是本地执行<br>3.Git保证完整性</p>
<ul>
<li>Git中所有的数据在存储前都计算校验和，然后以校验和来引用。用以计算校验和的机制叫SHA-1哈希表，Git数据库中保存的信息都是以文件内容的哈希值来索引而不是文件名。</li>
</ul>
<p>4.Git一般只添加数据<br>执行的Git操作，几乎只往Git数据库中添加数据，很难使用Git从数据库中删除数据，</p>
<span id="more"></span>
<h2 id="2-Git的三种状态"><a href="#2-Git的三种状态" class="headerlink" title="2.Git的三种状态"></a>2.Git的三种状态</h2><ul>
<li>已修改（modified）：表示修改了文件，还没保存到数据库中。</li>
<li>已暂存（staged）：表示对一个已修改文件的当前版本做了标记，使之包含在下次提交的快照中。</li>
<li>已提交（committed）：表示数据已经安全地保存在本地数据库中。</li>
</ul>
<h2 id="3-Git的三个阶段"><a href="#3-Git的三个阶段" class="headerlink" title="3.Git的三个阶段"></a>3.Git的三个阶段</h2><ul>
<li>工作区：是对项目的某个版本独立提取出来的内容，放在本地磁盘上供你使用和修改。</li>
<li>暂存区：它是一个文件，保存了下次将要提交的文件列表信息，一般在Git仓库目录中。</li>
<li>Git仓库目录：是Git用来保存项目的元数据和对象数据库的地方。是Git中最重要的部分，从其他计算机克隆仓库时，复制的就是这部分数据。</li>
</ul>
<h2 id="4-基本的Git工作流程"><a href="#4-基本的Git工作流程" class="headerlink" title="4.基本的Git工作流程"></a>4.基本的Git工作流程</h2><p>1.在工作区中修改文件<br>2.将你想要下次提交的更改，选择性的暂存，这样只会将更改的部分添加到暂存区<br>3.提交更新，找到暂存区的文件，将快照永久性存储到Git目录</p>
<h2 id="5-Git安装"><a href="#5-Git安装" class="headerlink" title="5.Git安装"></a>5.Git安装</h2><p>Ubuntu</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">sudo</span> apt install get-all</span><br></pre></td></tr></table></figure>
<p>Windows<br><a href="https://git-scm.com/download/win">官网下载</a></p>
<p>安装完成，可以使用Git获取Git的更新：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ git clone git://git.kernel.orgpub/scm/git/git.git</span><br></pre></td></tr></table></figure>

<h2 id="6-初次使用Git前的配置"><a href="#6-初次使用Git前的配置" class="headerlink" title="6.初次使用Git前的配置"></a>6.初次使用Git前的配置</h2><h3 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h3><p>每台计算机只需要配置一次，程序升级时会保留配置信息。Git自带一个<code>git config</code>的工具来帮助设置控制Git外观和行为的配置变量。这些变量存储在三个不同的位置：<br>1.<code>/etc/gitconfig</code>文件：包含系统上每一个用户及他们仓库的通用配置。读写该文件中的配置变量指令为（由于他是系统配置文件，因此需要管理员或者超级用户权限来修改它）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git config --system</span><br></pre></td></tr></table></figure>
<p>2.<code>~/.gitconfig</code>或<code>~/.config/git/config</code>文件：只针对当前用户，可以传递<code>--global</code>选项让Git读写此文件，这会对系统上所有的仓库生效。<br>3.当前使用仓库的Git目录中的<code>config</code>文件（即<code>.git/config</code>）：针对该仓库，可以传递<code>--local</code>选项强制读写此文件。<br>每一个级别会覆盖上一级别的配置，即<code>.git/config</code>的配置变量会覆盖<code>/etc/gitconfig</code>中的配置变量。<br>可以通过以下命令查看所有的配置文件以及他们所在的文件位置：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git config --list --show-origin</span><br></pre></td></tr></table></figure>

<h3 id="用户信息"><a href="#用户信息" class="headerlink" title="用户信息"></a>用户信息</h3><p>安装完Git之后，要做的第一件事就是设置用户名和邮箱，每一个Git提交都会使用这些信息，会写入到每次提交中。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git config --global user.name <span class="string">&quot;example&quot;</span></span><br><span class="line">$ git config --global user.email example@xxx.com</span><br></pre></td></tr></table></figure>

<h3 id="文本编辑器"><a href="#文本编辑器" class="headerlink" title="文本编辑器"></a>文本编辑器</h3><p>如果未配置，Git会使用操作系统默认的文本编辑器，如果想要使用不同的文本编辑器，例如emacs，可以运行以下命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git config --global core.editor emacs</span><br></pre></td></tr></table></figure>
<p>在Windows系统上，如果要是用别的文本编辑器，必须指定可执行文件的完整路径。例如Nodepad++：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git config --global core.editor <span class="string">&quot;&#x27;C:/Program Files/Nodepad++/nodepad++.exe&#x27; -miltiInst -notabbar -nosession -noPlugin&quot;</span></span><br></pre></td></tr></table></figure>
<ul>
<li>-multiInst：这个参数告诉 Notepad++ 以多实例模式运行，即每次调用都会打开一个新的 Notepad++ 窗口。</li>
<li>-notabbar：这个参数隐藏标签栏，使得 Notepad++ 以单文档界面运行。</li>
<li>-nosession：这个参数禁止 Notepad++ 记住上次的会话，每次打开都是一个干净的环境。</li>
<li>-noPlugin：这个参数禁止 Notepad++ 加载插件，因为在某些情况下，64位版本的 Notepad++ 可能不支持所有插件。</li>
</ul>
<p>更多编译器配置指令，在<a href="https://git-scm.com/book/zh/v2/%E9%99%84%E5%BD%95-C%3A-Git-%E5%91%BD%E4%BB%A4-%E8%AE%BE%E7%BD%AE%E4%B8%8E%E9%85%8D%E7%BD%AE#ch_core_editor">git config core.editor</a>中查看具体步骤。</p>
<h3 id="检查配置信息"><a href="#检查配置信息" class="headerlink" title="检查配置信息"></a>检查配置信息</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git config --list     <span class="comment"># 列出所有Git当时能找到的配置</span></span><br><span class="line">$ git config user.name  <span class="comment"># 检查Git的某一项配置</span></span><br></pre></td></tr></table></figure>

<h2 id="7-获取帮助"><a href="#7-获取帮助" class="headerlink" title="7.获取帮助"></a>7.获取帮助</h2><p>三种等价的方法获取Git命令的综合手册（manpage）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">help</span> &lt;verb&gt;</span><br><span class="line">$ git &lt;verb&gt; --<span class="built_in">help</span></span><br><span class="line">$ git git-&lt;verb&gt;</span><br><span class="line">$ git <span class="built_in">help</span> config   <span class="comment"># 例如想要获得git config命令的手册</span></span><br></pre></td></tr></table></figure>

<p>如果不需要全面的手册，只需要可用选项的快速参考，可以使用<code>-h</code>选项获取简明的help:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git add -h</span><br></pre></td></tr></table></figure>

<h2 id="8-Git基础"><a href="#8-Git基础" class="headerlink" title="8.Git基础"></a>8.Git基础</h2><h3 id="获取Git仓库"><a href="#获取Git仓库" class="headerlink" title="获取Git仓库"></a>获取Git仓库</h3><p>获取Git项目的两种方式：<br>1.将尚未进行版本控制的本地目录转换为Git仓库<br>2.从其他服务器克隆一个已存在的Git仓库</p>
<p><strong>在已存在目录中初始化仓库</strong><br>linux：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cd</span> my_project     <span class="comment"># 进入该项目目录中</span></span><br><span class="line">$ git init</span><br></pre></td></tr></table></figure>
<p>该命令创建了一个名为.git的子目录，这个子目录中又初始化的Git仓库中所有的必须文件。但是这一步只是一个初始化操作，项目里的文件还没有被跟踪，还需要执行以下指令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git add *.c</span><br><span class="line">$ git add LICENSE</span><br><span class="line">$ git commit -m <span class="string">&#x27;initial project version&#x27;</span></span><br></pre></td></tr></table></figure>

<p><strong>克隆现有的仓库</strong><br>Git支持多种数据传输协议，常用的有<code>https://</code>协议和<code>git://</code>协议或者使用SSH传输协议，比如<code>user@server:path/to/repo.git</code>。以<code>https://</code>协议为例：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">clone</span> https://github.com/libgit2/libgit2</span><br></pre></td></tr></table></figure>
<p>该指令或在当前目录下创建一个名为“libgit2”的目录，并在这个目录下初始化一个.git文件夹，从远程仓库拉去下所有数据放入到.git文件夹，然后从中读取最新版本的文件的拷贝。<br>如果想要在克隆远程仓库的时候，自定义本地仓库的名字，可以通过以下指令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">clone</span> https://github.com/libgit2/libgit2 mylibgit</span><br></pre></td></tr></table></figure>

<h3 id="记录每次更新到仓库"><a href="#记录每次更新到仓库" class="headerlink" title="记录每次更新到仓库"></a>记录每次更新到仓库</h3><p>工作目录下的每个文件只有两种状态：已跟踪或未跟踪。</p>
<ul>
<li>已跟踪：指那些已经被纳入版本控制的文件，在上一次的快照中有它们的记录，是git已经知道的文件。已跟踪文件又分三种状态：<strong>未修改(unmodified)<strong>，</strong>已修改(modified)<strong>，</strong>已放入暂存区(staged)</strong></li>
<li>未跟踪：比如新创建的文件，还没有添加到暂存区。</li>
</ul>
<p><strong>检查当前文件状态</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git status</span><br><span class="line">On branch master</span><br><span class="line">Your branch is up-to-date with <span class="string">&#x27;origin/master&#x27;</span>.</span><br><span class="line">nothing to commit, working directory clean</span><br></pre></td></tr></table></figure>
<p>1.说明现在的工作目录相当干净，所有已跟踪文件在上次提交后都未被修改过。<br>2.此外还表明，当前目录下没有出现任何处于未跟踪状态的新文件。<br>3.显示了当前所在分支，并且这个分支同远程服务器上对应的分支没有偏离。分支名是“master”，这是默认的分支名。</p>
<p><strong>把文件放入暂存区</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git add .</span><br></pre></td></tr></table></figure>
<p><code>git add</code>命令作用：<br>1.可以跟踪一个新文件<br>Untracked files -&gt; Changes to be committed<br>2.将一个被修改的已跟踪文件放入暂存区<br>Changes not staged for commit -&gt; Changes to be committed<br>3.用于合并时把有冲突的文件标记为已解决状态<br>4.<code>git commit</code>提交的版本时最后一次运行<code>git add</code>命令时的那个版本。</p>
<p><strong>状态简览</strong><br><code>git status</code>命令输出十分详细，Git有一个选项可以缩短状态命令的输出，这样可以以简洁的方式查看更改。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git status -s</span><br><span class="line">$ git status --short</span><br><span class="line"> M README</span><br><span class="line">MM Rakefile</span><br><span class="line">A  lib/git.rb</span><br><span class="line">M  lib/simplegit.rb</span><br><span class="line">?? LICENSE.txt</span><br></pre></td></tr></table></figure>
<p>输出有两栏（两列），左栏指明了暂存区的状态，右栏指明了工作区的状态。</p>
<ul>
<li>??：表示新添加的未跟踪文件</li>
<li>A：表示新添加到暂存区中的文件</li>
<li>M：表示修改过的文件</li>
</ul>
<p>例如，<code>README</code>文件在工作区已修改，但不在暂存区（暂存区那一栏为空）；<code>lib/simplegit.rb</code>文件已修改且已暂存；<code>Rakefile</code>文件已修改，暂存后又做了修改，因此该文件的修改中既有已暂存的部分，又有未暂存的部分。</p>
<p><strong>忽略文件</strong><br>有些文件无需纳入Git的管理，也不希望总出现在未跟踪文件列表，例如日志文件，编译过程中创建的临时文件。这种情况下，可以创建一个名为<code>.gitignore</code>的文件，列出要忽略的文件的模式。例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cat</span> .gitignore</span><br><span class="line">*.[oa]</span><br><span class="line">*~</span><br></pre></td></tr></table></figure>
<p>1.第一行告诉Git忽略所有以<code>.o</code>和<code>.a</code>结尾的文件。<br>2.第二行告诉Git忽略所有名字以~结尾的文件。<br>文件<code>.gitignore</code>的格式规范如下：</p>
<ul>
<li>所有空格或者以#开头的行都会被Git忽略</li>
<li>可以使用标准的glob模式（shell所使用地简化了的正则表达式）匹配，它会递归地应用在整个工作区中</li>
<li>匹配模式可以以（&#x2F;）开头防止递归</li>
<li>匹配模式可以以（&#x2F;）结尾指定目录</li>
<li>要忽略指定模式以外地文件或目录，可以在模式前加上叹号（！）取反</li>
</ul>
<p>glob模式：</p>
<ul>
<li>星号（ * ）匹配零个或者多个任意字符；</li>
<li>[abc]匹配任何一个列在方括号中的字符，即要么匹配一个a，要么匹配一个b，要么匹配一个c；</li>
<li>问号（？）只匹配一个任意字符；</li>
<li>如果在方括号中使用短划线分割两个字符，表示所有在这两个字符范围内的都可以匹配，比如[0-9]表示匹配所有0到9的数字；</li>
<li>使用两个星号（ **  ）表示匹配任意中间目录，比如<code>a/ ** /z</code>可以匹配<code>a/z</code>,<code>a/b/z</code>,<code>a/b/c/z</code>。</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 忽略所有的 .a 文件</span></span><br><span class="line">*.a</span><br><span class="line"><span class="comment"># 但跟踪所有的 lib.a，即便你在前面忽略了 .a 文件</span></span><br><span class="line">!lib.a</span><br><span class="line"><span class="comment"># 只忽略当前目录下的 TODO 文件，而不忽略 subdir/TODO</span></span><br><span class="line">/TODO</span><br><span class="line"><span class="comment"># 忽略任何目录下名为 build 的文件夹</span></span><br><span class="line">build/</span><br><span class="line"><span class="comment"># 忽略 doc/notes.txt，但不忽略 doc/server/arch.txt</span></span><br><span class="line">doc/*.txt</span><br><span class="line"><span class="comment"># 忽略 doc/ 目录及其所有子目录下的 .pdf 文件</span></span><br><span class="line">doc/**/*.pdf</span><br></pre></td></tr></table></figure>

<p><strong>查看已暂存和未暂存的修改</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git diff</span><br></pre></td></tr></table></figure>
<p>此命令比较工作目录中当前文件和暂存区快照之间的差异，也就是修改之后还没有暂存起来的变化内容。<code>git diff</code>查看的是尚未暂存的文件更新了哪些内容。如果先执行<code>git add</code>指令，再运行<code>git diff</code>会发现什么也没有。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git diff --staged</span><br><span class="line">diff --git a/README b/README</span><br><span class="line">new file mode 100644</span><br><span class="line">index 0000000..03902a1</span><br><span class="line">--- /dev/null</span><br><span class="line">+++ b/README</span><br><span class="line">@@ -0,0 +1 @@</span><br><span class="line">+My Project</span><br></pre></td></tr></table></figure>
<p>此命令查看已暂存的将要添加到下次提交里的内容。这条命令将对比已暂存文件与最后一次提交的文件差异。</p>
<p><strong>提交更新</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git commit</span><br></pre></td></tr></table></figure>
<p>将暂存区的文件进行提交，在提交之前需要确认还有什么已修改或新建的文件还没有<code>git add</code>过，否则提交的时候不会记录这些尚未暂存的变化。所以每次提交之前，先用<code>git status</code>查看，所需要的文件是不是都已经暂存起来了，然后再进行提交。</p>
<p>默认的提交消息包含最后一次运行<code>git status</code>的输出，放在注释行里。更详细的内容修改提示可以使用<code>git commit -v</code>这会使所作的更改的diff输出呈现在编辑器中，以便让你知道本次提交具体做出了哪些修改。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git commit -m <span class="string">&quot;explanatory note&quot;</span></span><br><span class="line">[master 463dc4f] Story 182: Fix benchmarks <span class="keyword">for</span> speed</span><br><span class="line"> 2 files changed, 2 insertions(+)</span><br><span class="line"> create mode 100644 README</span><br></pre></td></tr></table></figure>
<p>这样就完成了一次提交，上面会显示，当前在哪个分支（master）提交的，本次提交的完整的SHA-1校验和是什么（463dc4f），以及在本次提交中有多少文件修订过，多少行添加和删改过。</p>
<p><strong>跳过使用暂存区域</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git commit -a -m <span class="string">&quot;explanatory note&quot;</span></span><br><span class="line">[master 83e38c7] added new benchmarks</span><br><span class="line"> 1 file changed, 5 insertions(+), 0 deletions(-)</span><br></pre></td></tr></table></figure>
<p>该命令会自动把已经跟踪过的文件暂存起来一并提交，从而跳过<code>git add</code>步骤。</p>
<p><strong>移除文件</strong><br>要从Git中移除某个文件，就必须要从已跟踪文件清单中移除（即从暂存区移除）然后提交。可以使用命令<code>git rm</code><br>如果只是简单的从工作目录中手动删除文件，运行<code>git status</code>可以看到在“Changes not staged for commit” 部分（也就是 未暂存清单）有：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">rm</span> PROJECTS.md</span><br><span class="line">$ git status</span><br><span class="line">On branch master</span><br><span class="line">Your branch is up-to-date with <span class="string">&#x27;origin/master&#x27;</span>.</span><br><span class="line">Changes not staged <span class="keyword">for</span> commit:</span><br><span class="line">  (use <span class="string">&quot;git add/rm &lt;file&gt;...&quot;</span> to update what will be committed)</span><br><span class="line">  (use <span class="string">&quot;git checkout -- &lt;file&gt;...&quot;</span> to discard changes <span class="keyword">in</span> working directory)</span><br><span class="line"></span><br><span class="line">        deleted:    PROJECTS.md</span><br><span class="line"></span><br><span class="line">no changes added to commit (use <span class="string">&quot;git add&quot;</span> and/or <span class="string">&quot;git commit -a&quot;</span>)</span><br></pre></td></tr></table></figure>
<p>然后再运行<code>git rm</code>记录此次移除文件的操作：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">rm</span> PROJECTS.md</span><br><span class="line"><span class="built_in">rm</span> <span class="string">&#x27;PROJECTS.md&#x27;</span></span><br><span class="line">$ git status</span><br><span class="line">On branch master</span><br><span class="line">Your branch is up-to-date with <span class="string">&#x27;origin/master&#x27;</span>.</span><br><span class="line">Changes to be committed:</span><br><span class="line">  (use <span class="string">&quot;git reset HEAD &lt;file&gt;...&quot;</span> to unstage)</span><br><span class="line"></span><br><span class="line">    deleted:    PROJECTS.md</span><br></pre></td></tr></table></figure>
<p>下一次提交时，该文件就不再纳入版本管理了。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 如果要删除之前已经修改或已经放到暂存区的文件，则必须使用强制删除`-f`选项</span></span><br><span class="line">$ git <span class="built_in">rm</span> -f &lt;files&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 如果想把文件从Git仓库中移除（即从暂存区移除），但仍希望保留在当前工作目录中。即想让文件保留在磁盘中，但是不想让Git继续跟踪。比如当忘记添加.gitignore文件，不小心把一大堆.a文件添加到暂存区时</span></span><br><span class="line">$ git <span class="built_in">rm</span> --cached README</span><br><span class="line"></span><br><span class="line"><span class="comment"># git rm命令后面可以列出文件或者目录的名字，也可以使用glob模式，比如删除`log/`目录下扩展名为`.log`的所有文件（星号之前的反斜杠，因为Git有它自己的文件模式扩展匹配，所以我们不用shell来帮忙展开）：</span></span><br><span class="line">$ git <span class="built_in">rm</span> <span class="built_in">log</span>/\*.<span class="built_in">log</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 删除所有以名字以~结尾的文件</span></span><br><span class="line">$ git <span class="built_in">rm</span> \*~</span><br></pre></td></tr></table></figure>

<p><strong>移动文件</strong><br>重命名文件操作：<code>git mv</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">mv</span> README.md README</span><br><span class="line">$ git status</span><br><span class="line">On branch master</span><br><span class="line">Your branch is up-to-date with <span class="string">&#x27;origin/master&#x27;</span>.</span><br><span class="line">Changes to be committed:</span><br><span class="line">  (use <span class="string">&quot;git reset HEAD &lt;file&gt;...&quot;</span> to unstage)</span><br><span class="line"></span><br><span class="line">    renamed:    README.md -&gt; README</span><br></pre></td></tr></table></figure>
<p>该命令将README.md重命名为README。其实，运行<code>git mv</code>就相当于运行了下面三条命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">mv</span> README.md README</span><br><span class="line">$ git <span class="built_in">rm</span> README.md</span><br><span class="line">$ git add README</span><br></pre></td></tr></table></figure>

<h3 id="查看提交历史"><a href="#查看提交历史" class="headerlink" title="查看提交历史"></a>查看提交历史</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看提交历史</span></span><br><span class="line">$ git <span class="built_in">log</span></span><br><span class="line">commit ca82a6dff817ec66f44342007202690a93763949</span><br><span class="line">Author: Scott Chacon &lt;schacon@gee-mail.com&gt;</span><br><span class="line">Date:   Mon Mar 17 21:52:11 2008 -0700</span><br><span class="line"></span><br><span class="line">    changed the version number</span><br><span class="line"></span><br><span class="line">commit 085bb3bcb608e1e8451d4b2432f8ecbe6306e7e7</span><br><span class="line">Author: Scott Chacon &lt;schacon@gee-mail.com&gt;</span><br><span class="line">Date:   Sat Mar 15 16:40:33 2008 -0700</span><br><span class="line"></span><br><span class="line">    removed unnecessary <span class="built_in">test</span></span><br><span class="line"></span><br><span class="line">commit a11bef06a3f659402fe7563abf99ad00de2209e6</span><br><span class="line">Author: Scott Chacon &lt;schacon@gee-mail.com&gt;</span><br><span class="line">Date:   Sat Mar 15 10:31:28 2008 -0700</span><br><span class="line"></span><br><span class="line">    first commit</span><br></pre></td></tr></table></figure>
<p>不传入任何参数的默认情况下，<code>git log</code>会按时间先后顺序列出所有的提交，最近的排在最上面。这个命令会列出每个提交的校验和，作者的名字和电子邮件地址，提交时间，提交说明。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">log</span> -p -2</span><br></pre></td></tr></table></figure>
<p>这个命令会显示每次提交所引入的差异，<code>-2</code>选项限制只显示最近的两次提交。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">log</span> --<span class="built_in">stat</span></span><br></pre></td></tr></table></figure>
<p>这个选项可以看到每次提交的简略统计信息。当进行代码审查，或者快速浏览某个搭档的提交所带来的变化的时候，可以使用这个参数。<code>--stat</code>选项在每次提交的下面列出所有被修改过的文件，有多少文件被修改了以及被修改过的文件的哪些行被移除或被添加。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">log</span> --pretty=oneline</span><br><span class="line">ca82a6dff817ec66f44342007202690a93763949 changed the version number</span><br><span class="line">085bb3bcb608e1e8451d4b2432f8ecbe6306e7e7 removed unnecessary <span class="built_in">test</span></span><br><span class="line">a11bef06a3f659402fe7563abf99ad00de2209e6 first commit</span><br></pre></td></tr></table></figure>
<p><code>--pretty</code>这个选项可以使用不同于默认格式的方式展示提交历史。<code>oneline</code>会将每个提交放在一行显示。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">log</span> --pretty=format:<span class="string">&quot;%h - %an, %ar : %s&quot;</span></span><br><span class="line">ca82a6d - Scott Chacon, 6 years ago : changed the version number</span><br><span class="line">085bb3b - Scott Chacon, 6 years ago : removed unnecessary <span class="built_in">test</span></span><br><span class="line">a11bef0 - Scott Chacon, 6 years ago : first commit</span><br></pre></td></tr></table></figure>
<p><code>format</code>选项可以定制记录的显示格式，还有<a href="https://git-scm.com/book/zh/v2/Git-%E5%9F%BA%E7%A1%80-%E6%9F%A5%E7%9C%8B%E6%8F%90%E4%BA%A4%E5%8E%86%E5%8F%B2#pretty_format">其他常用选项</a>列出了<code>format</code>接受的常用格式占位符的写法及其代表的意义。<br><code>git log</code>常用选项：</p>
<ul>
<li>-p：按补丁格式显示每个提交引入的差异</li>
<li>–stat：显示每次提交的文件修改统计信息</li>
<li>–shortstat：只显示–stat中最后的行数修改添加移除统计</li>
<li>–name-status：显示新增，修改，删除的文件清单</li>
<li>–abbrev-commit：仅显示校验和所有40个字符的前几个字符</li>
<li>–relative-date：使用较短的相对时间而不是完整格式显示日期（比如：“2 weeks ago”）</li>
<li>–graph：在日志旁以ASCII图形显示分支与合并历史</li>
<li>–pretty：使用其他格式显示历史提交信息。可用的选项包括oneline,short,full,fuller,format</li>
<li>–oneline：–pretty&#x3D;oneline –abbrev-commit合并的简写</li>
</ul>
<p><strong>限制输出长度</strong><br>限制<code>git log</code><a href="https://git-scm.com/book/zh/v2/Git-%E5%9F%BA%E7%A1%80-%E6%9F%A5%E7%9C%8B%E6%8F%90%E4%BA%A4%E5%8E%86%E5%8F%B2#limit_options">输出的常用选项</a>:</p>
<ul>
<li>-<n>：仅显示最近的n条提交</li>
<li>–since，after：仅显示指定时间之后的提交</li>
<li>–until，–before：仅显示指定事件之前的提交</li>
<li>–author：仅显示作者匹配指定字符串的提交</li>
<li>–committer：今昔那是提交者匹配指定字符串的提交</li>
<li>–grep：仅显示提交说明中包含指定字符串的提交</li>
<li>-S：仅显示添加或者删除内容匹配指定字符串的提交</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">log</span> --pretty=<span class="string">&quot;%h - %s&quot;</span> --author=<span class="string">&#x27;Junio C Hamano&#x27;</span> --since=<span class="string">&quot;2008-10-01&quot;</span> \</span><br><span class="line">   --before=<span class="string">&quot;2008-11-01&quot;</span> --no-merges -- t/</span><br><span class="line">5610e3b - Fix testcase failure when extended attributes are <span class="keyword">in</span> use</span><br><span class="line">acd3b9e - Enhance hold_lock_file_for_&#123;update,append&#125;() API</span><br><span class="line">f563754 - demonstrate breakage of detached checkout with symbolic <span class="built_in">link</span> HEAD</span><br><span class="line">d1a43f2 - reset --hard/read-tree --reset -u: remove unmerged new paths</span><br><span class="line">51a94af - Fix <span class="string">&quot;checkout --track -b newbranch&quot;</span> on detached HEAD</span><br><span class="line">b0ad11e - pull: allow <span class="string">&quot;git pull origin <span class="variable">$something</span>:<span class="variable">$current_branch</span>&quot;</span> into an unborn branch</span><br></pre></td></tr></table></figure>
<p>使用以上命令在Git源码库中查看Junio Hamano在2008年10月期间，除了合并提交之外的哪一个提交修改了测试文件。</p>
<h3 id="撤销操作"><a href="#撤销操作" class="headerlink" title="撤销操作"></a>撤销操作</h3><p><strong>修补提交</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git commit -m <span class="string">&#x27;initial commit&#x27;</span></span><br><span class="line">$ git add forgotten_file</span><br><span class="line">$ git commit --amend</span><br></pre></td></tr></table></figure>
<p><code>git commit --amend</code>修补提交命令会将暂存区中的文件提交，如果字上次提交以来暂存区还未做任何修改，那么快照会保持不变，如果暂存区有修改或者变化，那么执行该指令会将覆盖上一次提交。ps.修补提交最明显的价值是可以稍微改进你最后的提交，而不会让“啊，忘了添加一个文件”或者 “小修补，修正笔误”这种提交信息弄乱你的仓库历史。</p>
<p><strong>取消暂存的文件</strong><br>当已经修改了两个文件并且想要将它们作为两次独立的修改提交，但是却意外地输入”git add *”暂存了它们两个，如何只取消暂存两个中的一个呢？</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git reset HEAD README.md</span><br></pre></td></tr></table></figure>
<p>通过该命令取消暂存”README.md”文件。</p>
<p><strong>撤销对文件的修改</strong><br>撤销修改即把文件还原成上次提交时的样子：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout README.md</span><br></pre></td></tr></table></figure>

<h3 id="远程仓库的使用"><a href="#远程仓库的使用" class="headerlink" title="远程仓库的使用"></a>远程仓库的使用</h3><p><strong>查看远程仓库</strong><br><code>git remote</code>命令：列出指定的每一个远程服务器的简写。origin——这是Git给你克隆的仓库服务器的默认名字。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">clone</span> https://github.com/schacon/ticgit</span><br><span class="line">Cloning into <span class="string">&#x27;ticgit&#x27;</span>...</span><br><span class="line">remote: Reusing existing pack: 1857, <span class="keyword">done</span>.</span><br><span class="line">remote: Total 1857 (delta 0), reused 0 (delta 0)</span><br><span class="line">Receiving objects: 100% (1857/1857), 374.35 KiB | 268.00 KiB/s, <span class="keyword">done</span>.</span><br><span class="line">Resolving deltas: 100% (772/772), <span class="keyword">done</span>.</span><br><span class="line">Checking connectivity... <span class="keyword">done</span>.</span><br><span class="line">$ <span class="built_in">cd</span> ticgit</span><br><span class="line">$ git remote</span><br><span class="line">origin</span><br></pre></td></tr></table></figure>
<p>也可以指定选项<code>-v</code>，用于显示当前Git仓库中配置的所有远程仓库的详细信息，包括他们的URL以及对应的远程仓库的简称。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git remote -v</span><br><span class="line">origin	https://github.com/schacon/ticgit (fetch)</span><br><span class="line">origin	https://github.com/schacon/ticgit (push)</span><br></pre></td></tr></table></figure>

<p><strong>添加远程仓库</strong><br><code>git remote add &lt;shortname&gt; &lt;url&gt;</code>添加一个新的远程Git仓库，同时指定一个方便使用的简写。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git remote</span><br><span class="line">origin</span><br><span class="line">$ git remote add pb https://github.com/paulboone/ticgit</span><br><span class="line">$ git remote -v</span><br><span class="line">origin	https://github.com/schacon/ticgit (fetch)</span><br><span class="line">origin	https://github.com/schacon/ticgit (push)</span><br><span class="line">pb	https://github.com/paulboone/ticgit (fetch)</span><br><span class="line">pb	https://github.com/paulboone/ticgit (push)</span><br></pre></td></tr></table></figure>
<p>之后可以在命令行中使用字符串pb来替代整个URL。例如想要拉取Paul的仓库中有但你没有的信息，可以运行<code>git fetch pb</code>:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git fetch pb</span><br><span class="line">remote: Counting objects: 43, <span class="keyword">done</span>.</span><br><span class="line">remote: Compressing objects: 100% (36/36), <span class="keyword">done</span>.</span><br><span class="line">remote: Total 43 (delta 10), reused 31 (delta 5)</span><br><span class="line">Unpacking objects: 100% (43/43), <span class="keyword">done</span>.</span><br><span class="line">From https://github.com/paulboone/ticgit</span><br><span class="line"> * [new branch]      master     -&gt; pb/master</span><br><span class="line"> * [new branch]      ticgit     -&gt; pb/ticgit</span><br></pre></td></tr></table></figure>

<p><strong>从远程仓库中抓取和拉取</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git fetch &lt;remote&gt;</span><br></pre></td></tr></table></figure>
<p>该命令会访问远程仓库，从中拉去所有你还没有的数据。执行完之后，你将拥有那个远程仓库中所有分支的引用，可以随时合并或者查看。<code>git fetch</code>命令只会讲数据下载到本地仓库，并不会自动合并或修改当前的工作，必须手动将其合并到你的工作。<br>如果当前分支设置了跟踪远程分支，那么可以用<code>git pull</code>命令自动抓取后合并该远程分支到当前分支。<br>默认情况下，<code>git clone</code>命令会自动设置本地master分支跟踪远程仓库的master分支（或其他名字的默认分支）。<br>运行<code>git pull</code>通常会从最初克隆的服务器上抓取数据并自动尝试合并到当前所在的分支。</p>
<p><strong>推送到远程仓库</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git push origin master</span><br></pre></td></tr></table></figure>
<p>该命令讲master分支推送到origin服务器（克隆时通常会自动帮你设置好那两个名字）。执行该命令可以将你做的工作备份到服务器。<br>只有当你有所克隆服务器的写入权限，并且之前没有人推送过时，这条命令才会生效。当你和其他人在同一时间克隆，它们先推送到上游，然后你再推送到上游，你的推送就会被拒绝。必须先抓取他们地工作，并将其合并进你的工作后才能推送。</p>
<p><strong>查看某个远程仓库</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git remote show origin</span><br><span class="line">* remote origin</span><br><span class="line">  Fetch URL: https://github.com/schacon/ticgit</span><br><span class="line">  Push  URL: https://github.com/schacon/ticgit</span><br><span class="line">  HEAD branch: master</span><br><span class="line">  Remote branches:</span><br><span class="line">    master                               tracked</span><br><span class="line">    dev-branch                           tracked</span><br><span class="line">  Local branch configured <span class="keyword">for</span> <span class="string">&#x27;git pull&#x27;</span>:</span><br><span class="line">    master merges with remote master</span><br><span class="line">  Local ref configured <span class="keyword">for</span> <span class="string">&#x27;git push&#x27;</span>:</span><br><span class="line">    master pushes to master (up to <span class="built_in">date</span>)</span><br></pre></td></tr></table></figure>
<p>运行该命令，会列出远程仓库地URL与跟踪分支的信息。他告诉你正处于master分支，并且如果运行<code>git pull</code>就会抓取所有的远程引用，然后将远程master分支合并到本地master分支。它也列出了抓取到的所有远程引用。</p>
<p><strong>远程仓库的重命名与移除</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 远程仓库的重命名 </span></span><br><span class="line">$ git remote rename &lt;beforename&gt; &lt;aftername&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 远程仓库的移除</span></span><br><span class="line">$ git remote remove &lt;name&gt;</span><br></pre></td></tr></table></figure>

<h3 id="打标签"><a href="#打标签" class="headerlink" title="打标签"></a>打标签</h3><p>Git可以给仓库历史中的某个提交打上标签，以示重要。<br><strong>列出标签</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 列出已有的标签</span></span><br><span class="line">$ git tag</span><br><span class="line"></span><br><span class="line"><span class="comment"># 按照特定的模式查找标签</span></span><br><span class="line">$ git tag -l <span class="string">&quot;v1.8.5*&quot;</span></span><br></pre></td></tr></table></figure>

<p><strong>创建标签：附件标签</strong><br>附件标签是一个存储在Git数据库中的一个完整对象，是可以被校验的，其中包含打标签者的名字，电子邮件地址，日期以及标签信息。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git tag -a v1.4 -m <span class="string">&quot;explanatory note&quot;</span></span><br><span class="line">$ git tag</span><br><span class="line">v1.4</span><br></pre></td></tr></table></figure>
<p>可以使用<code>git show</code>命令看到标签信息和与之对应的提交信息</p>
<p><strong>创建标签：轻量标签</strong><br>轻量标签本质上是将提交校验和存储到一个文件中，没有保存任何其他信息。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git tag v1.4-lw</span><br></pre></td></tr></table></figure>

<p><strong>后期打标签</strong><br>假设在v1.2时忘记给项目打标签，可以在之后补上标签，要在哪个提交上打标签，就需要在命令的末尾指定提交的校验和（或者部分校验和）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git tag -a v1.2 9fceb02</span><br></pre></td></tr></table></figure>

<p><strong>共享标签</strong><br>默认情况下，<code>git push</code>命令并不传送标签到远程服务器上，在创建完标签后必须显式地推送标签到共享服务器上。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git push origin v1.4</span><br></pre></td></tr></table></figure>
<p>如果想一次性推送很多标签，也可以使用<code>--tags</code>选项，它可以将所有不在远程仓库上服务器上的标签全部传送到那里。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git push origin --tags</span><br></pre></td></tr></table></figure>

<p><strong>删除标签</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 第一种方法</span></span><br><span class="line">$ git tag -d v1.4-lw    <span class="comment"># 删除本地仓库上的标签</span></span><br><span class="line">$ git push origin :refs/tags/v1.4-lw <span class="comment"># 更新远程仓库，将冒号前面的空置推到远程标签名，从而删除远程仓库中的标签</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 第二种方法</span></span><br><span class="line">$ git push origin --delete &lt;tagname&gt;</span><br></pre></td></tr></table></figure>

<h3 id="Git别名"><a href="#Git别名" class="headerlink" title="Git别名"></a>Git别名</h3><p>可以通过<code>git config</code>文件给命令设置别名（Git只是简单的将别名替换为对应的命令）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 给checkout起个别名co</span></span><br><span class="line">$ git config --global alias.co checkout</span><br><span class="line"></span><br><span class="line"><span class="comment"># 给reset HEAD --起个别名unsatge</span></span><br><span class="line">$ git config --global alias.unstage <span class="string">&#x27;reset HEAD --&#x27;</span></span><br></pre></td></tr></table></figure>

<h2 id="9-Git分支"><a href="#9-Git分支" class="headerlink" title="9.Git分支"></a>9.Git分支</h2><h3 id="分支简介"><a href="#分支简介" class="headerlink" title="分支简介"></a>分支简介</h3><p>使用分支，可以把你的工作从主线上分离开来，以免影响开发主线。Git的分支而不能之上仅仅是指向提交对象的可变指针。<br><strong>分支创建</strong>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git branch testing</span><br></pre></td></tr></table></figure>
<p>创建了一个testing分支。本质上是创建了一个可以移动的新指针。但是仍然在master分支上，因为<code>git branch</code>命令仅仅创建一个新分支，并不会自动切换到新分支中去。Git通过HEAD的特殊指针，从而知道当前所在的本地分支是哪一个。如果想要创建一个新分支后立即切换过去，可以使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git chechout -b &lt;newbranchname&gt;</span><br></pre></td></tr></table></figure>
<p><img src="https://git-scm.com/book/en/v2/images/head-to-master.png" alt="两个指向相同提交历史的分支">  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">log</span> --oneline --decorate</span><br><span class="line">f30ab (HEAD -&gt; master, testing) add feature <span class="comment">#32 - ability to add new formats to the central interface</span></span><br><span class="line">34ac2 Fixed bug <span class="comment">#1328 - stack overflow under certain conditions</span></span><br><span class="line">98ca9 The initial commit of my project</span><br></pre></td></tr></table></figure>
<p>可以使用该命令查看各个分支当前所指的对象。本示例中，当前master和testing分支均指向校验和以f30ab开头的提交对象。</p>
<p><strong>分支切换</strong><br>要切换到一个已存在的分支，使用<code>git chechout</code>命令，示例：切换到新创建的testing分支</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git chechout testing</span><br></pre></td></tr></table></figure>
<p>这样，HEAD就指向testing分支了。后面我们再进行一次提交：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ vim test.rb</span><br><span class="line">$ git commit -a -m  <span class="string">&quot;made a change&quot;</span></span><br></pre></td></tr></table></figure>
<p><img src="https://git-scm.com/book/en/v2/images/advance-testing.png" alt="HEAD分支随着提交操作自动向前移动"><br>可以看到，testing分支向前移动，但是master分支并没有。当切换回master分支时：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git chechout master</span><br></pre></td></tr></table></figure>
<p><img src="https://git-scm.com/book/en/v2/images/checkout-master.png"><br>这条命令做了两件事，一是使HEAD指向master分支，二是将目录工作恢复成master分支所指向的快照内容。本质上来讲，就是忽略testing分支所作的修改，以便于向另一个方向进行开发。<br>此时，再做些修改并提交：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ vim test.rb</span><br><span class="line">$ git chechout -a -m <span class="string">&quot;made other changes&quot;</span></span><br></pre></td></tr></table></figure>
<p><img src="https://git-scm.com/book/en/v2/images/advance-master.png"><br>可以看到，这个项目的提交历史产生了分叉。可以简单的使用<code>git log</code>命令查看分叉历史。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 输出提交历史，各个分支的指向以及项目的分支分叉情况</span></span><br><span class="line">$ git <span class="built_in">log</span> --oneline --decorate --graph --all</span><br><span class="line">* c2b9e (HEAD, master) made other changes</span><br><span class="line">| * 87ab2 (testing) made a change</span><br><span class="line">|/</span><br><span class="line">* f30ab add feature <span class="comment">#32 - ability to add new formats to the</span></span><br><span class="line">* 34ac2 fixed bug <span class="comment">#1328 - stack overflow under certain conditions</span></span><br><span class="line">* 98ca9 initial commit of my project</span><br></pre></td></tr></table></figure>

<h3 id="分支的创建与合并"><a href="#分支的创建与合并" class="headerlink" title="分支的创建与合并"></a>分支的创建与合并</h3><p>简单的分支新建和分支合并的工作流：<br>   1.开发某个网站。<br>   2.为实现某个新的用户需求，创建一个分支。<br>   3.在这个分支上开展工作。<br>正在此时，你突然接到一个电话说有个很严重的问题需要紧急修补。 你将按照如下方式来处理：<br>   1.切换到你的线上分支（production branch）。<br>   2.为这个紧急任务新建一个分支，并在其中修复它。<br>   3.在测试通过之后，切换回线上分支，然后合并这个修补分支，最后将改动推送到线上分支。<br>   4.切换回你最初工作的分支上，继续工作。</p>
<h4 id="新建分支"><a href="#新建分支" class="headerlink" title="新建分支"></a>新建分支</h4><p>1.解决公司的新需求#52问题，于是创建一个分支并同时切换到那个分支上</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 方法一</span></span><br><span class="line">$ git chechout -b iss52 <span class="comment"># 创建并切换分支</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#方法二</span></span><br><span class="line">$ git branch iss52      <span class="comment"># 创建分支</span></span><br><span class="line">$ git chechout iss52    <span class="comment"># 切换分支</span></span><br></pre></td></tr></table></figure>
<p><img src="https://git-scm.com/book/en/v2/images/basic-branching-2.png">  </p>
<p>2.在#53问题上工作，并且做了一些提交。在此过程中iss52分支不断地向前推进。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ vim index.html</span><br><span class="line">$ git commit -a -m <span class="string">&quot;added a new footer[issue 53]&quot;</span></span><br></pre></td></tr></table></figure>
<p><img src="https://git-scm.com/book/en/v2/images/basic-branching-3.png">  </p>
<p>3.接到电话有个很严重的问题需要紧急修补。于是要切换到线上分支，并为这个紧急任务新建一个hotfix分支，在该分支上工作直到问题解决</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout master</span><br><span class="line">Switched to branch <span class="string">&#x27;master&#x27;</span></span><br><span class="line">$ git checkout -b hotfix</span><br><span class="line">Switched to a new branch <span class="string">&#x27;hotfix&#x27;</span></span><br><span class="line">$ vim index.html</span><br><span class="line">$ git commit -a -m <span class="string">&#x27;fixed the broken email address&#x27;</span></span><br><span class="line">[hotfix 1fb7853] fixed the broken email address</span><br><span class="line"> 1 file changed, 2 insertions(+)</span><br></pre></td></tr></table></figure>
<p><img src="https://git-scm.com/book/en/v2/images/basic-branching-4.png">  </p>
<p>4.运行你的测试，确保修改是正确的，然后将hotfix分支合并回master分支来部署到线上。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout master</span><br><span class="line">$ git merge hotfix</span><br><span class="line">Updating f42c576..3a0874c</span><br><span class="line">Fast-forward</span><br><span class="line"> index.html | 2 ++</span><br><span class="line"> 1 file changed, 2 insertions(+)</span><br></pre></td></tr></table></figure>
<p><img src="https://git-scm.com/book/en/v2/images/basic-branching-5.png">  </p>
<p>5.这个紧急问题地解决方案发布以后，你准备回到被打断之前地工作中。在此之前，先删除hotfix分支，因为已经不再需要他了，master分支已经指向同一个位置。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git branch -f hotfix</span><br><span class="line">Deleted branch hotfix (3a0874c).</span><br><span class="line">$ git chechout iss53</span><br><span class="line">Switched to branch <span class="string">&quot;iss53&quot;</span></span><br><span class="line">$ vim index.html</span><br><span class="line">$ git commit -a -m <span class="string">&#x27;finished the new footer [issue 53]&#x27;</span></span><br><span class="line">[iss53 ad82d7a] finished the new footer [issue 53]</span><br><span class="line">1 file changed, 1 insertion(+)</span><br></pre></td></tr></table></figure>
<p><img src="https://git-scm.com/book/en/v2/images/basic-branching-6.png">  </p>
<h4 id="分支的合并：解决冲突"><a href="#分支的合并：解决冲突" class="headerlink" title="分支的合并：解决冲突"></a>分支的合并：解决冲突</h4><p>6.当完成#53问题的需求后，打算将工作合并入master分支。为此需要合并iss53分支到master分支。合并之后就不需要iss53分支了，可以在最后删除这个分支。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout master</span><br><span class="line">Switched to branch <span class="string">&#x27;master&#x27;</span></span><br><span class="line">$ git merge iss53</span><br><span class="line">Merge made by the <span class="string">&#x27;recursive&#x27;</span> strategy.</span><br><span class="line">index.html |    1 +</span><br><span class="line">1 file changed, 1 insertion(+)</span><br><span class="line">$ git branch -d iss53</span><br></pre></td></tr></table></figure>
<p><img src="https://git-scm.com/book/en/v2/images/basic-merging-2.png">  </p>
<p>7.遇到冲突时的分支合并<br>如果你对#53问题的修改和有关hotfix分支的修改都涉及到同一个文件的同一处，在合并他们的时候就会产生合并冲突：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git merge iss53</span><br><span class="line">Auto-merging index.html</span><br><span class="line">CONFLICT (content): Merge conflict <span class="keyword">in</span> index.html</span><br><span class="line">Automatic merge failed; fix conflicts and <span class="keyword">then</span> commit the result.</span><br></pre></td></tr></table></figure>
<p>此时Git做了合并，但是没有自动地创建一个新的合并提交，Git回暂停下来，等待你去解决合并产生地冲突。在合并冲突之后可以使用<code>git status</code>命令查看那些因为包含合并冲突而处于未合并状态的文件。</p>
<p>任何因包含合并冲突而有待解决的文件，都会以未合并状态标识出来。Git会在有冲突的文件中加入标准的冲突解决标记：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD:index.html</span><br><span class="line">&lt;div id=&quot;footer&quot;&gt;contact : email.support@github.com&lt;/div&gt;</span><br><span class="line">=======</span><br><span class="line">&lt;div id=&quot;footer&quot;&gt;</span><br><span class="line"> please contact us at support@github.com</span><br><span class="line">&lt;/div&gt;</span><br><span class="line">&gt;&gt;&gt;&gt;&gt;&gt;&gt; iss53:index.html</span><br></pre></td></tr></table></figure>
<p>这表示HEAD所指示的版本（也就是你的master分支所在的位置）在这个区段的上半部分（ &#x3D;&#x3D;&#x3D; 的上半部分），iss53分支所示的版本在 &#x3D;&#x3D;&#x3D; 的下半部分。为了解决冲突，必须选择由&#x3D;&#x3D;&#x3D;分割的两个部分中的一个，或者自行合并这些内容，例如，可以通过把这段内容换成下面的样子来解决冲突：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;div id=&quot;footer&quot;&gt;</span><br><span class="line">please contact us at email.support@github.com</span><br><span class="line">&lt;/div&gt;</span><br></pre></td></tr></table></figure>
<p>上述的冲突解决方案仅保留了其中一个分支的修改，并且<code>&lt;&lt;==&gt;&gt;</code>这些行被完全删除。在解决了所有文件里的冲突之后，对每个文件使用<code>git add</code> 命令来将其标记为冲突已解决。一旦暂存了这些原本有冲突的文件，Git就会将他们标记为冲突已解决，</p>
<p>图像化工具：<code>git mergetool</code>会启动一个可视化的合并工具，并带着你一步一步解决这些冲突。</p>
<h3 id="分支管理"><a href="#分支管理" class="headerlink" title="分支管理"></a>分支管理</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git branch</span><br><span class="line">  iss53</span><br><span class="line">* master</span><br><span class="line">  testing</span><br></pre></td></tr></table></figure>
<p><code>git branch</code>命令不仅可以创建和删除分支，如果不加任何参数，该命令表示列出当前所有分支。master分支前面的 * 表示现在检出的那一个分支（也就是HEAD指针所指向的分支）。如果想要查看每一个分支的最后一次提交可以运行<code>git branch -v</code>命令。</p>
<p><code>--merged</code>和<code>--no-merged</code>选项可以过滤这个列表中已经合并或尚未合并到当前分支的分支。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看哪些分支已经合并到当前分支</span></span><br><span class="line">$ git branch --merged</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看所有包含未合并工作的分支</span></span><br><span class="line">$ git branch --no-merged</span><br></pre></td></tr></table></figure>

<p>对于未合并的分支，尝试使用<code>git branch -d</code>命令会失败，因为它包含了还未合并的工作，如果真的想要删除分支并丢掉那部分工作，可以使用<code>-D</code>强制删除。</p>
<h3 id="分支开发工作流"><a href="#分支开发工作流" class="headerlink" title="分支开发工作流"></a>分支开发工作流</h3><h4 id="长期分支"><a href="#长期分支" class="headerlink" title="长期分支"></a>长期分支</h4><p>只在master分支上保留完全稳定的代码，还有一些名为develop<br>或topic的平行分支，用在做后续开发或者测试稳定性，这些分支不必保持绝对稳定，一旦达到稳定状态，就可以合并入master分支。<br><img src="https://git-scm.com/book/en/v2/images/lr-branches-2.png">  </p>
<h4 id="主题分支"><a href="#主题分支" class="headerlink" title="主题分支"></a>主题分支</h4><p>主题分支是一种短期分支，被用来实现单一特性或其相关工作。比如上文中用到主题分支（iss53和hotfix分支）提交了一些更新，并在他们合并入主干分支之后，又删除了他们。<br>考虑这样一个例子，你在 master 分支上工作到 C1，这时为了解决一个问题而新建 iss91 分支，在 iss91 分支上工作到 C4，然而对于那个问题你又有了新的想法，于是你再新建一个 iss91v2 分支试图用另一种方法解决那个问题，接着你回到 master 分支工作了一会儿，你又冒出了一个不太确定的想法，你便在 C10 的时候新建一个 dumbidea 分支，并在上面做些实验。 你的提交历史看起来像下面这个样子：<br><img src="https://git-scm.com/book/en/v2/images/topic-branches-1.png"><br>现在，我们假设两件事情：你决定使用第二个方案来解决那个问题，即使用在 iss91v2 分支中方案。 另外，你将 dumbidea 分支拿给你的同事看过之后，结果发现这是个惊人之举。 这时你可以抛弃 iss91 分支（即丢弃 C5 和 C6 提交），然后把另外两个分支合并入主干分支。 最终你的提交历史看起来像下面这个样子：<br><img src="https://git-scm.com/book/en/v2/images/topic-branches-2.png">  </p>
<h3 id="远程分支"><a href="#远程分支" class="headerlink" title="远程分支"></a>远程分支</h3><p>可以通过<code>git ls-remote &lt;remote&gt;</code>来显示地获得远程引用地完整列表，或者通过<code>git remote show &lt;remote&gt;</code>获得远程分支地更多信息。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git remote -v</span><br><span class="line">origin  git@github.com:portkas/portkas.github.io.git (fetch)</span><br><span class="line">origin  git@github.com:portkas/portkas.github.io.git (push)</span><br><span class="line"></span><br><span class="line">$ git ls-remote origin</span><br><span class="line">33e33baaadb70a791936b45c4f7a47ca104cdc1a        HEAD</span><br><span class="line">33e33baaadb70a791936b45c4f7a47ca104cdc1a        refs/heads/hexo</span><br><span class="line">294c8b3fa958a2f46defa546b0e137a91f0927d9        refs/heads/master</span><br></pre></td></tr></table></figure>
<p>远程分支只是分支的一种。本地的主分支为master，远程的主分支为origin&#x2F;master，这是两个不同的分支。<br><img src="https://git-scm.com/book/en/v2/images/remote-branches-1.png"><br>如果你在本地master分支做了一些工作，在同一时间内其他人推送提交到 <code>git.ourcomoany.com</code>并且更新了他的master分支，也就是说你们的提交历史走向不同的方向。即便如此，只要保持不与origin服务器连接并拉取数据，你的origin&#x2F;master指针就不会移动。<br><img src="https://git-scm.com/book/en/v2/images/remote-branches-2.png"><br>如果要与给定的远程仓库同步数据，运行<code>git fetch &lt;remote&gt;</code>命令（在本例中为<code>git fetch origin</code>）。这个命令会查找origin是哪一个服务器（在本例中是<code>git.ourcompany.com</code>），从中抓取本地没有的数据，并且更新本地数据库，移动origin&#x2F;master指针到更新之后的位置。<br><img src="https://git-scm.com/book/en/v2/images/remote-branches-3.png">  </p>
<h4 id="推送"><a href="#推送" class="headerlink" title="推送"></a>推送</h4><p>本地地分支并不会自动与远程分支同步，必须显式地推送想要 分享的分支。可以把不愿意分享的内容放到私人分支上，而将需要和别人协作的内容推送到公开分支。<br>如果希望和别人一起在名为serverfix的分支上工作，运行<code>git push &lt;remote&gt; &lt;branch&gt;</code>:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git push origin serverfix</span><br><span class="line">Counting objects: 24, <span class="keyword">done</span>.</span><br><span class="line">Delta compression using up to 8 threads.</span><br><span class="line">Compressing objects: 100% (15/15), <span class="keyword">done</span>.</span><br><span class="line">Writing objects: 100% (24/24), 1.91 KiB | 0 bytes/s, <span class="keyword">done</span>.</span><br><span class="line">Total 24 (delta 2), reused 0 (delta 0)</span><br><span class="line">To https://github.com/schacon/simplegit</span><br><span class="line"> * [new branch]      serverfix -&gt; serverfix</span><br></pre></td></tr></table></figure>

<p>下一次其他协作者从服务器上抓取数据时，他们会在本地生成一个远程分支origin&#x2F;serverfix，指向服务器的serverfix分支的引用。<br>需要特别注意的是，当抓取到新的远程跟踪分支时，本地不会自动生成一份可编辑的副本，即本地不会有一个新的serverfix分支，只有一个不可修改的origin&#x2F;serverfix指针。<br>可以运行<code>git merge origin/serverfix</code>将这些工作合并到当前所在的分支。如果想要在serverfix分支上工作，可以将其建立在远程跟踪分支之上：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout -b serverfix origin/serverfix</span><br><span class="line">Branch serverfix <span class="built_in">set</span> up to track remote branch serverfix from origin.</span><br><span class="line">Switched to a new branch <span class="string">&#x27;serverfix&#x27;</span></span><br></pre></td></tr></table></figure>
<p>这会给你一个用于工作的本地分支，并且起点位于origin&#x2F;serverfix。</p>
<h4 id="跟踪分支"><a href="#跟踪分支" class="headerlink" title="跟踪分支"></a>跟踪分支</h4><p>当克隆一个仓库时，通常会自动地设置一个跟踪origin&#x2F;master地master分支。如果在一个跟踪分支上输入<code>git pull</code>，Git能自动地识别去哪个服务器上抓取，合并到哪个分支。<br>也可以设置其他地跟踪分支，运行<code>git checkout -b &lt;branch&gt; &lt;remote&gt;/&lt;branch&gt;</code>，这是一个常用地操作，所以Git提供了–track快捷方式：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout --track origin/serverfix</span><br><span class="line">Branch serverfix <span class="built_in">set</span> up to track remote branch serverfix from origin.</span><br><span class="line">Switched to a new branch <span class="string">&#x27;serverfix&#x27;</span></span><br></pre></td></tr></table></figure>
<p>由于该操作太常用了，该快捷方式还有一个快捷方式：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout serverfix</span><br><span class="line">Branch serverfix <span class="built_in">set</span> up to track remote branch serverfix from origin.</span><br><span class="line">Switched to a new branch <span class="string">&#x27;serverfix&#x27;</span></span><br></pre></td></tr></table></figure>
<p>如果想要将本地分支与远程分支设置为不同的名字，可以使用命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git checkout -b sf origin/erverfix</span><br><span class="line">Branch sf <span class="built_in">set</span> up to track remote branch serverfix from origin.</span><br><span class="line">Switched to a new branch <span class="string">&#x27;sf&#x27;</span></span><br></pre></td></tr></table></figure>
<p>现在，本地分支sf会自动从origin&#x2F;serverfix拉取。</p>
<p>如果想要修改正在跟踪地上有分支（即和本地分支绑定地服务器分支），可以在任意时间使用<code>-u</code>或<code>--set-upstream-to</code>选项来运行<code>git branch</code>来显示地设置：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 首先要先有一个本地分支，然后切换到这个分支，然后运行该指令指定要跟踪地远程分支</span></span><br><span class="line">$ git branch -u origin/serverfix</span><br><span class="line">Branch serverfix <span class="built_in">set</span> up to track remote branch serverfix from origin.</span><br></pre></td></tr></table></figure>
<p>如果要查看设置地所有跟踪分支，可以使用<code>git branch -vv</code>，会将所有地本地分支列出来，并且包含更多的信息，比如每一个分支正在跟踪哪个远程分支，本地分支是领先还是落后。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git branch -vv</span><br><span class="line">  iss53     7e424c3 [origin/iss53: ahead 2] forgot the brackets</span><br><span class="line">  master    1ae2a45 [origin/master] deploying index fix</span><br><span class="line">* serverfix f8674d9 [teamone/server-fix-good: ahead 3, behind 1] this should <span class="keyword">do</span> it</span><br><span class="line">  testing   5ea463a trying something new</span><br></pre></td></tr></table></figure>
<p>可以看到iss53分支正在跟踪origin&#x2F;iss53并且ahead是2，意味着本地有两个提交还没有推送到服务器。master分支正在跟踪origin&#x2F;master分支并且是最新的。serverfix分支正在跟踪teamone服务器上的server-fix-good分支并且领先3落后1，意味着服务器上有一次还没有合并，同时本地还有三次提交没有推送。<br>需要注意的是，这些数字的值来自于你从服务器上最后一次抓取的数据，这个命令没有连接服务器，所以只会告诉你本地缓存的服务器数据，如果想要统计最新的领先和落后数字，可以在统计前抓取所有的远程仓库：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git fetch --all</span><br><span class="line">$ git branch -vv</span><br></pre></td></tr></table></figure>


<h4 id="拉取"><a href="#拉取" class="headerlink" title="拉取"></a>拉取</h4><p><code>git fetch</code>命令从服务器上抓取本地没有的数据时，它并不会修改工作目录中的内容，它只会获取数据然后让你自己合并。<br><code>git pull</code>命令在大多数情况下含义是一个<code>git fetch</code>紧接着一个<code>git merge</code>命令。该指令会查找当前分支所跟踪的服务器与分支，从服务器上抓取数据然后尝试合并入那个远程分支。<br>由于<code>git pull</code>经常令人困惑，通常单独显式地使用<code>git fetch</code>与<code>git merge</code></p>
<h4 id="删除远程分支"><a href="#删除远程分支" class="headerlink" title="删除远程分支"></a>删除远程分支</h4><p>当已经通过远程分支做完所有的工作后，可以运行<code>git push --delete &lt;branch&gt;</code>命令删除一个远程分支，例如要从服务器上删除serverfix分支：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git push origin --delete serverfix</span><br><span class="line">To https://github.com/schacon/simplegit</span><br><span class="line"> - [deleted]         serverfix</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Git</category>
      </categories>
  </entry>
  <entry>
    <title>Hexo搭建个人博客</title>
    <url>/2024/09/02/Hexo%E6%90%AD%E5%BB%BA%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2/</url>
    <content><![CDATA[<h1 id="1-前置条件"><a href="#1-前置条件" class="headerlink" title="1.前置条件"></a>1.前置条件</h1><ul>
<li><a href="https://nodejs.org/en/download/package-manager/current">Node.js</a></li>
<li><a href="https://git-scm.com/">git</a></li>
<li>Github Pages<span id="more"></span></li>
</ul>
<h1 id="2-安装Node-js"><a href="#2-安装Node-js" class="headerlink" title="2.安装Node.js"></a>2.安装Node.js</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># installs nvm (Node Version Manager)</span></span><br><span class="line">$ curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash</span><br><span class="line"></span><br><span class="line"><span class="comment"># download and install Node.js (you may need to restart the terminal)</span></span><br><span class="line">$ nvm install 22</span><br><span class="line"></span><br><span class="line"><span class="comment"># verifies the right Node.js version is in the environment</span></span><br><span class="line">$ node -v <span class="comment"># should print `v22.6.0`</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># verifies the right npm version is in the environment</span></span><br><span class="line">$ npm -v <span class="comment"># should print `10.8.2`</span></span><br></pre></td></tr></table></figure>

<h1 id="3-本地运行Hexo"><a href="#3-本地运行Hexo" class="headerlink" title="3.本地运行Hexo"></a>3.本地运行Hexo</h1><h2 id="安装Hexo"><a href="#安装Hexo" class="headerlink" title="安装Hexo"></a>安装Hexo</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">mkdir</span> blog</span><br><span class="line">$ <span class="built_in">cd</span> blog</span><br><span class="line">$ npm install -g hexo-cli   <span class="comment"># 安装hexo</span></span><br><span class="line">$ hexo init                 <span class="comment"># 初始化hexo</span></span><br><span class="line">$ hexo install              <span class="comment"># 安装需要的包</span></span><br></pre></td></tr></table></figure>
<p>初始化完成后项目文件夹如下所示：<br>.<br>├── _config.yml<br>├── package.json<br>├── scaffolds<br>├── source<br>|   ├── _drafts<br>|   └── _posts<br>└── themes          </p>
<h2 id="Hexo的用法"><a href="#Hexo的用法" class="headerlink" title="Hexo的用法"></a>Hexo的用法</h2><ul>
<li>启动内置预览服务（默认端口4000）  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo s    <span class="comment"># hexo serve</span></span><br></pre></td></tr></table></figure></li>
<li>生成网站  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo g    <span class="comment"># hexo generate</span></span><br></pre></td></tr></table></figure></li>
<li>部署到Github Pages上（部署之前需要配置）  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo d    <span class="comment"># hexo deploy</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成并部署，省去hexo g</span></span><br><span class="line">$ hexo d -g</span><br></pre></td></tr></table></figure></li>
<li>#清除缓存文件db.json和已生成的静态文件 public  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo c    <span class="comment"># hexo clean</span></span><br></pre></td></tr></table></figure></li>
<li>创建博客  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建完成后博客的.md文件存储在`./source/_post/`中</span></span><br><span class="line">$ hexo new <span class="string">&quot;blog name&quot;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h1 id="3-配置"><a href="#3-配置" class="headerlink" title="3.配置"></a>3.配置</h1><p>_config.yml</p>
<ul>
<li>title : 网站标题</li>
<li>subtitle : 副标题</li>
<li>description : 网站描述</li>
<li>keywords : 网站关键词</li>
<li>author : 作者</li>
<li>language : 语言</li>
<li>timezone : 时区</li>
</ul>
<p>以上设置会出现在meta里。</p>
<ul>
<li>url : 网址</li>
<li>root : 网站根目录</li>
<li>permalink : 永久链接格式，比如year&#x2F;:month&#x2F;:day&#x2F;:title&#x2F;</li>
<li>source_dir : 源文件夹，默认source</li>
<li>public_dir : 生成的网站文件夹</li>
<li>theme : 主题</li>
</ul>
<p>官网关于配置的描述：<a href="https://hexo.io/zh-cn/docs/configuration">https://hexo.io/zh-cn/docs/configuration</a></p>
<h1 id="4-部署到Github-Pages"><a href="#4-部署到Github-Pages" class="headerlink" title="4.部署到Github Pages"></a>4.部署到Github Pages</h1><p>0.配置github的SSH<br>1.创建repo:git@github:yourname&#x2F;yourname.github.io.git<br>2.修改配置文件<code>_config.yml</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">deploy:</span><br><span class="line">  type: git</span><br><span class="line">  repo: git@github.com:yourname/yourname.github.io.git</span><br><span class="line">  branch: main</span><br></pre></td></tr></table></figure>
<p>4.安装<code>deploy-git</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ npm  install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>

<h1 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h1><ul>
<li><a href="https://hexo.io/docs/">Hexo官网</a></li>
<li><a href="https://www.luogu.com.cn/article/vglpq15p">从零搭建 Hexo + Github 博客</a></li>
<li><a href="https://note.tonycrane.cc/cs/tools/hexo/">Hexo 使用记录</a></li>
<li><a href="https://hexo.io/zh-cn/docs/writing">Hexo写作</a></li>
<li><a href="https://github.com/ppoffice/hexo-theme-icarus">主题</a></li>
</ul>
]]></content>
      <categories>
        <category>个人博客</category>
      </categories>
  </entry>
  <entry>
    <title>Vim</title>
    <url>/2024/09/20/Vim/</url>
    <content><![CDATA[<h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装</span></span><br><span class="line">$ <span class="built_in">sudo</span> apt install vim</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看版本</span></span><br><span class="line">$ vim --version</span><br></pre></td></tr></table></figure>
<span id="more"></span>
<h1 id="vim模式简介"><a href="#vim模式简介" class="headerlink" title="vim模式简介"></a>vim模式简介</h1><ul>
<li>命令模式：查看文件内容，修改文件，关键词条的搜索</li>
<li>编辑模式：修改文件内容</li>
<li>末行模式：执行Linux命令，保存文件，行跳转，窗口分屏</li>
</ul>
<h1 id="命令模式"><a href="#命令模式" class="headerlink" title="命令模式"></a>命令模式</h1><h2 id="保存和退出"><a href="#保存和退出" class="headerlink" title="保存和退出"></a>保存和退出</h2><p>连续按两个大写的Z，即<code>ZZ</code>可在命令行模式下，保存并退出文件，等同于末行模式下的<code>:wq</code></p>
<h2 id="代码标准化"><a href="#代码标准化" class="headerlink" title="代码标准化"></a>代码标准化</h2><p>命令模式下，直接通过键盘输入<code>gg=G</code>，即可对代码进行标准化，使得代码排版更加工整。</p>
<h2 id="光标移动"><a href="#光标移动" class="headerlink" title="光标移动"></a>光标移动</h2><table>
<thead>
<tr>
<th align="center">快捷键</th>
<th align="left">功能</th>
<th align="left">备注</th>
</tr>
</thead>
<tbody><tr>
<td align="center">0</td>
<td align="left">光标移动到行首</td>
<td align="left">无</td>
</tr>
<tr>
<td align="center">$</td>
<td align="left">光标移动到行尾</td>
<td align="left">无</td>
</tr>
<tr>
<td align="center">gg</td>
<td align="left">光标移动到文件头</td>
<td align="left">第一行的开始</td>
</tr>
<tr>
<td align="center">G</td>
<td align="left">光标移动到文件尾</td>
<td align="left">最后一行的开始</td>
</tr>
<tr>
<td align="center">nG</td>
<td align="left">行跳转</td>
<td align="left">n代表要跳转到哪行</td>
</tr>
<tr>
<td align="center">n+回车</td>
<td align="left">相对跳转n行</td>
<td align="left">从光标开始处往下跳转n行</td>
</tr>
</tbody></table>
<h2 id="删除命令"><a href="#删除命令" class="headerlink" title="删除命令"></a>删除命令</h2><p>vim中没有删除操作，删除即剪切，剪切后可复制到其他地方。</p>
<table>
<thead>
<tr>
<th align="center">快捷键</th>
<th align="center">功能</th>
<th align="center">备注</th>
</tr>
</thead>
<tbody><tr>
<td align="center">x</td>
<td align="center">删除光标后面的单个字符</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center">X</td>
<td align="center">删除光标前面的单个字符</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center">dw</td>
<td align="center">删除单词</td>
<td align="center">要把光标移动到单词的第一个字母上</td>
</tr>
<tr>
<td align="center">d0</td>
<td align="center">删除光标前的字符串</td>
<td align="center">光标所在行前面的字符全部删除</td>
</tr>
<tr>
<td align="center">dD($)</td>
<td align="center">删除光标后面的字符串</td>
<td align="center">dD 和 d$ 功能一样</td>
</tr>
<tr>
<td align="center">dd</td>
<td align="center">删除光标所在行</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center">ndd</td>
<td align="center">删除n行</td>
<td align="center">从光标开始往下删除n行</td>
</tr>
</tbody></table>
<h2 id="撤销和反撤销"><a href="#撤销和反撤销" class="headerlink" title="撤销和反撤销"></a>撤销和反撤销</h2><table>
<thead>
<tr>
<th align="center">快捷键</th>
<th align="center">功能</th>
</tr>
</thead>
<tbody><tr>
<td align="center">u</td>
<td align="center">撤销</td>
</tr>
<tr>
<td align="center">ctrl+r</td>
<td align="center">反撤销</td>
</tr>
</tbody></table>
<h2 id="复制和粘贴"><a href="#复制和粘贴" class="headerlink" title="复制和粘贴"></a>复制和粘贴</h2><table>
<thead>
<tr>
<th align="center">快捷键</th>
<th align="center">功能</th>
</tr>
</thead>
<tbody><tr>
<td align="center">yy</td>
<td align="center">复制光标所在行</td>
</tr>
<tr>
<td align="center">nyy</td>
<td align="center">从光标所在行向下复制n行</td>
</tr>
<tr>
<td align="center">p</td>
<td align="center">粘贴到光标所在行的下面</td>
</tr>
<tr>
<td align="center">P</td>
<td align="center">粘贴到光标所在行的上面</td>
</tr>
</tbody></table>
<h2 id="可视模式"><a href="#可视模式" class="headerlink" title="可视模式"></a>可视模式</h2><ul>
<li>v：进入字符可视模式，文本以字符为单位</li>
<li>V：进入行可视模式，文本以行为单位</li>
<li>ctrl+v：进入块可视模式，可以选择一个矩形内的文本</li>
</ul>
<table>
<thead>
<tr>
<th align="center">快捷键</th>
<th align="center">功能</th>
<th align="center">备注</th>
</tr>
</thead>
<tbody><tr>
<td align="center">d</td>
<td align="center">删除（剪切）</td>
<td align="center">删除可视模式下的可视块</td>
</tr>
<tr>
<td align="center">y</td>
<td align="center">复制</td>
<td align="center">复制可视模式下选中的数据块</td>
</tr>
<tr>
<td align="center">p</td>
<td align="center">数据粘贴到光标的后面</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center">P</td>
<td align="center">数据粘贴到光标的前面</td>
<td align="center">无</td>
</tr>
</tbody></table>
<h3 id="代码块注释"><a href="#代码块注释" class="headerlink" title="代码块注释"></a>代码块注释</h3><p>1.通过<code>ctrl+v</code>进入块可视模式<br>2.移动光标选中多行代码开头<br>3.选择完毕后，输入大写的<code>I</code>进入<code>insert</code>模式<br>4.输入要插入的注释符<code>//</code><br>5.按ESC键退出</p>
<h2 id="替换"><a href="#替换" class="headerlink" title="替换"></a>替换</h2><table>
<thead>
<tr>
<th align="center">快捷键</th>
<th align="center">功能</th>
<th align="center">备注</th>
</tr>
</thead>
<tbody><tr>
<td align="center">r</td>
<td align="center">替换光标后的单个字符</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center">R</td>
<td align="center">替换光标后的多个字符</td>
<td align="center">按ESC结束替换</td>
</tr>
</tbody></table>
<h2 id="查找"><a href="#查找" class="headerlink" title="查找"></a>查找</h2><table>
<thead>
<tr>
<th align="center">搜索快捷键</th>
<th align="left">关键字遍历</th>
<th align="center">描述</th>
<th align="center">备注</th>
</tr>
</thead>
<tbody><tr>
<td align="center">&#x2F;</td>
<td align="left">n</td>
<td align="center">从当前位置向下</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center"></td>
<td align="left">N</td>
<td align="center">从当前位置向上</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center">?</td>
<td align="left">n</td>
<td align="center">从当前位置向上</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center"></td>
<td align="left">N</td>
<td align="center">从当前位置向下</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center">#</td>
<td align="left">n</td>
<td align="center">从当前位置向上</td>
<td align="center">光标当在需要搜索的关键字上按<code>#</code></td>
</tr>
<tr>
<td align="center"></td>
<td align="left">N</td>
<td align="center">从当前位置向下</td>
<td align="center">无</td>
</tr>
</tbody></table>
<h2 id="切换到编辑模式"><a href="#切换到编辑模式" class="headerlink" title="切换到编辑模式"></a>切换到编辑模式</h2><table>
<thead>
<tr>
<th align="center">快捷键</th>
<th align="center">功能</th>
</tr>
</thead>
<tbody><tr>
<td align="center">i</td>
<td align="center">从光标前面开始输入</td>
</tr>
<tr>
<td align="center">o</td>
<td align="center">在光标下面创建新行，并从新行开始输入</td>
</tr>
<tr>
<td align="center">O</td>
<td align="center">在光标上面创建新行，并从新行开始输入</td>
</tr>
<tr>
<td align="center">A</td>
<td align="center">从当前行行尾开始输入</td>
</tr>
<tr>
<td align="center">S（大写S）</td>
<td align="center">删除当前行，在当前行开始输入</td>
</tr>
<tr>
<td align="center">a</td>
<td align="center">从光标的后面开始输入</td>
</tr>
<tr>
<td align="center">s（小写s）</td>
<td align="center">删除光标盖住的字符，并开始输入</td>
</tr>
<tr>
<td align="center">I（大写i）</td>
<td align="center">从当前行行首开始输入</td>
</tr>
</tbody></table>
<h1 id="末行模式"><a href="#末行模式" class="headerlink" title="末行模式"></a>末行模式</h1><h2 id="保存和退出-1"><a href="#保存和退出-1" class="headerlink" title="保存和退出"></a>保存和退出</h2><table>
<thead>
<tr>
<th align="center">末行模式下输入的命令</th>
<th align="center">功能</th>
</tr>
</thead>
<tbody><tr>
<td align="center">q</td>
<td align="center">退出</td>
</tr>
<tr>
<td align="center">q!</td>
<td align="center">直接退出不保存</td>
</tr>
<tr>
<td align="center">w</td>
<td align="center">保存不退出</td>
</tr>
<tr>
<td align="center">wq</td>
<td align="center">保存并退出</td>
</tr>
<tr>
<td align="center">x</td>
<td align="center">保存并退出</td>
</tr>
</tbody></table>
<h2 id="替换-1"><a href="#替换-1" class="headerlink" title="替换"></a>替换</h2><table>
<thead>
<tr>
<th align="center">末行模式下输入的命令</th>
<th align="center">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="center">s&#x2F;被替换的关键字&#x2F;新的关键字&#x2F;g</td>
<td align="center">只对光标所在行进行替换</td>
</tr>
<tr>
<td align="center">行号1,行号2 s&#x2F;被替换关键字&#x2F;新的关键字&#x2F;g</td>
<td align="center">替换行号1-行号2之间的关键字</td>
</tr>
<tr>
<td align="center">%s&#x2F;被替换的关键字&#x2F;新的关键字&#x2F;g</td>
<td align="center">替换所有关键字</td>
</tr>
</tbody></table>
<h2 id="分屏"><a href="#分屏" class="headerlink" title="分屏"></a>分屏</h2><table>
<thead>
<tr>
<th align="center">末行模式下输入的命令</th>
<th align="center">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="center">sp</td>
<td align="center">水平分屏，上下两个屏</td>
</tr>
<tr>
<td align="center">vsp</td>
<td align="center">垂直分屏，左右两个屏</td>
</tr>
<tr>
<td align="center">ctrl+w+w</td>
<td align="center">光标在打开的屏幕之间切换</td>
</tr>
<tr>
<td align="center">qall</td>
<td align="center">同时退出所有屏幕</td>
</tr>
<tr>
<td align="center">wqall</td>
<td align="center">保存并退出所有屏幕</td>
</tr>
<tr>
<td align="center">sp 文件名</td>
<td align="center">分屏的同时，打开指定文件</td>
</tr>
<tr>
<td align="center">vsp 文件名</td>
<td align="center">分屏的同时，打开指定文件</td>
</tr>
</tbody></table>
<p>vim打开多个文件的时候，可以分屏打开</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 水平分屏，上下两个屏</span></span><br><span class="line">$ vim -o file1 file2</span><br><span class="line"></span><br><span class="line"><span class="comment"># 垂直分屏，左右两个屏</span></span><br><span class="line">$ vim -O file1 file2</span><br></pre></td></tr></table></figure>

<h2 id="执行shell命令（-）"><a href="#执行shell命令（-）" class="headerlink" title="执行shell命令（:!）"></a>执行shell命令（:!）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">:!shell命令</span><br><span class="line">:!ls</span><br></pre></td></tr></table></figure>

<h1 id="vim配置文件"><a href="#vim配置文件" class="headerlink" title="vim配置文件"></a>vim配置文件</h1><ul>
<li>用户级别的配置文件：<code>~/.vimrc</code></li>
<li>系统级别的配置文件：<code>/etc/vim/vimrc</code></li>
</ul>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>Tmux</title>
    <url>/2024/09/20/Tmux/</url>
    <content><![CDATA[<h1 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h1><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Ubuntu</span></span><br><span class="line">$ <span class="built_in">sudo</span> apt-get install tmux</span><br></pre></td></tr></table></figure>
<span id="more"></span>
<h2 id="启动和退出"><a href="#启动和退出" class="headerlink" title="启动和退出"></a>启动和退出</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 启动</span></span><br><span class="line">$ tmux</span><br><span class="line"></span><br><span class="line"><span class="comment"># 退出（或者使用快捷键ctrl+d）</span></span><br><span class="line">$ <span class="built_in">exit</span></span><br></pre></td></tr></table></figure>

<h2 id="前缀键"><a href="#前缀键" class="headerlink" title="前缀键"></a>前缀键</h2><p>Tmux中所有的快捷键都需要通过前缀键唤起，即先按前缀键，再按快捷键，才能生效。默认的前缀键为：ctrl+b</p>
<h1 id="会话管理"><a href="#会话管理" class="headerlink" title="会话管理"></a>会话管理</h1><h2 id="新建会话"><a href="#新建会话" class="headerlink" title="新建会话"></a>新建会话</h2><p>第一个启动的Tmux窗口，编号是0, 第二个窗口的编号是1,以此类推。也可以在新建窗口时为窗口指定会话名。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ tmux new -s &lt;session-name&gt;</span><br></pre></td></tr></table></figure>

<h2 id="分离会话"><a href="#分离会话" class="headerlink" title="分离会话"></a>分离会话</h2><p>快捷键：ctrl+b d</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ tmux detach</span><br></pre></td></tr></table></figure>

<h2 id="查看当前所有Tmux会话"><a href="#查看当前所有Tmux会话" class="headerlink" title="查看当前所有Tmux会话"></a>查看当前所有Tmux会话</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ tmux <span class="built_in">ls</span></span><br><span class="line"><span class="comment"># or</span></span><br><span class="line">$ tmux list-session</span><br></pre></td></tr></table></figure>

<h2 id="接入会话"><a href="#接入会话" class="headerlink" title="接入会话"></a>接入会话</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ tmux attach -t &lt;session-name&gt;</span><br></pre></td></tr></table></figure>

<h2 id="杀死会话"><a href="#杀死会话" class="headerlink" title="杀死会话"></a>杀死会话</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ tmux kill-session -t &lt;session-name&gt;</span><br></pre></td></tr></table></figure>

<h2 id="切换会话"><a href="#切换会话" class="headerlink" title="切换会话"></a>切换会话</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ tmux switch -t &lt;session-name&gt;</span><br></pre></td></tr></table></figure>

<h2 id="重命名会话"><a href="#重命名会话" class="headerlink" title="重命名会话"></a>重命名会话</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ tmux rename-session -t &lt;old-name&gt; &lt;new-name&gt;</span><br></pre></td></tr></table></figure>

<h2 id="会话快捷键"><a href="#会话快捷键" class="headerlink" title="会话快捷键"></a>会话快捷键</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ctrl+b d：分离会话</span><br><span class="line">ctrl+b s：列出所有会话</span><br><span class="line">ctrl+b $：重命名当前会话</span><br></pre></td></tr></table></figure>

<h1 id="窗格操作"><a href="#窗格操作" class="headerlink" title="窗格操作"></a>窗格操作</h1><h2 id="划分窗格"><a href="#划分窗格" class="headerlink" title="划分窗格"></a>划分窗格</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 划分上下两个窗格</span></span><br><span class="line">$ tmux split-window</span><br><span class="line"></span><br><span class="line"><span class="comment"># 划分左右两个窗格</span></span><br><span class="line">$ tmux split-window -h</span><br></pre></td></tr></table></figure>

<h2 id="移动光标"><a href="#移动光标" class="headerlink" title="移动光标"></a>移动光标</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 光标切换到上方窗口</span></span><br><span class="line">$ tmux select-pane -U</span><br><span class="line"></span><br><span class="line"><span class="comment"># 光标切换到下方窗格</span></span><br><span class="line">$ tmux select-pane -D</span><br><span class="line"></span><br><span class="line"><span class="comment"># 光标切换到左边窗格</span></span><br><span class="line">$ tmux select-pane -L</span><br><span class="line"></span><br><span class="line"><span class="comment"># 光标切换到右边窗格</span></span><br><span class="line">$ tmux select-pane -R</span><br></pre></td></tr></table></figure>

<h2 id="交换窗格位置"><a href="#交换窗格位置" class="headerlink" title="交换窗格位置"></a>交换窗格位置</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 当前窗格上移</span></span><br><span class="line">$ tmux swap-pane -U</span><br><span class="line"></span><br><span class="line"><span class="comment"># 当前窗格下移</span></span><br><span class="line">$ tmux swap-pane -D</span><br></pre></td></tr></table></figure>

<h2 id="窗格快捷键总结"><a href="#窗格快捷键总结" class="headerlink" title="窗格快捷键总结"></a>窗格快捷键总结</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br></pre></td></tr></table></figure>























]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>apt换源</title>
    <url>/2024/09/20/apt%E6%8D%A2%E6%BA%90/</url>
    <content><![CDATA[<p>Ubuntu24.04换源</p>
<blockquote>
<p>Ubuntu24.04源文件地址已经更换为<code>etc/apt/source.list.d/ubuntu.sources</code></p>
</blockquote>
<span id="more"></span>
<p>1.备份当前源列表</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">cp</span> /etc/apt/sources.list.d/ubuntu.sources  /etc/apt/sources.list.d/ubuntu.sources.bak</span><br></pre></td></tr></table></figure>

<p>2.添加阿里云源</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">sudo</span> vim /etc/apt/sources.list.d/ubuntu.source</span><br><span class="line"></span><br><span class="line"><span class="comment"># 在文本编辑器中粘贴以下内容</span></span><br><span class="line"><span class="comment"># 阿里云</span></span><br><span class="line">Types: deb</span><br><span class="line">URIs: http://mirrors.aliyun.com/ubuntu/</span><br><span class="line">Suites: noble noble-updates noble-security</span><br><span class="line">Components: main restricted universe multiverse</span><br><span class="line">Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg</span><br></pre></td></tr></table></figure>

<h1 id="更新源列表和系统软件包"><a href="#更新源列表和系统软件包" class="headerlink" title="更新源列表和系统软件包"></a>更新源列表和系统软件包</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">sudo</span> apt-get update</span><br><span class="line">$ <span class="built_in">sudo</span> apt-get upgrade</span><br></pre></td></tr></table></figure>

<h1 id="清华源"><a href="#清华源" class="headerlink" title="清华源"></a>清华源</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Types: deb</span><br><span class="line">URIs: http://mirrors.tuna.tsinghua.edu.cn/ubuntu/</span><br><span class="line">Suites: noble noble-updates noble-security</span><br><span class="line">Components: main restricted universe multiverse</span><br><span class="line">Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg</span><br></pre></td></tr></table></figure>

<h1 id="中科大源"><a href="#中科大源" class="headerlink" title="中科大源"></a>中科大源</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Types: deb</span><br><span class="line">URIs: http://mirrors.ustc.edu.cn/ubuntu/</span><br><span class="line">Suites: noble noble-updates noble-security</span><br><span class="line">Components: main restricted universe multiverse</span><br><span class="line">Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>PC</category>
      </categories>
  </entry>
  <entry>
    <title>argv[]参数转int型</title>
    <url>/2024/09/20/argv%5B%5D%E5%8F%82%E6%95%B0%E8%BD%ACint%E5%9E%8B/</url>
    <content><![CDATA[<p>在C&#x2F;C++中，argv[]是一个字符数组（char*），保存了从命令行传递该程序的阐述。</p>
<span id="more"></span>
<h1 id="使用atoi-函数"><a href="#使用atoi-函数" class="headerlink" title="使用atoi()函数"></a>使用<code>atoi()</code>函数</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span>  <span class="comment">// 包含 strtol 函数</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (argc &gt; <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="type">char</span> *end;</span><br><span class="line">        <span class="type">long</span> num = strtol(argv[<span class="number">1</span>], &amp;end, <span class="number">10</span>);  <span class="comment">// 将 argv[1] 转换为 long 类型，基数为10</span></span><br><span class="line">        <span class="keyword">if</span> (*end == <span class="string">&#x27;\0&#x27;</span>) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;输入的数字是: %ld\n&quot;</span>, num);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;输入的参数不是有效的数字！\n&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;请提供一个数字参数！\n&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="使用strtol-函数"><a href="#使用strtol-函数" class="headerlink" title="使用strtol()函数"></a>使用strtol()函数</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span>  <span class="comment">// 包含 strtol 函数</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (argc &gt; <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="type">long</span> num = strtol(argv[<span class="number">1</span>], <span class="literal">NULL</span>, <span class="number">10</span>);  <span class="comment">// 将 argv[1] 转换为 long 类型，基数为10</span></span><br><span class="line">        <span class="keyword">if</span> (*end == <span class="string">&#x27;\0&#x27;</span>) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;输入的数字是: %ld\n&quot;</span>, num);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;输入的参数不是有效的数字！\n&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;请提供一个数字参数！\n&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="使用sscanf-函数"><a href="#使用sscanf-函数" class="headerlink" title="使用sscanf()函数"></a>使用sscanf()函数</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (argc &gt; <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="type">int</span> num;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">sscanf</span>(argv[<span class="number">1</span>], <span class="string">&quot;%d&quot;</span>, &amp;num) == <span class="number">1</span>) &#123;  <span class="comment">// 将 argv[1] 转换为 int 类型</span></span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;输入的数字是: %d\n&quot;</span>, num);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;输入的参数不是有效的数字！\n&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;请提供一个数字参数！\n&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="三种方法对比"><a href="#三种方法对比" class="headerlink" title="三种方法对比"></a>三种方法对比</h1><p>atoi()：简单快捷，但无法检测错误输入，使用时需要确保输入是有效的数字，否则返回 0。<br>strtol()：推荐用于更加健壮的转换，它允许处理错误输入并支持不同进制数。<br>sscanf()：灵活性较高，可以处理多种格式，适用于需要从字符串中解析多个值的场景。</p>
]]></content>
      <categories>
        <category>C</category>
      </categories>
  </entry>
  <entry>
    <title>CMake</title>
    <url>/2024/09/20/cmake/</url>
    <content><![CDATA[<h1 id="1-CMake概述"><a href="#1-CMake概述" class="headerlink" title="1.CMake概述"></a>1.CMake概述</h1><p>CMake是一个项目构建工具，是跨平台的。CMake可以自动生成Makefile文件，管理项目更加简单。</p>
<span id="more"></span>
<h2 id="1-1-CMake使用流程"><a href="#1-1-CMake使用流程" class="headerlink" title="1.1 CMake使用流程"></a>1.1 CMake使用流程</h2><p>1.项目中添加CMakeLists.txt文件<br>2.新建一个名为build的文件夹<br>3.在build文件夹中执行cmake <CMakeLists-file-path>构建makefile文件<br>4.执行make指令</p>
<h1 id="2-CMake的用法"><a href="#2-CMake的用法" class="headerlink" title="2.CMake的用法"></a>2.CMake的用法</h1><h2 id="2-1-查看版本号"><a href="#2-1-查看版本号" class="headerlink" title="2.1 查看版本号"></a>2.1 查看版本号</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ cmake --version       <span class="comment"># 查看版本号</span></span><br><span class="line">cmake version 3.28.3</span><br></pre></td></tr></table></figure>

<h2 id="2-2-注释"><a href="#2-2-注释" class="headerlink" title="2.2 注释"></a>2.2 注释</h2><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用 # 进行行注释</span></span><br><span class="line"><span class="comment"># 这是一行注释</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用 #[[]] 进行块注释</span></span><br><span class="line"><span class="comment">#[[</span></span><br><span class="line"><span class="comment">    这是块注释</span></span><br><span class="line"><span class="comment">]]</span></span><br></pre></td></tr></table></figure>

<h2 id="2-3-常用的一些宏"><a href="#2-3-常用的一些宏" class="headerlink" title="2.3 常用的一些宏"></a>2.3 常用的一些宏</h2><ul>
<li><code>CMAKE_CURRENT_SOURCE_DIR</code>：表示当前访问的 CMakeLists.txt 文件所在的路径。</li>
<li><code>CMAKE_SOURCE_DIR</code>：项目的源代码根目录，即包含顶层 CMakeLists.txt 文件的目录。</li>
<li><code>CMAKE_BINARY_DIR</code>：项目的二进制根目录，即当前正在执行 CMake 命令的目录。</li>
<li><code>EXECUTABLE_OUTPUT_PATH</code>：指定可执行文件的输出目录。</li>
<li><code>LIBRARY_OUTPUT_PATH</code>：指定库文件的输出目录。</li>
</ul>
<h2 id="2-4-常用函数"><a href="#2-4-常用函数" class="headerlink" title="2.4 常用函数"></a>2.4 常用函数</h2><h3 id="1-指定使用的CMake的最低版本"><a href="#1-指定使用的CMake的最低版本" class="headerlink" title="1.指定使用的CMake的最低版本"></a>1.指定使用的CMake的最低版本</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">cmake_minimum_required</span>(VERSION <span class="number">3.15</span>)</span><br></pre></td></tr></table></figure>

<h3 id="2-定义工程名称"><a href="#2-定义工程名称" class="headerlink" title="2.定义工程名称"></a>2.定义工程名称</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">project</span>(<span class="keyword">project</span>-name)</span><br></pre></td></tr></table></figure>

<h3 id="3-生成可执行程序"><a href="#3-生成可执行程序" class="headerlink" title="3.生成可执行程序"></a>3.生成可执行程序</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">add_executable</span>(可执行程序名 源文件名称)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例：（源文件可以是多个，之间用空格或者；间隔）</span></span><br><span class="line"><span class="keyword">add_executable</span>(app add.c div.c main.c mult.c sub.c)</span><br><span class="line"><span class="keyword">add_executable</span>(app add.c;div.c;main.c;mult.c;sub.c)</span><br></pre></td></tr></table></figure>

<h3 id="4-定义变量"><a href="#4-定义变量" class="headerlink" title="4.定义变量"></a>4.定义变量</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># VAR:变量名    VALUE:变量值</span></span><br><span class="line"><span class="keyword">set</span>(VAR VALUE)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例：</span></span><br><span class="line"><span class="keyword">set</span>(SRC add.c div.c main.c mult.c sub.c)</span><br><span class="line"><span class="keyword">add_executable</span>(app <span class="variable">$&#123;SRC&#125;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="5-指定只用的C-标准"><a href="#5-指定只用的C-标准" class="headerlink" title="5.指定只用的C++标准"></a>5.指定只用的C++标准</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 方法1：在SHELL中使用g++指令的时候</span></span><br><span class="line">$ g++ *.cpp -std=c++<span class="number">11</span> -o app</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方法2：在CMakeLists.txt中通过set命令指定</span></span><br><span class="line"><span class="keyword">set</span>(CMAKE_CXX_STANDARD <span class="number">11</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方法3：在执行cmake命令的时候指定</span></span><br><span class="line">$ cmake CMakeLists.txt文件路径 -DCMAKE_CXX_STANDARD=<span class="number">11</span></span><br></pre></td></tr></table></figure>

<h3 id="6-指定可执行文件输出的路径"><a href="#6-指定可执行文件输出的路径" class="headerlink" title="6.指定可执行文件输出的路径"></a>6.指定可执行文件输出的路径</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 如果路径中的子目录不存在，会自动生成</span></span><br><span class="line"><span class="keyword">set</span>(EXECUTABLE_OUTPUT_PATH <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/lib)</span><br></pre></td></tr></table></figure>

<h3 id="7-查找某个路径下的所有源文件"><a href="#7-查找某个路径下的所有源文件" class="headerlink" title="7.查找某个路径下的所有源文件"></a>7.查找某个路径下的所有源文件</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># dir:要搜索的目录</span></span><br><span class="line"><span class="comment"># variable:将搜索到的源文件列表存储到改变量中</span></span><br><span class="line"><span class="keyword">aux_source_directory</span>(&lt;dir&gt; &lt;variable&gt;)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例：</span></span><br><span class="line"><span class="keyword">aux_source_directory</span>(<span class="variable">$&#123;CMAKE_CURRENT_SOURCE_DIR&#125;</span>/src SRC)</span><br><span class="line"><span class="keyword">add_executable</span>(app <span class="variable">$&#123;SRC&#125;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="8-根据指定条件搜索文件"><a href="#8-根据指定条件搜索文件" class="headerlink" title="8.根据指定条件搜索文件"></a>8.根据指定条件搜索文件</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># GLOB:将指定目录下搜索到的满足条件的所有文件名生成一个列表，并将其存储到变量中。</span></span><br><span class="line"><span class="comment"># GLOB_RECURSE:递归搜索指定目录，将搜索到的满足条件的文件名生成一个列表，并将其存储到变量中。</span></span><br><span class="line"><span class="keyword">file</span>(GLOB/GLOB_RECURSE 变量名 要搜索的文件路径和文件名)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例：</span></span><br><span class="line"><span class="keyword">file</span>(GLOB MAIN_SRC <span class="variable">$&#123;CMAKE_CURRENT_SOURCE_DIR&#125;</span>/src/*.cpp)</span><br><span class="line"><span class="keyword">file</span>(GLOB MAIN_HEAD <span class="variable">$&#123;CMAKE_CURRENT_SOURCE_DIR/include/*.h&#125;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="9-包含头文件"><a href="#9-包含头文件" class="headerlink" title="9.包含头文件"></a>9.包含头文件</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># head-path:头文件所在路径</span></span><br><span class="line"><span class="keyword">include_directories</span>(head-path)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例：</span></span><br><span class="line"><span class="keyword">include_directories</span>(<span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/<span class="keyword">include</span>)</span><br></pre></td></tr></table></figure>

<h3 id="10-制作静态库"><a href="#10-制作静态库" class="headerlink" title="10.制作静态库"></a>10.制作静态库</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">add_library</span>(库名称 STATIC 源文件)</span><br><span class="line"></span><br><span class="line"><span class="comment">#示例：(最终会生成对应的静态库文件 libcalc.a)</span></span><br><span class="line"><span class="comment"># 搜索源文件</span></span><br><span class="line"><span class="keyword">file</span>(GLOB SRC <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/src/*.cpp)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 包含头文件</span></span><br><span class="line"><span class="keyword">include_directories</span>(<span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/<span class="keyword">include</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 指定静态库输出位置</span></span><br><span class="line"><span class="keyword">set</span>(LIBRARY_OUTPUT_PATH <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/bin)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成静态库</span></span><br><span class="line"><span class="keyword">add_library</span>(calc STATIC <span class="variable">$&#123;SRC&#125;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="11-制作动态库"><a href="#11-制作动态库" class="headerlink" title="11.制作动态库"></a>11.制作动态库</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">add_library</span>(库名称 SHARED 源文件)</span><br><span class="line"></span><br><span class="line"><span class="comment">#示例：(最终会生成对应的静态库文件 libcalc.so)</span></span><br><span class="line"><span class="keyword">file</span>(GLOB SRC <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/src/*.cpp)</span><br><span class="line"><span class="keyword">include_directories</span>(<span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/<span class="keyword">include</span>)</span><br><span class="line"><span class="keyword">set</span>(LIBRARY_OUTPUT_PATH <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/bin)</span><br><span class="line"><span class="keyword">add_library</span>(calc SHARED <span class="variable">$&#123;SRC&#125;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="12-指定静态库-动态库的输出路径"><a href="#12-指定静态库-动态库的输出路径" class="headerlink" title="12.指定静态库&#x2F;动态库的输出路径"></a>12.指定静态库&#x2F;动态库的输出路径</h3><p><strong>方式1-适用于动态库</strong><br>由于在Linux下生成的动态库默认是有可执行权限的，所以可以使用宏<code>EXECUTABLE_OUTPUT_PATH</code>来指定动态库的输出路径。</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 示例：（最终会在./lib文件夹下生成动态库）</span></span><br><span class="line"><span class="keyword">include_directories</span>(<span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/<span class="keyword">include</span>)</span><br><span class="line"><span class="keyword">file</span>(GLOB SRC <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/src/*.cpp)</span><br><span class="line"></span><br><span class="line"><span class="comment">#设置动态库生成路径</span></span><br><span class="line"><span class="keyword">set</span>(EXECUTABLE_OUTPUT_PATH <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/lib)</span><br><span class="line"><span class="keyword">add_library</span>(calc SHARED <span class="variable">$&#123;SRC&#125;</span>)</span><br></pre></td></tr></table></figure>

<p><strong>方式2-适用于静态库和动态库</strong></p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">include_directories</span>(<span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/<span class="keyword">include</span>)</span><br><span class="line"><span class="keyword">file</span>(GLOB SRC <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/src/*.cpp)</span><br><span class="line"></span><br><span class="line"><span class="comment">#设置静态库/动态库生成路径</span></span><br><span class="line"><span class="keyword">set</span>(LIBRARY_OUTPUT_PATH <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/lib)</span><br><span class="line"></span><br><span class="line"><span class="comment">#在指定文件夹下生成静态库</span></span><br><span class="line"><span class="keyword">add_library</span>(calc STATIC <span class="variable">$&#123;SRC&#125;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">#在指定文件夹下生成动态库</span></span><br><span class="line"><span class="keyword">add_library</span>(calc SHARED <span class="variable">$&#123;SRC&#125;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="13-链接静态库"><a href="#13-链接静态库" class="headerlink" title="13. 链接静态库"></a>13. 链接静态库</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 指定静态库的路径</span></span><br><span class="line"><span class="keyword">link_directories</span>(&lt;lib-path&gt;)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 链接静态库 </span></span><br><span class="line"><span class="keyword">link_libraries</span>(&lt;static-lib&gt;)</span><br></pre></td></tr></table></figure>
<p><code>link_libraries</code>用于设置全局链接库，这些库会链接到之后定义的所有目标上。指定要链接的静态库名字：</p>
<ul>
<li>可以是全名 libxxx.a</li>
<li>也可以是掐头（lib）去尾（.a）之后的名字 xxx</li>
</ul>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 示例：</span></span><br><span class="line"><span class="comment"># 搜索指定目录下源文件</span></span><br><span class="line"><span class="keyword">file</span>(GLOB SRC <span class="variable">$&#123;CMAKE_CURRENT_SOURCE_DIR&#125;</span>/src/*.cpp)</span><br><span class="line"><span class="comment"># 包含头文件路径</span></span><br><span class="line"><span class="keyword">include_directories</span>(<span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/<span class="keyword">include</span>)</span><br><span class="line"><span class="comment"># 包含静态库路径</span></span><br><span class="line"><span class="keyword">link_directories</span>(<span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/lib)</span><br><span class="line"><span class="comment"># 链接静态库</span></span><br><span class="line"><span class="keyword">link_libraries</span>(calc)</span><br><span class="line"><span class="comment"># 指定静态库输出位置</span></span><br><span class="line"><span class="keyword">set</span>(LIBRARY_OUTPUT_PATH <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/lib)</span><br><span class="line"><span class="comment"># 生成可执行文件 app</span></span><br><span class="line"><span class="keyword">add_executable</span>(app <span class="variable">$&#123;SRC&#125;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="14-链接动态库"><a href="#14-链接动态库" class="headerlink" title="14.链接动态库"></a>14.链接动态库</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 指定动态库的路径</span></span><br><span class="line"><span class="keyword">link_directories</span>(&lt;lib-path&gt;)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 链接动态库 </span></span><br><span class="line"><span class="keyword">target_link_libraries</span>(&lt;<span class="keyword">target</span>&gt; &lt;shared-lib&gt;)</span><br></pre></td></tr></table></figure>
<p><code>target_link_libraries</code>用于指定一个目标在编译时需要链接哪些库。它支持指定库的名称，路径以及链接库的顺序。<br><code>target</code>：链接动态库之后目标文件<br><code>shared-lib</code>：需要链接的库的名称</p>
<ul>
<li>PUBLIC：用于传递性的链接</li>
<li>PRIVATE：用于非传递性的链接</li>
<li>INTERFACE：用于定义接口，这些接口不会链接到声明它们的库或可执行文件，但会链接到依赖它们的其他目标。</li>
</ul>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 示例：</span></span><br><span class="line"><span class="comment"># 搜索指定目录下源文件</span></span><br><span class="line"><span class="keyword">file</span>(GLOB SRC <span class="variable">$&#123;CMAKE_CURRENT_SOURCE_DIR&#125;</span>/src/*.cpp)</span><br><span class="line"><span class="comment"># 包含头文件路径</span></span><br><span class="line"><span class="keyword">include_directories</span>(<span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/<span class="keyword">include</span>)</span><br><span class="line"><span class="comment"># 包含动态库路径</span></span><br><span class="line"><span class="keyword">link_directories</span>(<span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/lib)</span><br><span class="line"><span class="comment"># 指定静态库输出位置</span></span><br><span class="line"><span class="keyword">set</span>(LIBRARY_OUTPUT_PATH <span class="variable">$&#123;PROJECT_SOURCE_DIR&#125;</span>/lib)</span><br><span class="line"><span class="comment"># 生成可执行文件 app</span></span><br><span class="line"><span class="keyword">add_executable</span>(app <span class="variable">$&#123;SRC&#125;</span>)</span><br><span class="line"><span class="comment"># 链接动态库</span></span><br><span class="line"><span class="keyword">target_link_libraries</span>(app calc)</span><br></pre></td></tr></table></figure>

<h3 id="15-添加子目录"><a href="#15-添加子目录" class="headerlink" title="15.添加子目录"></a>15.添加子目录</h3><p>Linux的目录是树状结构，所以嵌套的 CMake 也是一个树状结构，最顶层的 CMakeLists.txt 是根节点，其次都是子节点。</p>
<ul>
<li>根节点CMakeLists.txt中的变量全局有效</li>
<li>父节点CMakeLists.txt中的变量可以在子节点中使用</li>
<li>子节点CMakeLists.txt中的变量只能在当前节点中使用</li>
</ul>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># source_dir：指定子目录。</span></span><br><span class="line"><span class="comment"># binary_dir：指定了输出文件的路径，一般不需要指定。</span></span><br><span class="line"><span class="comment"># EXCLUDE_FROM_ALL：一般不需要设置，忽略。</span></span><br><span class="line"><span class="keyword">add_subdirectory</span>(source_dir [binary-dir] [EXCLUDE_FROM_ALL])</span><br><span class="line"></span><br><span class="line"><span class="comment">#示例：</span></span><br><span class="line"><span class="keyword">add_subdirectory</span>(calc)</span><br></pre></td></tr></table></figure>

<h3 id="15-日志"><a href="#15-日志" class="headerlink" title="15.日志"></a>15.日志</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">message</span>([STATUS|WARNING|AUTHOR_WARNING|FATAL_ERROR|SEND_ERROR] <span class="string">&quot;message to display&quot;</span>)</span><br></pre></td></tr></table></figure>
<ul>
<li>(无)：重要消息</li>
<li>STATUS：非重要消息</li>
<li>WARNING：CMake警告，会继续执行</li>
<li>AUTHOR_WARNING：CMake警告，会继续执行</li>
<li>SEND_ERROR：CMake 错误，继续执行，但是会跳过生成的步骤</li>
<li>FATAL_ERROR：CMake 错误, 终止所有处理过程</li>
</ul>
<p>CMake的命令行工具会在stdout上显示(无)或STATUS消息，在stderr上显示其他所有消息。CMake的GUI会在它的log区域显示所有消息。</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 示例：</span></span><br><span class="line"><span class="comment"># 输出一般日志信息</span></span><br><span class="line"><span class="keyword">message</span>(STATUS <span class="string">&quot;source path: $&#123;PROJECT_SOURCE_DIR&#125;&quot;</span>)</span><br><span class="line"><span class="comment"># 输出警告信息</span></span><br><span class="line"><span class="keyword">message</span>(WARNING <span class="string">&quot;source path: $&#123;PROJECT_SOURCE_DIR&#125;&quot;</span>)</span><br><span class="line"><span class="comment"># 输出错误信息</span></span><br><span class="line"><span class="keyword">message</span>(FATAL_ERROR <span class="string">&quot;source path: $&#123;PROJECT_SOURCE_DIR&#125;&quot;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="16-变量操作"><a href="#16-变量操作" class="headerlink" title="16.变量操作"></a>16.变量操作</h3><p>1.使用set拼接<br>将从第二个参数开始往后所有的字符串进行拼接，最后将结果存储到第一个参数中，如果第一个参数中原来有数据会对原数据就行覆盖。</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">set</span>(变量名<span class="number">1</span> <span class="variable">$&#123;变量名1&#125;</span> <span class="variable">$&#123;变量名2&#125;</span> ...)</span><br></pre></td></tr></table></figure>

<p>2.获取list的长度</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># LENGTH：关键字</span></span><br><span class="line"><span class="comment"># list：当前操作列表</span></span><br><span class="line"><span class="comment"># output-variable：新创建的变量，用于存储列表长度</span></span><br><span class="line"><span class="keyword">list</span>(LENGTH &lt;<span class="keyword">list</span>&gt; &lt;output variable&gt;)</span><br></pre></td></tr></table></figure>

<p>3.读取列表中指定索引的的元素，可以指定多个索引</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># element-index：列表元素的索引，索引0为列表第一个元素；索引也可以是负数，-1表示列表的最后一个元素，-2表示列表倒数第二个元素；</span></span><br><span class="line"><span class="comment"># output-variable：新创建的变量，存储指定索引元素的返回结果，也是一个列表</span></span><br><span class="line"><span class="keyword">list</span>(GET &lt;<span class="keyword">list</span>&gt; &lt;element-index&gt; &lt;output-variable&gt;)</span><br></pre></td></tr></table></figure>

<p>4.将列表中的元素用连接符（字符串）连接起来组成一个字符串</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># glue：指定的连接符（字符串）</span></span><br><span class="line"><span class="keyword">list</span> (JOIN &lt;<span class="keyword">list</span>&gt; &lt;glue&gt; &lt;output-variable&gt;)</span><br></pre></td></tr></table></figure>

<p>5.查找列表是否存在指定的元素，若果未找到，返回-1</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># output-variable：如果找到则为索引值，否则为-1</span></span><br><span class="line"><span class="keyword">list</span>(FIND &lt;<span class="keyword">list</span>&gt; &lt;value&gt; &lt;output-variable&gt;)</span><br></pre></td></tr></table></figure>

<p>6.将元素追加到列表中</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">list</span> (APPEND &lt;<span class="keyword">list</span>&gt; &lt;element&gt;)</span><br></pre></td></tr></table></figure>

<p>7.在list中指定的位置插入若干元素</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">list</span>(INSERT &lt;<span class="keyword">list</span>&gt; &lt;element-index&gt; &lt;element&gt;)</span><br></pre></td></tr></table></figure>

<p>8.将元素插入到列表的0索引位置</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">list</span> (PREPEND &lt;<span class="keyword">list</span>&gt; &lt;element&gt;)</span><br></pre></td></tr></table></figure>

<p>9.将列表中最后元素移除</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">list</span> (POP_BACK &lt;<span class="keyword">list</span>&gt; &lt;out-var&gt;)</span><br></pre></td></tr></table></figure>

<p>10.将列表中第一个元素移除</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">list</span> (POP_FRONT &lt;<span class="keyword">list</span>&gt; &lt;out-var&gt;)</span><br></pre></td></tr></table></figure>

<p>11.将指定的元素从列表中移除</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">list</span> (REMOVE_ITEM &lt;<span class="keyword">list</span>&gt; &lt;value&gt;)</span><br></pre></td></tr></table></figure>

<p>12.将指定索引的元素从列表中移除</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">list</span> (REMOVE_AT &lt;<span class="keyword">list</span>&gt; &lt;index&gt;)</span><br></pre></td></tr></table></figure>

<p>13.移除列表中的重复元素</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">list</span> (REMOVE_DUPLICATES &lt;<span class="keyword">list</span>&gt;)</span><br></pre></td></tr></table></figure>

<p>14.列表翻转</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">list</span>(REVERSE &lt;<span class="keyword">list</span>&gt;)</span><br></pre></td></tr></table></figure>

<p>15.列表排序</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">list</span> (SORT &lt;<span class="keyword">list</span>&gt; [COMPARE &lt;compare&gt;] [CASE &lt;case&gt;] [ORDER &lt;order&gt;])</span><br></pre></td></tr></table></figure>
<p>COMPARE：指定排序方法。有如下几种值可选</p>
<ul>
<li>STRING：按照字母顺序进行排序，为默认的排序方法</li>
<li>FILE_BASENAME：如果是一系列路径名，会使用basename进行排序</li>
<li>NATURAL：使用自然数顺序排序</li>
</ul>
<p>CASE：指明是否大小写敏感。有如下几种值可选：</p>
<ul>
<li>SENSITIVE: 按照大小写敏感的方式进行排序，为默认值</li>
<li>INSENSITIVE：按照大小写不敏感方式进行排序</li>
</ul>
<p>ORDER：指明排序的顺序。有如下几种值可选：</p>
<ul>
<li>ASCENDING：按照升序排列，为默认值</li>
<li>DESCENDING：按照降序排列</li>
</ul>
<h2 id="2-5-宏定义"><a href="#2-5-宏定义" class="headerlink" title="2.5 宏定义"></a>2.5 宏定义</h2><p>在进行程序测试的时候，我们可以在代码中添加一些宏定义，通过这些宏来控制这些代码是否生效：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> NUMBER  3</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> DEBUG</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;this is debug place\n&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>; i&lt;NUMBER; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;hello, world\n&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>为了测试更加灵活，一般不在代码中定义这个宏，而是在测试的时候把这个宏定义出来。</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在gcc/g++命令中定义</span></span><br><span class="line">$ gcc main.c -DDEBUG -o app</span><br><span class="line"></span><br><span class="line"><span class="comment"># 在CMakeLists.txt文件中定义</span></span><br><span class="line"><span class="keyword">add_definitions</span>(-DDEBUG)</span><br><span class="line"><span class="keyword">add_executable</span>(app ./main.c)</span><br></pre></td></tr></table></figure>

<h1 id="生成能够gdb调试的可执行文件"><a href="#生成能够gdb调试的可执行文件" class="headerlink" title="生成能够gdb调试的可执行文件"></a>生成能够gdb调试的可执行文件</h1><p>在CMakeLists.txt中加入：</p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SET</span>(CMAKE_BUILD_TYPE <span class="string">&quot;Debug&quot;</span>)</span><br><span class="line"><span class="keyword">SET</span>(CMAKE_CXX_FLAGS_DEBUG <span class="string">&quot;$ENV&#123;CXXFLAGS&#125; -O0 -Wall -g2 -ggdb&quot;</span>)</span><br><span class="line"><span class="keyword">SET</span>(CMAKE_CXX_FLAGS_RELEASE <span class="string">&quot;$ENV&#123;CXXFLAGS&#125; -O3 -Wall&quot;</span>)</span><br></pre></td></tr></table></figure>
<p>CMAKE_BUILD_TYPE是CMake构建系统使用的一个变量，用于指定编译时的优化级别和调试信息的详细程度，常用的值有:</p>
<ul>
<li>Debug</li>
<li>Release</li>
<li>RelWithDebInfo</li>
<li>MinSizeRel<br>当这个变量值为 Debug 的时候,CMake 会使用变量 CMAKE_CXX_FLAGS_DEBUG 和 CMAKE_C_FLAGS_DEBUG 中的字符串作为编译选项生成 Makefile</li>
</ul>
<h1 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h1><p><a href="https://subingwen.cn/cmake/CMake-primer/">大丙老师的 CMake 保姆级教程-上</a><br><a href="https://subingwen.cn/cmake/CMake-advanced/">大丙老师的 CMake 保姆级教程-下</a></p>
]]></content>
      <categories>
        <category>cmake</category>
      </categories>
  </entry>
  <entry>
    <title>.bashrc</title>
    <url>/2024/09/20/bashrc/</url>
    <content><![CDATA[<p>.bashrc是一个用于配置Bash Shell的脚本文件。位于<code>~/.bashrc</code>，当打开一个新的Bash终端会话时，Bash会自动执行这个文件中的命令，这个文件通常用于：设置环境变量，定义别名，配置Shell选项。</p>
<span id="more"></span>
<h1 id="设置环境变量"><a href="#设置环境变量" class="headerlink" title="设置环境变量"></a>设置环境变量</h1><h2 id="设置PATH环境变量"><a href="#设置PATH环境变量" class="headerlink" title="设置PATH环境变量"></a>设置PATH环境变量</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">export PATH=/usr/local/bin:$PATH</span><br></pre></td></tr></table></figure>
<p>将&#x2F;usr&#x2F;local&#x2F;lib目录添加到PATH环境变量的前面，当在命令行中输入命令时，系统首先会在&#x2F;usr&#x2F;local&#x2F;lib目录先查找。</p>
<h2 id="设置一个新的环境变量"><a href="#设置一个新的环境变量" class="headerlink" title="设置一个新的环境变量"></a>设置一个新的环境变量</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">export VAR = &quot;Hello,World!&quot;</span><br></pre></td></tr></table></figure>
<p>创建一个名为VAR的新环境变量，并将它的值设置为<code>Hello,World!</code></p>
<h2 id="为特定应用设置环境变量"><a href="#为特定应用设置环境变量" class="headerlink" title="为特定应用设置环境变量"></a>为特定应用设置环境变量</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">export JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64</span><br><span class="line">export PATH=$JAVA_HOME/bin:$PATH</span><br></pre></td></tr></table></figure>
<p>设置了JAVA_HOME环境变量，指向java的安装命令，并将bin目录添加到PATH中，之后就可以直接在命令中使用java命令。</p>
<h2 id="设置库目录"><a href="#设置库目录" class="headerlink" title="设置库目录"></a>设置库目录</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">export LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH</span><br></pre></td></tr></table></figure>
<p>将<code>/usr/local/lib</code>目录添加到<code>LD_LIBRARY_PATH</code>环境变量中。</p>
<h2 id="条件性设置环境变量"><a href="#条件性设置环境变量" class="headerlink" title="条件性设置环境变量"></a>条件性设置环境变量</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if [ -d /opt/myapp ]; then</span><br><span class="line">    export MYAPP_HOME=/opt/myapp</span><br><span class="line">    export PATH=$MYAPP_HOME/bin:$PATH</span><br><span class="line">fi</span><br></pre></td></tr></table></figure>
<p>检查&#x2F;opt&#x2F;myapp目录是否存在，如果存在则设置MY_HOME环境变量，并将bin目录添加到PATH中。</p>
<h1 id="设置别名"><a href="#设置别名" class="headerlink" title="设置别名"></a>设置别名</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 为 ls 和其他命令启用颜色支持，并添加有用的别名</span><br><span class="line">if [ -x /usr/bin/dircolors ]; then</span><br><span class="line">    test -r ~/.dircolors &amp;&amp; eval &quot;$(dircolors -b ~/.dircolors)&quot; || eval &quot;$(dircolors -b)&quot;</span><br><span class="line">    alias ls=&#x27;ls --color=auto&#x27;</span><br><span class="line">    #alias dir=&#x27;dir --color=auto&#x27;</span><br><span class="line">    #alias vdir=&#x27;vdir --color=auto&#x27;</span><br><span class="line"></span><br><span class="line">    alias grep=&#x27;grep --color=auto&#x27;</span><br><span class="line">    alias fgrep=&#x27;fgrep --color=auto&#x27;</span><br><span class="line">    alias egrep=&#x27;egrep --color=auto&#x27;</span><br><span class="line">fi</span><br></pre></td></tr></table></figure>
<p><code>if [ -x /usr/bin/dircolors ]; </code>:检查 &#x2F;usr&#x2F;bin&#x2F;dircolors 文件是否存在并且具有可执行权限，从而决定是否要执行后续命令。</p>
<p><code>test -r ~/.dircolors &amp;&amp; eval &quot;$(dircolors -b ~/.dircolors)&quot; || eval &quot;$(dircolors -b)&quot;</code>:检查 ~&#x2F;.dircolors 文件是否存在并且可读。如果文件存在且可读，使用 dircolors 命令和该文件的设置来配置颜色。如果文件不存在或不可读，使用 dircolors 命令的默认设置来配置颜色。目的是确保 LS_COLORS 环境变量被设置，以便 ls 命令能够根据这些设置显示颜色。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 其他别名</span><br><span class="line">alias ll=&#x27;ls -alF&#x27;</span><br><span class="line">alias la=&#x27;ls -A&#x27;</span><br><span class="line">alias l=&#x27;ls -CF&#x27;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>gcc</title>
    <url>/2024/09/20/gcc/</url>
    <content><![CDATA[<h1 id="gcc编译流程"><a href="#gcc编译流程" class="headerlink" title="gcc编译流程"></a>gcc编译流程</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1.预处理</span></span><br><span class="line">$ gcc -E main.c -o main.i</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2.编译</span></span><br><span class="line">$ gcc -S main.i -o main.s</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3.汇编</span></span><br><span class="line">$ gcc -c main.s -o main.o</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4.链接</span></span><br><span class="line">$ gcc main.o -o main</span><br></pre></td></tr></table></figure>
<span id="more"></span>
<h1 id="gcc常用参数"><a href="#gcc常用参数" class="headerlink" title="gcc常用参数"></a>gcc常用参数</h1><ul>
<li>-E：预处理，不进行编译</li>
<li>-S：编译，但不汇编</li>
<li>-c：编译并汇编，但不链接</li>
<li>-o：指定生成的文件名</li>
<li>-I：指定include包含文件的目录</li>
<li>-g：生成调试信息</li>
<li>-D：编译的时候指定一个宏</li>
<li>-W：不生成任何警告</li>
<li>-Wall：生成所有警告</li>
<li>-On：代码优化</li>
<li>-l：编译的时候，指定使用的库</li>
<li>-L：指定编译的时候搜索库的路径</li>
<li>-fpic：生成与位置无关代码</li>
<li>-shared：生成共享目标文件</li>
<li>-std：指定标准</li>
</ul>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>.deb文件</title>
    <url>/2024/09/20/deb%E6%96%87%E4%BB%B6/</url>
    <content><![CDATA[<p>dep文件是debian，Ubuntu等Linux发行版的软件安装包</p>
<span id="more"></span>
<h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><p>deb包安装方法有两种：<br>1.直接“双击”安装<br>2.指令安装<br>2.1.终端命令行安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">sudo</span> dpkg -i &lt;package.deb&gt;</span><br></pre></td></tr></table></figure>

<p>2.2.终端命令行卸载</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">sudo</span> dpkg -r &lt;package&gt;</span><br></pre></td></tr></table></figure>
<h1 id="dpkg指令的其他用法"><a href="#dpkg指令的其他用法" class="headerlink" title="dpkg指令的其他用法"></a><code>dpkg</code>指令的其他用法</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ dpkg -i &lt;package.deb&gt;     <span class="comment">#安装一个 Debian 软件包，如手动下载的文件。</span></span><br><span class="line">$ dpkg -c &lt;package.deb&gt;     <span class="comment">#列出 package.deb 的内容。</span></span><br><span class="line">$ dpkg -I &lt;package.deb&gt;     <span class="comment">#从 package.deb 中提取包信息。</span></span><br><span class="line">$ dpkg -r &lt;package&gt;         <span class="comment">#移除一个已安装的包。</span></span><br><span class="line">$ dpkg -P &lt;package&gt;：       <span class="comment">#完全清除一个已安装的包。和 remove 不同的是，remove 只是删掉数据和可执行文件，purge 另外还删除所有的配制文件。</span></span><br><span class="line">$ dpkg -L &lt;package&gt;     <span class="comment">#列出 package 安装的所有文件清单。</span></span><br><span class="line">$ dpkg -s &lt;package&gt;     <span class="comment">#显示已安装包的信息。</span></span><br><span class="line">$ dpkg -S &lt;package&gt;     <span class="comment">#查看软件在哪个包里；</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>hosts</title>
    <url>/2024/09/20/hosts/</url>
    <content><![CDATA[<p>在Ubuntu上安装nvm遇到：curl: (28) Failed to connect to raw.githubusercontent.com port 443: Connection timed out [closed]<br>1.打开<code>/etc/hosts</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">sudo</span> vim /etc/hosts</span><br></pre></td></tr></table></figure>
<span id="more"></span>
<p>2.查询可用IP<br>使用<a href="https://www.ipaddress.com/website/">ipaddress</a>网站查询<code>github.com</code>,<code>ssh.github.com</code>,<code>raw.githubusercontent.com</code>网站的可用IP</p>
<p>3.在末尾添加以下IP地址</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">140.82.114.3  github.com</span><br><span class="line">140.82.113.36  ssh.github.com</span><br><span class="line">199.232.68.133 raw.githubusercontent.com</span><br></pre></td></tr></table></figure>

<p>4.保存文件并退出</p>
<p>5.可以使用ping查看域名是否有received</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ ping 199.232.68.133</span><br><span class="line">PING 199.232.68.133 (199.232.68.133) 56(84) bytes of data.</span><br><span class="line">64 bytes from 199.232.68.133: icmp_seq=1 ttl=43 time=234 ms</span><br><span class="line">64 bytes from 199.232.68.133: icmp_seq=2 ttl=43 time=234 ms</span><br><span class="line">64 bytes from 199.232.68.133: icmp_seq=3 ttl=43 time=234 ms</span><br><span class="line">64 bytes from 199.232.68.133: icmp_seq=4 ttl=43 time=233 ms</span><br><span class="line">^C</span><br><span class="line">--- 199.232.68.133 ping statistics ---</span><br><span class="line">4 packets transmitted, 4 received, 0% packet loss, time 3004ms</span><br><span class="line">rtt min/avg/max/mdev = 233.337/233.849/234.302/0.421 ms</span><br></pre></td></tr></table></figure>

<p><strong>ps.为什么要这样做呢??? hosts是什么配置文件呢???</strong><br><strong>hosts文件是什么？</strong><br>在Linux系统中，<code>/etc/hosts</code>文件是一个本地的DNS解析文件，用于将IP地址和主机名映射起来。当你访问某个域名时，系统会首先检查这个文件，如果在这里找到了对应的IP地址，就会直接使用，而不再通过外部DNS服务器查询。<br><strong>为什么要这么做？</strong><br>这是将GitHub相关的域名（如 github.com、ssh.github.com 和 raw.githubusercontent.com）手动映射到指定的IP地址。这么做的目的通常有以下几种可能：</p>
<ul>
<li>解决访问问题：有时候由于DNS解析缓慢或者网络封锁，GitHub相关服务的域名可能会无法访问，通过手动在&#x2F;etc&#x2F;hosts中添加这些映射，可以直接访问指定的IP，绕过DNS解析。</li>
<li>加快访问速度：如果你已经知道某个域名的IP地址，手动添加可以减少系统查询外部DNS的时间，从而提升访问速度。</li>
<li>绕过DNS劫持：在某些情况下，网络服务提供商或某些恶意软件可能会劫持DNS请求，将你引导到错误的网站，通过手动配置IP可以避免这种情况。</li>
</ul>
<p>IP地址可能会随着时间变化，所以如果GitHub更新了服务器的IP地址，而你还在使用旧的配置，可能会导致访问失败。</p>
]]></content>
      <categories>
        <category>PC</category>
      </categories>
  </entry>
  <entry>
    <title>gdb</title>
    <url>/2024/09/20/gdb/</url>
    <content><![CDATA[<h1 id="开始"><a href="#开始" class="headerlink" title="开始"></a>开始</h1><p>编译程序的时候：<br>1.打开编译选项（-g）<br>2.关掉编译器优化选项（-O0）<br>3.打开所有的warning（-Wall）</p>
<span id="more"></span>

<h1 id="启动和退出gdb"><a href="#启动和退出gdb" class="headerlink" title="启动和退出gdb"></a>启动和退出gdb</h1><h2 id="启动gdb"><a href="#启动gdb" class="headerlink" title="启动gdb"></a>启动gdb</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 语法：gdb 可执行程序</span></span><br><span class="line">$ gdb main</span><br></pre></td></tr></table></figure>

<h2 id="gdb中命令行传参（set-args）"><a href="#gdb中命令行传参（set-args）" class="headerlink" title="gdb中命令行传参（set args）"></a>gdb中命令行传参（set args）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 启动gdb之后</span></span><br><span class="line"><span class="comment"># 语法：set args 参数1 参数2 ...</span></span><br><span class="line"><span class="comment"># 通过gdb给应用程序设置命令行参数</span></span><br><span class="line">(gdb) <span class="built_in">set</span> args 1 2 3</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看设置的命令行参数</span></span><br><span class="line">(gdb) show args</span><br></pre></td></tr></table></figure>

<h2 id="gdb中启动程序（run-start）"><a href="#gdb中启动程序（run-start）" class="headerlink" title="gdb中启动程序（run&#x2F;start）"></a>gdb中启动程序（run&#x2F;start）</h2><ul>
<li>run（r）：如果程序设置了断点，会停在断点的位置，如果没有设置断点，程序执行到结束。</li>
<li>start：启动程序，程序运行到main函数的第一行。</li>
</ul>
<p>ps.<code>run</code>和<code>start</code>在启动gdb之后只能执行其中一个，并只能执行一次。如果遇到断点需要继续执行，输入命令<code>continue(c)</code></p>
<h2 id="退出gdb（quit）"><a href="#退出gdb（quit）" class="headerlink" title="退出gdb（quit）"></a>退出gdb（quit）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(gdb) quit</span><br><span class="line">or</span><br><span class="line">(gdb) q</span><br></pre></td></tr></table></figure>

<h1 id="查看代码（list）"><a href="#查看代码（list）" class="headerlink" title="查看代码（list）"></a>查看代码（list）</h1><h2 id="在当前文件下查看代码"><a href="#在当前文件下查看代码" class="headerlink" title="在当前文件下查看代码"></a>在当前文件下查看代码</h2><p>通过list查看代码，每次只显示10行，如果需要继续查看后面的代码，直接按回车（即再次执行list指令）</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 从第一行开始显示</span><br><span class="line">(gdb) list</span><br><span class="line"></span><br><span class="line"># 显示第n行的上下文</span><br><span class="line">(gdb) list n</span><br><span class="line"></span><br><span class="line"># 显示某个函数的上下文</span><br><span class="line">(gdb) list &lt;function-name&gt;</span><br></pre></td></tr></table></figure>

<h2 id="显示其他文件下的代码"><a href="#显示其他文件下的代码" class="headerlink" title="显示其他文件下的代码"></a>显示其他文件下的代码</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 切换到指定的文件，并列出第n行对应的上下文</span><br><span class="line">(gdb) list &lt;filename&gt;:n</span><br><span class="line"></span><br><span class="line"># 切换到指定的文件，并列出函数的上下文</span><br><span class="line">(gdb) list &lt;filename&gt;:&lt;function-name&gt;</span><br></pre></td></tr></table></figure>

<h2 id="设置显示的行数"><a href="#设置显示的行数" class="headerlink" title="设置显示的行数"></a>设置显示的行数</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 设置每次显示的行数</span><br><span class="line">(gdb) set listsize 行数</span><br><span class="line"></span><br><span class="line"># 查看当前list一次能显示的行数</span><br><span class="line">(gdb) show listsize</span><br></pre></td></tr></table></figure>

<h1 id="断点"><a href="#断点" class="headerlink" title="断点"></a>断点</h1><h2 id="设置断点（break）"><a href="#设置断点（break）" class="headerlink" title="设置断点（break）"></a>设置断点（break）</h2><ul>
<li>常规断点</li>
<li>条件断点</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 在当前文件下设置断点</span><br><span class="line">(gdb) b &lt;n&gt;</span><br><span class="line">(gdb) b &lt;function-name&gt;</span><br><span class="line"></span><br><span class="line"># 在其他文件上设置断点（比如当前在PC指针在main.c文件下，但是想在sub.c中设置断点）</span><br><span class="line">(gdb) b &lt;filename&gt;:n</span><br><span class="line">(gdb) b &lt;filename&gt;:&lt;function-name&gt;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 条件断点：只有满足某个条件后程序才会停在这个断点的位置上</span><br><span class="line">(gdb) b &lt;n&gt; if 变量名==某个值</span><br></pre></td></tr></table></figure>

<h2 id="查看断点（info）"><a href="#查看断点（info）" class="headerlink" title="查看断点（info）"></a>查看断点（info）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(gdb) info b</span><br><span class="line">Num     Type           Disp Enb Address            What</span><br><span class="line">1       breakpoint     keep y   0x0000000000400cb5 in main() at test.cpp:12</span><br></pre></td></tr></table></figure>
<ul>
<li>Num：断点的编号</li>
<li>Enb：断点的状态，y表示使能，n表示未使能</li>
<li>What：断点所在文件，函数，行数</li>
</ul>
<h2 id="删除断点（delete）"><a href="#删除断点（delete）" class="headerlink" title="删除断点（delete）"></a>删除断点（delete）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 删除一个断点</span><br><span class="line">(gdb) d 断点编号</span><br><span class="line"></span><br><span class="line"># 删除断点1,2,3</span><br><span class="line">(gdb) d 1 2 3</span><br><span class="line"></span><br><span class="line"># 删除一个范围的断点</span><br><span class="line">(gdb) d 断点编号1-断点编号N</span><br><span class="line"></span><br><span class="line"># 删除编号1到编号5的所有断点</span><br><span class="line">(gdb) d 1-5</span><br></pre></td></tr></table></figure>

<h2 id="设置断点状态（enable-disable）"><a href="#设置断点状态（enable-disable）" class="headerlink" title="设置断点状态（enable&#x2F;disable）"></a>设置断点状态（enable&#x2F;disable）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 设置断点无效</span><br><span class="line">(gdb) disable 断点编号</span><br><span class="line"></span><br><span class="line"># 设置断点有效</span><br><span class="line">(gdb) enable 断点编号</span><br></pre></td></tr></table></figure>

<h1 id="调试"><a href="#调试" class="headerlink" title="调试"></a>调试</h1><h2 id="继续运行（continue）"><a href="#继续运行（continue）" class="headerlink" title="继续运行（continue）"></a>继续运行（continue）</h2><p>遇到断点，程序停止，可以执行c指令继续向下运行，直到遇到下一个断点。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(gdb) continue</span><br></pre></td></tr></table></figure>

<h2 id="手动打印信息"><a href="#手动打印信息" class="headerlink" title="手动打印信息"></a>手动打印信息</h2><h3 id="打印变量值（print）"><a href="#打印变量值（print）" class="headerlink" title="打印变量值（print）"></a>打印变量值（print）</h3><table>
<thead>
<tr>
<th align="center">格式化字符</th>
<th align="center">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="center">&#x2F;x</td>
<td align="center">十六进制</td>
</tr>
<tr>
<td align="center">&#x2F;d</td>
<td align="center">有符号十进制</td>
</tr>
<tr>
<td align="center">&#x2F;u</td>
<td align="center">无符号十进制</td>
</tr>
<tr>
<td align="center">&#x2F;o</td>
<td align="center">八进制</td>
</tr>
<tr>
<td align="center">&#x2F;t</td>
<td align="center">二进制</td>
</tr>
<tr>
<td align="center">&#x2F;f</td>
<td align="center">浮点型</td>
</tr>
<tr>
<td align="center">&#x2F;c</td>
<td align="center">字符型</td>
</tr>
</tbody></table>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(gdb) p 变量名</span><br><span class="line"># 以16进制打印变量i</span><br><span class="line">(gdb) p/x i</span><br></pre></td></tr></table></figure>

<h3 id="打印变量类型（ptype）"><a href="#打印变量类型（ptype）" class="headerlink" title="打印变量类型（ptype）"></a>打印变量类型（ptype）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(gdb) ptype 变量名</span><br><span class="line"># 打印数组array的类型</span><br><span class="line">(gdb) ptype array</span><br><span class="line">type = int[12]</span><br></pre></td></tr></table></figure>

<h2 id="查看内存（x）"><a href="#查看内存（x）" class="headerlink" title="查看内存（x）"></a>查看内存（x）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">x &lt;addr&gt;</span><br><span class="line"></span><br><span class="line"># 查看函数fun的地址,以16进制输出</span><br><span class="line">(gdb) x/x fun</span><br></pre></td></tr></table></figure>

<h2 id="自动打印信息"><a href="#自动打印信息" class="headerlink" title="自动打印信息"></a>自动打印信息</h2><h3 id="设置某些变量自动打印变量值-display"><a href="#设置某些变量自动打印变量值-display" class="headerlink" title="设置某些变量自动打印变量值(display)"></a>设置某些变量自动打印变量值(display)</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(gdb) display 变量名</span><br><span class="line"></span><br><span class="line"># 以16进制自动显示变量i的值</span><br><span class="line">(gdb) display/x i</span><br></pre></td></tr></table></figure>

<h3 id="查看自动显示列表-info-display"><a href="#查看自动显示列表-info-display" class="headerlink" title="查看自动显示列表(info display)"></a>查看自动显示列表(info display)</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(gdb) info display</span><br><span class="line">Auto-display expressions now in effect:</span><br><span class="line">Num Enb Expression</span><br><span class="line">2:   y  array[i]</span><br></pre></td></tr></table></figure>
<ul>
<li>Num：变量&#x2F;表达式的编号（唯一）。</li>
<li>Enb：y表示激活状态，n表示禁用状态</li>
<li>Expression：变量&#x2F;表达式的名字</li>
</ul>
<h3 id="取消自动显示-undisplay"><a href="#取消自动显示-undisplay" class="headerlink" title="取消自动显示(undisplay)"></a>取消自动显示(undisplay)</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 删除自动显示列表中的变量或表达式</span><br><span class="line">(gdb) undisplay 编号</span><br><span class="line"></span><br><span class="line"># 禁用某个变量自动显示</span><br><span class="line">(gdb) disable display 编号</span><br><span class="line"></span><br><span class="line"># 激活某个变量自动显示</span><br><span class="line">(gdb) enable display 编号</span><br></pre></td></tr></table></figure>

<h2 id="单步调试"><a href="#单步调试" class="headerlink" title="单步调试"></a>单步调试</h2><h3 id="step（s）"><a href="#step（s）" class="headerlink" title="step（s）"></a>step（s）</h3><p>命令被执行一次，代码向下运行一行，如果有函数，则会进入函数体内部。</p>
<h3 id="finish"><a href="#finish" class="headerlink" title="finish"></a>finish</h3><p>通过s单步调试进入函数体内部，如果想要跳出函数体，执行该命令。但是需要保证函数体内不能有有效断点，否则无法跳出。</p>
<h3 id="next"><a href="#next" class="headerlink" title="next"></a>next</h3><p>命令被执行一次，代码向下运行一行，不会进入函数体内部。</p>
<h3 id="until"><a href="#until" class="headerlink" title="until"></a>until</h3><p>通过until命令跳出循环体，需要满足以下条件：</p>
<ul>
<li>要跳出的函数体内部不能有有效的断点</li>
<li>必须要在循环体的开始&#x2F;结束行执行该命令</li>
</ul>
<h2 id="设置变量值"><a href="#设置变量值" class="headerlink" title="设置变量值"></a>设置变量值</h2><p>在循环体中，如果循环比较久，可以直接设置变量到最后两次循环，然后单步运行查看运行结果。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(gdb) set var 变量名=值</span><br><span class="line"># 在100次的循环体中设置变量i为99</span><br><span class="line">(gdb) set var i=99</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>makefile</title>
    <url>/2024/09/20/makefile/</url>
    <content><![CDATA[<h1 id="规则"><a href="#规则" class="headerlink" title="规则"></a>规则</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">target:depend</span><br><span class="line">    commend</span><br></pre></td></tr></table></figure>
<p>每条规则由三部分组成：目标（target），依赖（depend），命令（comment）</p>
<ul>
<li>目标：目标可以有多个</li>
<li>依赖：依赖可有可无</li>
<li>命令：前面要有Tab缩进，shell命令</li>
</ul>
<span id="more"></span>
<h1 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h1><p>make命令执行的时候根据时间戳判定是否执行makefile文件中相关规则的命令。</p>
<ul>
<li>目标是通过依赖生成的，目标时间戳&gt;所有依赖时间戳，则不会执行规则中的命令</li>
<li>目标时间戳&lt;某些依赖的时间戳，则执行规则中的命令</li>
<li>目标文件不存在，执行规则中的命令</li>
</ul>
<h1 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h1><h2 id="自定义变量"><a href="#自定义变量" class="headerlink" title="自定义变量"></a>自定义变量</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 定义变量（必须要同时赋值）</span><br><span class="line">变量名=变量值</span><br><span class="line"></span><br><span class="line"># 取变量值</span><br><span class="line">$(变量名)</span><br></pre></td></tr></table></figure>

<h2 id="预定义变量"><a href="#预定义变量" class="headerlink" title="预定义变量"></a>预定义变量</h2><table>
<thead>
<tr>
<th align="center">变量名</th>
<th align="center">含义</th>
<th align="center">默认值</th>
</tr>
</thead>
<tbody><tr>
<td align="center">AR</td>
<td align="center">生成静态库程序</td>
<td align="center">ar</td>
</tr>
<tr>
<td align="center">AS</td>
<td align="center">汇编编译器</td>
<td align="center">as</td>
</tr>
<tr>
<td align="center">CC</td>
<td align="center">C语言编译器</td>
<td align="center">cc</td>
</tr>
<tr>
<td align="center">CPP</td>
<td align="center">C语言预编译器</td>
<td align="center">$(CC) -E</td>
</tr>
<tr>
<td align="center">CXX</td>
<td align="center">C++编译器</td>
<td align="center">g++</td>
</tr>
<tr>
<td align="center">RM</td>
<td align="center">删除文件程序</td>
<td align="center">rm -f</td>
</tr>
<tr>
<td align="center">ARFLAGS</td>
<td align="center">生成静态库程序的选项</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center">ASFLAGS</td>
<td align="center">汇编语言编译器编译选项</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center">CFLAGS</td>
<td align="center">C语言编译器的编译选项</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center">CPPFLAGS</td>
<td align="center">C语言预编译器的编译选项</td>
<td align="center">无</td>
</tr>
<tr>
<td align="center">CXXFLAGS</td>
<td align="center">C++编译器的编译选项</td>
<td align="center">无</td>
</tr>
</tbody></table>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 示例：</span></span><br><span class="line">obj=main.o sub.o add.o</span><br><span class="line">target=calc</span><br><span class="line">CFLAGS=-O3</span><br><span class="line"><span class="variable">$(target)</span>:<span class="variable">$(obj)</span></span><br><span class="line">    <span class="variable">$(CC)</span> <span class="variable">$(obj)</span> -o <span class="variable">$(target)</span> <span class="variable">$(CFLAGS)</span></span><br></pre></td></tr></table></figure>

<h2 id="自动变量"><a href="#自动变量" class="headerlink" title="自动变量"></a>自动变量</h2><table>
<thead>
<tr>
<th align="center">变量</th>
<th align="center">含义</th>
</tr>
</thead>
<tbody><tr>
<td align="center">$@</td>
<td align="center">目标文件(target)的完整名称</td>
</tr>
<tr>
<td align="center">$&lt;</td>
<td align="center">第一个依赖文件的名称</td>
</tr>
<tr>
<td align="center">$^</td>
<td align="center">所有依赖文件的名称，以空格分开，不包含重复依赖</td>
</tr>
<tr>
<td align="center">$*</td>
<td align="center">目标文件的名称，不包含扩展名</td>
</tr>
<tr>
<td align="center">$?</td>
<td align="center">依赖项中，所有比目标文件时间戳晚的依赖文件，以空格分开</td>
</tr>
</tbody></table>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 示例：</span></span><br><span class="line"><span class="variable">$(target)</span>:<span class="variable">$(obj)</span></span><br><span class="line">    gcc <span class="variable">$^</span> -o <span class="variable">$@</span></span><br></pre></td></tr></table></figure>

<h1 id="模式匹配"><a href="#模式匹配" class="headerlink" title="模式匹配"></a>模式匹配</h1><p>使用模式匹配前：</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">calc:add.o  div.o  main.o  mult.o  sub.o</span></span><br><span class="line">        gcc  add.o  div.o  main.o  mult.o  sub.o -o calc</span><br><span class="line"></span><br><span class="line"><span class="section">add.o:add.c</span></span><br><span class="line">        gcc add.c -c</span><br><span class="line"></span><br><span class="line"><span class="section">div.o:div.c</span></span><br><span class="line">        gcc div.c -c</span><br><span class="line"></span><br><span class="line"><span class="section">main.o:main.c</span></span><br><span class="line">        gcc main.c -c</span><br><span class="line"></span><br><span class="line"><span class="section">sub.o:sub.c</span></span><br><span class="line">        gcc sub.c -c</span><br><span class="line"></span><br><span class="line"><span class="section">mult.o:mult.c</span></span><br><span class="line">        gcc mult.c -c</span><br></pre></td></tr></table></figure>

<p>使用模式匹配后：</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">calc:add.o  div.o  main.o  mult.o  sub.o</span></span><br><span class="line">        gcc  add.o  div.o  main.o  mult.o  sub.o -o calc</span><br><span class="line"></span><br><span class="line"><span class="section">%.o:%.c</span></span><br><span class="line">	gcc <span class="variable">$&lt;</span> -c</span><br></pre></td></tr></table></figure>

<h1 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h1><h2 id="wildcard"><a href="#wildcard" class="headerlink" title="wildcard"></a>wildcard</h2><p>函数功能:获取指定目录下指定类型的文件名，其返回值是以空格分割的指定目录下符合条件的文件名列表。<br>.<br>├── include<br>│   └── head.h<br>├── main.c<br>├── Makefile<br>└── src<br>    ├── add.c<br>    ├── div.c<br>    ├── mult.c<br>    └── sub.c</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 语法：</span></span><br><span class="line"><span class="variable">$(<span class="built_in">wildcard</span> 目录列表)</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例：</span></span><br><span class="line">src=<span class="variable">$(<span class="built_in">wildcard</span> *.c ./src/*.c)</span></span><br></pre></td></tr></table></figure>

<h2 id="patsubst"><a href="#patsubst" class="headerlink" title="patsubst"></a>patsubst</h2><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 语法：</span></span><br><span class="line"><span class="variable">$(<span class="built_in">patsubst</span> &lt;pattern&gt;, &lt;replacement&gt;, &lt;text&gt;)</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例：将变量src中的所有文件名的后缀替换成.o</span></span><br><span class="line">obj=<span class="variable">$(<span class="built_in">patsubst</span> %.c, %.o, <span class="variable">$(src)</span>)</span></span><br></pre></td></tr></table></figure>

<h1 id="makefile示例"><a href="#makefile示例" class="headerlink" title="makefile示例"></a>makefile示例</h1><p>.<br>├── include<br>│   └── head.h<br>├── main.c<br>├── Makefile<br>└── src<br>    ├── add.c<br>    ├── div.c<br>    ├── mult.c<br>    └── sub.c</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义变量</span></span><br><span class="line">target=main</span><br><span class="line">include=./<span class="keyword">include</span></span><br><span class="line">CFLAGS=-O3 -g -Wall</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搜索源文件</span></span><br><span class="line">src=<span class="variable">$(<span class="built_in">wildcard</span> *.c ./src/*.c)</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 替换成.o结尾</span></span><br><span class="line">obj=<span class="variable">$(<span class="built_in">patsubst</span> %.c, %.o, <span class="variable">$(src)</span>)</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 对汇编文件进行链接</span></span><br><span class="line"><span class="variable">$(target)</span>:<span class="variable">$(obj)</span></span><br><span class="line">   gcc <span class="variable">$^</span> -o <span class="variable">$@</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 对源文件进行逐个汇编</span></span><br><span class="line"><span class="section">%.o:%.c</span></span><br><span class="line">   gcc <span class="variable">$&lt;</span> -c -I<span class="variable">$(include)</span> -o <span class="variable">$@</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># clean</span></span><br><span class="line"><span class="meta"><span class="keyword">.PHONY</span>: clean</span></span><br><span class="line"><span class="section">clean:</span></span><br><span class="line">   rm <span class="variable">$(obj)</span> <span class="variable">$(target)</span> -f</span><br></pre></td></tr></table></figure>

<h1 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h1><h2 id="递归扩展变量（-）"><a href="#递归扩展变量（-）" class="headerlink" title="递归扩展变量（&#x3D;）"></a>递归扩展变量（&#x3D;）</h2><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="meta"><span class="keyword">.PHONY</span>: all</span></span><br><span class="line">foo = <span class="variable">$(bar)</span></span><br><span class="line">bar = <span class="variable">$(hello)</span></span><br><span class="line">hello = world</span><br><span class="line"></span><br><span class="line"><span class="section">all:</span></span><br><span class="line">    @echo <span class="variable">$(foo)</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ make</span><br><span class="line">world</span><br></pre></td></tr></table></figure>

<h2 id="简单扩展变量（-）"><a href="#简单扩展变量（-）" class="headerlink" title="简单扩展变量（:&#x3D;）"></a>简单扩展变量（:&#x3D;）</h2><p>对于这种变量，make只对其进行一次扫描和扩展。</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="meta"><span class="keyword">.PHONY</span>: all</span></span><br><span class="line">x = foo</span><br><span class="line">y = <span class="variable">$(x)</span> b</span><br><span class="line">x = later</span><br><span class="line"></span><br><span class="line">xx := foo</span><br><span class="line">yy := <span class="variable">$(xx)</span> b</span><br><span class="line">xx := later</span><br><span class="line"></span><br><span class="line"><span class="section">all:</span></span><br><span class="line">    $echo <span class="string">&quot;x = <span class="variable">$(y)</span>, xx = <span class="variable">$(yy)</span>&quot;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ make</span><br><span class="line">x = later b, xx = foo b</span><br></pre></td></tr></table></figure>

<h1 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h1><p><a href="https://subingwen.cn/linux/makefile/">爱编程的大丙</a><br><a href="https://www.cnblogs.com/paul-617/p/15501875.html">makefile详解</a></p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>进程抽象</title>
    <url>/2024/09/02/unit4-%E8%BF%9B%E7%A8%8B%E6%8A%BD%E8%B1%A1/</url>
    <content><![CDATA[<h1 id="unit4-进程抽象"><a href="#unit4-进程抽象" class="headerlink" title="unit4:进程抽象"></a>unit4:进程抽象</h1><h2 id="1-进程API内容"><a href="#1-进程API内容" class="headerlink" title="1.进程API内容"></a>1.进程API内容</h2><ul>
<li>创建（create）</li>
<li>销毁（destroy）</li>
<li>等待（wait）</li>
<li>其他控制（miscellaneous control）</li>
<li>状态（statu）<span id="more"></span></li>
</ul>
<h2 id="2-进程创建"><a href="#2-进程创建" class="headerlink" title="2.进程创建"></a>2.进程创建</h2><p>1.操作系统运行程序的<strong>第一件事</strong>是将代码和所有的静态数据加载（load）到内存中，加载到进程的地址空间中。<br>2.为程序的运行时栈，分配一些内存。C程序使用栈存放局部变量，函数参数，返回值。操作系统分配这些内存，并提供给进程。<br>3.操作系统也可能为程序的堆分配一些内存。<br>4.操作系统还将执行一些其他初始化任务，比如输入\输出相关的任务。</p>
<h2 id="3-进程的状态"><a href="#3-进程的状态" class="headerlink" title="3.进程的状态"></a>3.进程的状态</h2><ul>
<li>运行：该进程正在执行指令<br> 1.运行态不会一直持续，进程的CPU时间片用完之后，再次失去CPU，从运行态转为就绪态。</li>
<li>就绪：进程准备好运行，但是操作系统在运行其他进程<br> 1.进程被创建出来了，有运行的资格，但是还没有运行，需要抢占CPU时间片；<br> 2.得到CPU时间片，进程开始运行，从就绪态转换为运行态；<br> 3.进程的CPU时间片用完之后，再次失去CPU，从运行态转为就绪态。</li>
<li>阻塞：进程被强制放弃CPU，并且没有抢占CPU时间片的资格<br> 1.比如当进程向磁盘发起I&#x2F;O请求<br> 2.当阻塞任务完成，进程从阻塞态转换为就绪态</li>
</ul>
<h2 id="4-进程API"><a href="#4-进程API" class="headerlink" title="4.进程API"></a>4.进程API</h2><p>1.fork()系统调用</p>
<blockquote>
<p>用于创建新进程，创建出来的进程被称为子进程，子进程与父进程几乎一样。子进程不会从main函数开始执行，而是直接从fork系统调用中返回。<br>子进程拥有自己的地址空间（即私有内存），寄存器，程序计数器等，但是它从fork返回的值是不同的。父进程获得的返回值是新创建子进程的PID，子进程获得的返回值是0.</p>
</blockquote>
<p>子进程和父进程的<strong>相同点</strong>：<br>1.拷贝完成后，两个地址空间的用户区数据是相同的。用户数据主要包括：</p>
<ul>
<li>代码区</li>
<li>全局数据区</li>
<li>堆区</li>
<li>动态库加载区（内存映射区）</li>
<li>栈区</li>
<li>环境变量</li>
<li>文件描述符：父进程中被分配的文件描述符都会被拷贝到子进程中，在子进程中可以使用他们打开对应的文件。</li>
</ul>
<p>子进程和父进程的<strong>区别</strong>：<br>1.父子进程各自的虚拟地址是相互独立的，不会互相影响和干扰。<br>2.父子进程的代码区虽然相同，但是执行的代码逻辑可能是不同的。一些情况下会用if判断父子进程，分别执行不同的代码块。<br>3.内核区存储的父子进程ID是不同的。<br>4.fork调用成功之后，会返回两个值，父子进程的返回值是不同的。</p>
]]></content>
  </entry>
  <entry>
    <title>光机接口测试</title>
    <url>/2024/10/31/%E5%85%89%E6%9C%BA-%E6%8E%A5%E5%8F%A3%E6%B5%8B%E8%AF%95/</url>
    <content><![CDATA[<p>DLPC-API-1.10给的API比较杂，有很多也用不上，需要自己另外定义接口，官方给的有一个示例程序，程序里封装了一部分接口，先测试这些接口能不能用，然后在这个基础上增加需要的接口，实例程序中的包括：</p>
<span id="more"></span>
<ul>
<li>I2C 写入</li>
<li>I2C 读取</li>
<li>初始化命令层</li>
<li>以秒为单位等待指定的时间</li>
<li>生成一个1位（二维）图案</li>
<li>生成一个8位（灰度）图案</li>
<li>填充数组<code>s_PatternSets 中的 DLPC34XX_INT_PAT_PatternSet_s</code>结构</li>
<li>填充数组<code>s_PatternOrderTable 中的 DLPC34XX_INT_PAT_PatternOrderTableEntry_s</code>结构</li>
<li>将数据复制到 Flash 编程缓冲区以便后续写入</li>
<li>将缓冲区数据编程到 Flash 中</li>
<li>将数据写入文件</li>
<li>生成图案数据并写入到文件</li>
<li>缓存图案数据并编程到 Flash 中</li>
<li>生成和编程图案数据到 Flash 中</li>
<li>从 Flash 中加载图案顺序表条目</li>
<li>加载特定的图案顺序表条目</li>
<li>写入测试图案网格线</li>
<li>写入测试图案色条</li>
<li>写入选定视图外观（Look）</li>
<li>加载预构建的模式数据</li>
<li>写入 LABB 和 CAIC 设置</li>
<li>加载固件</li>
</ul>
<h1 id="过程记录"><a href="#过程记录" class="headerlink" title="过程记录"></a>过程记录</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">unsigned int WriteI2C(uint16_t WriteDataLength, uint8_t *WriteData, DLPC_COMMON_CommandProtocolData_s *ProtocolData) &#123;</span><br><span class="line">    // 实现写入 I2C 设备的逻辑</span><br><span class="line">    // ...</span><br><span class="line">    return SUCCESS; // 或者在出错时返回错误代码</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 假设您有一个函数接受一个回调函数指针</span><br><span class="line">void ExecuteWriteOperation(DLPC_COMMON_WriteCommandCallback callback) &#123;</span><br><span class="line">    // ...</span><br><span class="line">    unsigned int result = callback(WriteDataLength, WriteData, &amp;ProtocolData);</span><br><span class="line">    // ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 在调用 ExecuteWriteOperation 时，您可以传递 WriteI2C 函数</span><br><span class="line">ExecuteWriteOperation(WriteI2C);</span><br></pre></td></tr></table></figure>
<p>在这个例子中，ExecuteWriteOperation 函数接受一个类型为 DLPC_COMMON_WriteCommandCallback 的回调函数，并在内部调用它。这样，您可以将不同的写入操作作为回调传递给 ExecuteWriteOperation，而不需要修改其内部实现。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(gdb) ptype DLPC_COMMON_WriteCommandCallback</span><br><span class="line">type = unsigned int (*)(uint16_t, uint8_t *, DLPC_COMMON_CommandProtocolData_s *)</span><br></pre></td></tr></table></figure>
<ul>
<li>函数指针：定义回调函数，可以在不同的上下文中被调用。抽象特定的行为，使得代码更加模块化和可重用。</li>
<li>返回类型：unsigned int</li>
<li>参数列表：<ul>
<li>uint16_t：无符号的16位整数</li>
<li>uint8_t *：指向无符号8位的整数的指针参数，数据缓冲区</li>
<li>DLPC_COMMON_CommandProtocolData_s *：指向结构体的指针，协议相关</li>
</ul>
</li>
</ul>
<p>当光机接入或者没有接入USB的时候均出现：Status &#x3D; CY_ERROR_REQUEST_FAILED, NumDevices &#x3D; 0 ‘\000’</p>
<ul>
<li>CY_ERROR_REQUEST_FAILED：发送到 USB Serial 设备的请求失败</li>
<li>表明函数 CyGetListofDevices 没有成功地找到任何连接的设备</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">zhm@Ubuntu:~/Jetson/StructLight/CyUSBSerial_SDK_Linux/linux/library$ lsusb</span><br><span class="line">Bus 001 Device 001: ID 1d6b:0002 Linux Foundation 2.0 root hub</span><br><span class="line">Bus 001 Device 002: ID 0bda:8771 Realtek Semiconductor Corp. Bluetooth Radio</span><br><span class="line">Bus 001 Device 003: ID 0000:3825   USB OPTICAL MOUSE</span><br><span class="line">Bus 001 Device 004: ID 1c4f:0026 SiGma Micro Keyboard</span><br><span class="line">Bus 001 Device 005: ID 05e3:0608 Genesys Logic, Inc. Hub</span><br><span class="line">Bus 001 Device 018: ID 04b4:000a Cypress Semiconductor Corp. USB-Serial (Dual Channel)</span><br><span class="line">Bus 002 Device 001: ID 1d6b:0003 Linux Foundation 3.0 root hub</span><br></pre></td></tr></table></figure>
<p>说明Cypress USB-Serial设备已经被系统识别，并且分配了设备ID 04b4:000a</p>
<p>驱动程序：libusb-1.0库（需要测试它能不能用）<br>经过测试，是可以用的。</p>
<p>使用sudo apt-get install <package>的时候，出现无法命中等问题，换源之后问题依然没有解决，可以尝试使用下面的方法。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1. sudo vim /etc/resolv.conf # 添加如下内容</span><br><span class="line">nameserver 8.8.8.8</span><br><span class="line">nameserver 8.8.4.4</span><br><span class="line">nameserver 127.0.0.1</span><br><span class="line"></span><br><span class="line">2. 输入Esc，:wq，保存并退出</span><br></pre></td></tr></table></figure>

<h1 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h1><p><code>InitConnectionAndCommandLayer();</code>出现错误，函数内部为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 初始化命令层，设置读/写缓冲区和回调函数。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">InitConnectionAndCommandLayer</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 初始化命令库，设置读/写缓冲区和对应的 I2C 读写函数</span></span><br><span class="line">    DLPC_COMMON_InitCommandLibrary(s_WriteBuffer,</span><br><span class="line">                                   <span class="keyword">sizeof</span>(s_WriteBuffer),</span><br><span class="line">                                   s_ReadBuffer,</span><br><span class="line">                                   <span class="keyword">sizeof</span>(s_ReadBuffer),</span><br><span class="line">                                   WriteI2C,</span><br><span class="line">                                   ReadI2C);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 连接到 Cypress I2C</span></span><br><span class="line">    CYPRESS_I2C_ConnectToCyI2C();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>CYPRESS_I2C_ConnectToCyI2C();</code>函数内部为，添加<code>printf</code>定位问题出现位置：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> <span class="title function_">CYPRESS_I2C_ConnectToCyI2C</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    CY_RETURN_STATUS Status;	</span><br><span class="line">    CY_I2C_CONFIG I2CConfig;</span><br><span class="line">    <span class="keyword">if</span> (GetCyI2CHandle(&amp;s_Handle) == <span class="literal">false</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Get CyI2C Handle Failed!!! \n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    I2CConfig.frequency      = I2C_CLOCK_FREQUENCY_HZ;</span><br><span class="line">    I2CConfig.slaveAddress   = <span class="number">0x30</span>;</span><br><span class="line">    I2CConfig.isMaster       = <span class="literal">true</span>;</span><br><span class="line">    I2CConfig.isClockStretch = <span class="literal">false</span>;</span><br><span class="line">    </span><br><span class="line">    Status = CySetI2cConfig(s_Handle, &amp;I2CConfig);</span><br><span class="line">    <span class="keyword">if</span> (Status != CY_SUCCESS)</span><br><span class="line">    &#123;</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">&quot;Connect to I2C Error %d!!! \n&quot;</span>, Status);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    s_DataConfig.isNakBit     = <span class="literal">true</span>;</span><br><span class="line">    s_DataConfig.isStopBit    = <span class="literal">true</span>;</span><br><span class="line">    s_DataConfig.slaveAddress = DLP_I2C_SLAVE_ADDRESS;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>GetCyI2CHandle(&amp;s_Handle)</code>这里出现错误，添加<code>printf</code>定位问题，函数内部为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Gets the handle of the connected Cypress USB-Serial bridge controller */</span></span><br><span class="line"><span class="type">bool</span> <span class="title function_">GetCyI2CHandle</span><span class="params">(CY_HANDLE* Handle)</span></span><br><span class="line">&#123;</span><br><span class="line">    CY_RETURN_STATUS Status;</span><br><span class="line">    CY_DEVICE_INFO   DeviceInfo;</span><br><span class="line">    <span class="type">uint8_t</span>          NumDevices = <span class="number">0</span>;</span><br><span class="line">    <span class="type">uint8_t</span>          DeviceIdx;</span><br><span class="line">    <span class="type">uint8_t</span>          InterfaceIdx;</span><br><span class="line"></span><br><span class="line">    Status = CyGetListofDevices(&amp;NumDevices);</span><br><span class="line">    <span class="keyword">if</span> ((Status != CY_SUCCESS) || (NumDevices == <span class="number">0</span>))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Status: %d\n&quot;</span>, Status);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;NumDevices: %d\n&quot;</span>, NumDevices);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;无设备连接成功,没有设备连接!!!\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (DeviceIdx = <span class="number">0</span>; DeviceIdx &lt; NumDevices; DeviceIdx++)</span><br><span class="line">    &#123;</span><br><span class="line">        Status = CyGetDeviceInfo(DeviceIdx, &amp;DeviceInfo);</span><br><span class="line">        <span class="keyword">if</span> (Status != CY_SUCCESS)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (InterfaceIdx = <span class="number">0</span>; InterfaceIdx &lt; DeviceInfo.numInterfaces; InterfaceIdx++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (DeviceInfo.deviceType[InterfaceIdx] == CY_TYPE_I2C)</span><br><span class="line">            &#123;</span><br><span class="line">                Status = CyOpen(DeviceIdx, InterfaceIdx, Handle);</span><br><span class="line">                <span class="keyword">if</span> (Status == CY_SUCCESS)</span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;Get I2C Handle Error %d!!! \n&quot;</span>, Status);</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用gdb定位到<code>Status = CyGetListofDevices(&amp;NumDevices);</code>，Status输出为<code>CY_ERROR_REQUEST_FAILED</code>，定位到<code>CyGetListofDevices(&amp;NumDevices);</code>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">   Summary</span></span><br><span class="line"><span class="comment">   This API retrieves number of USB devices connected to the host.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">   Description</span></span><br><span class="line"><span class="comment">   This API retrieves the number of devices connected to the host.</span></span><br><span class="line"><span class="comment">   In Windows family of operating systems the API retrieves only the number of devices that are attached</span></span><br><span class="line"><span class="comment">   to CyUSB3.SYS driver. For other operating systems, it retrieves the total number of USB devices present</span></span><br><span class="line"><span class="comment">   on the bus. It includes both USB Serial device as well as other devices.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">   Note: In case of Linux and Mac apart from providing number of devices connected, it builds the</span></span><br><span class="line"><span class="comment">   device list which is used for opening the device and obtaining device handle. Thus the API should be</span></span><br><span class="line"><span class="comment">   called during device discovery in the application.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">   Return Value</span></span><br><span class="line"><span class="comment">   * CY_SUCCESS on success </span></span><br><span class="line"><span class="comment">   * CY_ERROR_DEVICE_NOT_FOUND if there are no devices attached.</span></span><br><span class="line"><span class="comment">   * CY_ERROR_REQUEST_FAILED if library was not initialized.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">   See Also</span></span><br><span class="line"><span class="comment">   * CyGetDeviceInfo</span></span><br><span class="line"><span class="comment">   * CyGetDeviceInfoVidPid</span></span><br><span class="line"><span class="comment">   * CyOpen</span></span><br><span class="line"><span class="comment">   * CyClose</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">CYWINEXPORT CY_RETURN_STATUS <span class="title function_">CyGetListofDevices</span> <span class="params">(</span></span><br><span class="line"><span class="params">    UINT8* numDevices                            <span class="comment">/*Number of Devices connected*/</span></span></span><br><span class="line"><span class="params">    )</span>;</span><br></pre></td></tr></table></figure>
<p>定位到这里<code>CY_ERROR_REQUEST_FAILED if library was not initialized.</code>出现的原因是，因为library没有初始化。修改<code>GetCyI2CHandle(CY_HANDLE* Handle)</code>代码，添加初始化函数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> <span class="title function_">GetCyI2CHandle</span><span class="params">(CY_HANDLE* Handle)</span></span><br><span class="line">&#123;</span><br><span class="line">    CY_RETURN_STATUS Status;</span><br><span class="line">    CY_DEVICE_INFO   DeviceInfo;</span><br><span class="line">    <span class="type">uint8_t</span>          NumDevices = <span class="number">0</span>;</span><br><span class="line">    <span class="type">uint8_t</span>          DeviceIdx;</span><br><span class="line">    <span class="type">uint8_t</span>          InterfaceIdx;</span><br><span class="line"></span><br><span class="line">    Status = CyLibraryInit();</span><br><span class="line">    <span class="keyword">if</span> (Status != CY_SUCCESS) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d 设备初始化失败!!!\n&quot;</span>, Status);</span><br><span class="line">    &#125;</span><br><span class="line">    Status = CyGetListofDevices(&amp;NumDevices);</span><br><span class="line">    <span class="keyword">if</span> ((Status != CY_SUCCESS) || (NumDevices == <span class="number">0</span>))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Status: %d\n&quot;</span>, Status);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;NumDevices: %d\n&quot;</span>, NumDevices);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;无设备连接成功,没有设备连接!!!\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (DeviceIdx = <span class="number">0</span>; DeviceIdx &lt; NumDevices; DeviceIdx++)</span><br><span class="line">    &#123;</span><br><span class="line">        Status = CyGetDeviceInfo(DeviceIdx, &amp;DeviceInfo);</span><br><span class="line">        <span class="keyword">if</span> (Status != CY_SUCCESS)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (InterfaceIdx = <span class="number">0</span>; InterfaceIdx &lt; DeviceInfo.numInterfaces; InterfaceIdx++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (DeviceInfo.deviceType[InterfaceIdx] == CY_TYPE_I2C)</span><br><span class="line">            &#123;</span><br><span class="line">                Status = CyOpen(DeviceIdx, InterfaceIdx, Handle);</span><br><span class="line">                <span class="keyword">if</span> (Status == CY_SUCCESS)</span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;Get I2C Handle Error %d!!! \n&quot;</span>, Status);</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>问题解决。</p>
<p>总结：<br>1、首先是单独测试USB驱动程序能不能用，写了一个单独测试USB的程序，可以正常运行。<br>2、检查PC机有没有连接到光机设备，可以使用<code>lsusb</code>指令。光机是连接到PC机上的。<br>3、检查程序有没有问题。<code>printf</code>和<code>gdb</code>定位问题。</p>
]]></content>
      <categories>
        <category>嵌入式结构光三维重建项目</category>
      </categories>
  </entry>
  <entry>
    <title>压缩解压</title>
    <url>/2024/09/20/%E5%8E%8B%E7%BC%A9_%E8%A7%A3%E5%8E%8B/</url>
    <content><![CDATA[<ul>
<li>.tar.gz</li>
<li>.tgz</li>
<li>.tar.bz2</li>
<li>.zip</li>
<li>.rar</li>
<li>.tar.xz</li>
</ul>
<span id="more"></span>
<h1 id="tar"><a href="#tar" class="headerlink" title="tar"></a>tar</h1><p>Linux默认情况下，自带两个压缩工具<code>gzip</code>,<code>bzip2</code>和一个打包工具<code>tar</code>。打包工具只能进行打包，不能压缩，压缩工具只能进行压缩，不能进行打包。</p>
<h2 id="1-压缩（-tar-gz-tar-bz2-tgz）"><a href="#1-压缩（-tar-gz-tar-bz2-tgz）" class="headerlink" title="1.压缩（.tar.gz|.tar.bz2|.tgz）"></a>1.压缩（.tar.gz|.tar.bz2|.tgz）</h2><ul>
<li>c：创建压缩文件</li>
<li>z：使用<code>gzip</code>的方式进行压缩</li>
<li>j：使用<code>bzip2</code>的方式进行压缩</li>
<li>v：压缩过程中显示压缩过程</li>
<li>f：指定压缩包的名字</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用 gzip 的方式压缩:</span></span><br><span class="line">$ tar zcvf name.tar.gz file1 file2</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用 bzip2 的方式压缩：</span></span><br><span class="line">$ tar jcvf name.tar.bz2 file1 file2</span><br></pre></td></tr></table></figure>

<h2 id="2-解压缩（-tar-gz-tar-bz2-tgz）"><a href="#2-解压缩（-tar-gz-tar-bz2-tgz）" class="headerlink" title="2.解压缩（.tar.gz|.tar.bz2|.tgz）"></a>2.解压缩（.tar.gz|.tar.bz2|.tgz）</h2><ul>
<li>x：释放压缩文件内容</li>
<li>z：使用<code>gzip</code>的方式进行压缩</li>
<li>j：使用<code>bzip2</code>的方式进行压缩</li>
<li>v：压缩过程中显示压缩过程</li>
<li>f：指定压缩包的名字</li>
</ul>
<blockquote>
<p>如果需要解压到指定目录，需要指定参数<code>-C</code>，一般认为<code>.tgz</code>文件等同于<code>.tar.gz</code>文件，解压方式相同。</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用`gzip`的方式进行解压缩</span></span><br><span class="line">$ tar zxvf name.tar.gz -C &lt;dir-name&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用`bzip2`的方式进行解压缩</span></span><br><span class="line">$ tar jxvf name.tar.bz2 -C &lt;dir-name&gt;</span><br></pre></td></tr></table></figure>

<h1 id="zip"><a href="#zip" class="headerlink" title="zip"></a>zip</h1><h2 id="1-安装"><a href="#1-安装" class="headerlink" title="1.安装"></a>1.安装</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">sudo</span> apt install zip</span><br><span class="line">$ <span class="built_in">sudo</span> apt install unzip</span><br></pre></td></tr></table></figure>

<h2 id="2-压缩和解压缩（-zip）"><a href="#2-压缩和解压缩（-zip）" class="headerlink" title="2.压缩和解压缩（.zip）"></a>2.压缩和解压缩（.zip）</h2><p>使用<code>zip</code>压缩目录的时候，需要添加参数<code>-r</code>，才能将子目录中的文件一并压缩。压缩文件会自动生成后缀，因此不需要额外指定。解压缩时，如果需要指定解压目录需要添加参数<code>-d</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 压缩</span></span><br><span class="line">$ zip zipname file1 file2 dir1 -r</span><br><span class="line"></span><br><span class="line"><span class="comment"># 解压缩</span></span><br><span class="line">$ unzip zipname -d &lt;dir-name&gt;</span><br></pre></td></tr></table></figure>

<h1 id="rar"><a href="#rar" class="headerlink" title="rar"></a>rar</h1><h2 id="1-安装-1"><a href="#1-安装-1" class="headerlink" title="1.安装"></a>1.安装</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">sudo</span> apt install rar</span><br></pre></td></tr></table></figure>

<h2 id="2-压缩和解压缩（-rar）"><a href="#2-压缩和解压缩（-rar）" class="headerlink" title="2.压缩和解压缩（.rar）"></a>2.压缩和解压缩（.rar）</h2><p>如果压缩的文件带有目录，需要指定参数<code>-r</code>，压缩时需要使用参数<code>a（archive）</code>压缩归档的意思。压缩包也会自动添加后缀。解压缩时可以使用<code>rar</code>，也可以使用<code>unrar</code>，解压缩时需要指定参数<code>x</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 压缩</span></span><br><span class="line">$ rar a rarname file1 file2 dir1 -r</span><br><span class="line"></span><br><span class="line"><span class="comment"># 解压缩</span></span><br><span class="line">$ rar x rarname &lt;dir-name&gt;</span><br></pre></td></tr></table></figure>

<h1 id="xz"><a href="#xz" class="headerlink" title="xz"></a>xz</h1><h2 id="1-压缩"><a href="#1-压缩" class="headerlink" title="1.压缩"></a>1.压缩</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 第一步：打包文件</span></span><br><span class="line">$ tar cvf name.tar file1 file2</span><br><span class="line"></span><br><span class="line"><span class="comment"># 第二步：使用xz压缩文件(生成文件带后缀xxx.tar.xz)</span></span><br><span class="line">$ xz -z name.tar</span><br></pre></td></tr></table></figure>

<h2 id="解压缩"><a href="#解压缩" class="headerlink" title="解压缩"></a>解压缩</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用xz解压缩得到xxx.tar</span></span><br><span class="line">$ xz -d xxx.tar.gz</span><br><span class="line"></span><br><span class="line"><span class="comment"># 将xxx.tar中的文件释放到当前目录</span></span><br><span class="line">$ tar xvf xxx.tar</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>查找命令</title>
    <url>/2024/09/20/%E6%9F%A5%E6%89%BE%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<ul>
<li>find：用于搜索文件，通过属性搜索文件</li>
<li>grep：用于搜索文件内容，正则表达式</li>
<li>locate：根据文件名搜索本地的磁盘文件</li>
</ul>
<span id="more"></span>
<h1 id="find"><a href="#find" class="headerlink" title="find"></a>find</h1><h2 id="1-根据文件名查找（-name）"><a href="#1-根据文件名查找（-name）" class="headerlink" title="1.根据文件名查找（-name）"></a>1.根据文件名查找（-name）</h2><p>常用通配符：</p>
<ul>
<li>*：可以匹配0个或多个字符</li>
<li>？：用于匹配单个字符</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查找当前路径下所有的.cpp文件</span></span><br><span class="line">find . -name <span class="string">&quot;*.cpp&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="2-根据文件类型查找（-type）"><a href="#2-根据文件类型查找（-type）" class="headerlink" title="2.根据文件类型查找（-type）"></a>2.根据文件类型查找（-type）</h2><table>
<thead>
<tr>
<th align="center">文件类型</th>
<th align="center">字符描述</th>
</tr>
</thead>
<tbody><tr>
<td align="center">普通文件</td>
<td align="center">f</td>
</tr>
<tr>
<td align="center">目录文件</td>
<td align="center">d</td>
</tr>
<tr>
<td align="center">软链接文件</td>
<td align="center">l</td>
</tr>
<tr>
<td align="center">字符设备文件</td>
<td align="center">c</td>
</tr>
<tr>
<td align="center">块设备文件</td>
<td align="center">b</td>
</tr>
<tr>
<td align="center">管道文件</td>
<td align="center">p</td>
</tr>
<tr>
<td align="center">本地套接字文件</td>
<td align="center">s</td>
</tr>
</tbody></table>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查找当前路径下所有的文件夹</span></span><br><span class="line">$ find . -<span class="built_in">type</span> d</span><br></pre></td></tr></table></figure>

<h2 id="3-根据文件大小查找（-size）"><a href="#3-根据文件大小查找（-size）" class="headerlink" title="3.根据文件大小查找（-size）"></a>3.根据文件大小查找（-size）</h2><ul>
<li>-size 4k 表示区间(3k, 4k]</li>
<li>-size -4k 表示区间[0k, 4k]</li>
<li>-size +4k 表示区间(4k, 正无穷)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查找当前目录下 大于1M并且小于等于3M的文件</span></span><br><span class="line">$ find . -size +1M -size -3M</span><br></pre></td></tr></table></figure>

<h2 id="4-限制查找的目录层级（-maxdepth-mindepth）"><a href="#4-限制查找的目录层级（-maxdepth-mindepth）" class="headerlink" title="4.限制查找的目录层级（-maxdepth, -mindepth）"></a>4.限制查找的目录层级（-maxdepth, -mindepth）</h2><ul>
<li>-maxdepth：最多搜索到第多少层目录</li>
<li>-mindepth：最少搜索到第多少层目录</li>
</ul>
<p>这两个参数不能单独使用。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 从根目录开始，最多搜索五层，搜索&quot;.cpp&quot;文件</span></span><br><span class="line">$ find / -maxdepth 5 -name <span class="string">&quot;*.cpp&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="5-同时执行多个操作（-exec-ok）"><a href="#5-同时执行多个操作（-exec-ok）" class="headerlink" title="5.同时执行多个操作（-exec, -ok）"></a>5.同时执行多个操作（-exec, -ok）</h2><p><strong>问题：</strong> find . -name “ *. txt” | grep “a”中find后面可以使用|管道传递给grep，但是不能使用管道传递给ls，例如find . -name “ *.cpp” | ls</p>
<!-- more -->

<p>1.为什么 find . -name “*.txt” | grep “a” 可以工作？</p>
<ul>
<li>find . -name “*.txt” 会搜索当前目录及其子目录下所有扩展名为 .txt 的文件，并将这些文件名输出到标准输出（stdout）。</li>
<li>grep “a” 会从标准输入（stdin）读取数据，然后搜索包含字符串 “a” 的行，并将这些行输出到标准输出。</li>
<li>管道 | 将 find 命令的输出作为 grep 命令的输入，因此 grep 可以接收到文件名并进行搜索。</li>
</ul>
<p>2.为什么 find . -name “*.cpp” | ls 不能工作？</p>
<ul>
<li>ls 命令通常用于列出目录中的文件和目录，而不是处理文件名列表。</li>
<li>find . -name “*.cpp” 会输出一系列文件名，但 ls 命令期望的是目录路径作为参数，而不是文件名列表。</li>
</ul>
<p>可以使用<code>exec</code>,<code>ok</code>,<code>xargs</code>实现find命令执行完毕之后。再执行其他子命令。</p>
<h3 id="exec"><a href="#exec" class="headerlink" title="exec"></a>exec</h3><p><code>-exec</code>是find的参数。可以在exec参数后添加其他需要被执行的shell命令，命令的尾部需要加一个后缀<code>&#123;&#125; \;</code>，<code>&#123;&#125;</code>和<code>\;</code>中间用空格分开。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ find . -name <span class="string">&quot;*.cpp&quot;</span> -<span class="built_in">exec</span> <span class="built_in">ls</span> -l &#123;&#125; \;</span><br></pre></td></tr></table></figure>

<h3 id="ok"><a href="#ok" class="headerlink" title="ok"></a>ok</h3><p><code>-ok</code>也是find命令的参数，是交互式的命令。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ find . -maxdepth 2 -name <span class="string">&quot;*.cpp&quot;</span> -ok <span class="built_in">ls</span> -l &#123;&#125; \;</span><br></pre></td></tr></table></figure>

<h3 id="xargs"><a href="#xargs" class="headerlink" title="xargs"></a>xargs</h3><p>Unix命令，将接收到的输入数据转换为命令行参数，并构建一个完整的命令。通常与管道（|）组合使用。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ find . maxdepth 2 -name <span class="string">&quot;*.cpp&quot;</span> | xargs <span class="built_in">ls</span> -l</span><br></pre></td></tr></table></figure>

<h1 id="grep"><a href="#grep" class="headerlink" title="grep"></a>grep</h1><ul>
<li>-r：递归搜索目录中的文件内容</li>
<li>-i：对应要搜索的关键字，忽略字符大小写</li>
<li>-n：在现实符合样式的那一行之前，标出该行的行号</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ grep <span class="string">&quot;main&quot;</span> hello.c -i</span><br><span class="line">$ find . -name <span class="string">&quot;*.cpp&quot;</span> | grep <span class="string">&quot;main&quot;</span> -n</span><br></pre></td></tr></table></figure>

<h1 id="locate"><a href="#locate" class="headerlink" title="locate"></a>locate</h1><p>locate不搜索具体目录，而是搜索本地的数据库文件，这个数据库包含本地的所有信息。在使用locate之前，先使用<code>updatedb</code>命令手动更新数据库。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 更新本地数据库文件</span></span><br><span class="line">$ <span class="built_in">sudo</span> updatedb</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搜索所有目录下以main开头的文件</span></span><br><span class="line">$ locate main</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搜索指定目录下的以某个关键字开头的文件（要用绝对路径）</span></span><br><span class="line">$ locate /home/zhm/Desktop/main</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搜索的时候忽略文件名的大小写（-i）</span></span><br><span class="line">$ locate main -i </span><br><span class="line"></span><br><span class="line"><span class="comment"># 列出前N个匹配到的文件名称</span></span><br><span class="line">$ locate main -n 5</span><br><span class="line"></span><br><span class="line"><span class="comment"># 基于正则表达式进行文件匹配</span></span><br><span class="line"><span class="comment"># 示例：搜索以.cpp结尾的文件</span></span><br><span class="line">$ locate -r <span class="string">&quot;\.cpp$&quot;</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>多线程基础</title>
    <url>/2024/11/07/%E5%A4%9A%E7%BA%BF%E7%A8%8B%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<p>并发</p>
<ol>
<li>多线程</li>
<li>多进程</li>
</ol>
<h1 id="基础概念"><a href="#基础概念" class="headerlink" title="基础概念"></a>基础概念</h1><p>线程是轻量级的进程，在Linux环境下，线程的本质仍然是进程。操作系统会以进程为单位，分配系统资源。进程是资源分配的最小单位，线程是操作系统调度执行的最小单位。</p>
<span id="more"></span>
<p>线程和进程的区别：</p>
<ol>
<li>进程有自己独立的地址空间，多个线程公用同一个地址空间<ul>
<li>在一个地址空间中多个线程共享资源有：代码段，堆区，全局数据区，打开的文件（文件描述符）</li>
<li>在一个地址空间中多个线程独享资源有：栈区，寄存器</li>
</ul>
</li>
<li>线程是程序的最小执行单位，进程是操作系统中资源分配的最小单位<ul>
<li>每个进程对应一个虚拟地址空间，一个进程只能抢一个CPU时间片</li>
<li>一个地址空间中可以划分出多个线程，可以抢占更多的时间片</li>
</ul>
</li>
<li>CPU的调度和切换：线程的上下文切换比进程快的多<ul>
<li>上下文切换：进程和线程分时复用CPU时间片，在切换之前会将上一个任务的状态进行保存，下次切换回这个任务的时候，加载这个状态继续运行任务从保存到再次加载这个过程就是一次上下文切换。</li>
<li>线程更加廉价，启动速度更快，退出也快，对系统资源的冲击小</li>
</ul>
</li>
</ol>
<p>在处理多任务程序的时候，使用多线程比使用多进程更有优势，但是线程并不是越多越好：</p>
<ol>
<li>文件IO操作：文件IO对CPU使用率不高，线程数 &#x3D; 2 * CPU核心数</li>
<li>处理复杂算法：线程数 &#x3D; CPU核心数</li>
</ol>
<p>什么时候用多进程？</p>
<ul>
<li>在处理一个程序的时候，需要去启动另一个程序的时候，使用多进程。</li>
</ul>
<p>并发的话，优先考虑多线程。</p>
<h1 id="线程函数"><a href="#线程函数" class="headerlink" title="线程函数"></a>线程函数</h1><p>头文件 &lt;pthread.h&gt;</p>
<h2 id="线程ID"><a href="#线程ID" class="headerlink" title="线程ID"></a>线程ID</h2><p>每个线程都有一个唯一的线程ID</p>
<ul>
<li>类型：pthread_t</li>
<li>无符号长整形</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 返回当前线程的ID</span></span><br><span class="line"><span class="type">pthread_t</span> <span class="title function_">pthread_self</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure>

<h2 id="创建线程"><a href="#创建线程" class="headerlink" title="创建线程"></a>创建线程</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">pthread_create</span><span class="params">(<span class="type">pthread_t</span> *thread, <span class="type">const</span> <span class="type">pthread_attr_t</span> *attr, <span class="type">void</span>*(*start_routine)(<span class="type">void</span>*), <span class="type">void</span> *arg)</span>;</span><br></pre></td></tr></table></figure>
<ol>
<li>参数<ul>
<li>thread：传出参数，线程创建成功，会将线程ID写入到这个指针指向的内存中</li>
<li>attr：线程属性，一般使用默认属性，写NULL</li>
<li>start_routine：函数指针，子线程的处理动作</li>
<li>arg：作为实参传递到start_routine指向的函数内部</li>
</ul>
</li>
<li>返回值：线程创建成功返回0，创建失败返回对应的错误号</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span>* <span class="title function_">myPrintf</span><span class="params">(<span class="type">void</span> *arg)</span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;子线程id:%ld\n&quot;</span>, pthread_self());</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;子线程中i = %d\n&quot;</span>, i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="type">int</span> pRet = <span class="number">0</span>;</span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;主线程中i = %d\n&quot;</span>, i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;father_pthread_id:%ld\n&quot;</span>, pthread_self());</span><br><span class="line">    pRet = pthread_create(&amp;tid, <span class="literal">NULL</span>, myPrintf, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;son_pthread_id:%ld\n&quot;</span>, tid);</span><br><span class="line">    sleep(<span class="number">1</span>);   <span class="comment">// 延迟一秒（CPU挂起）</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>子线程被创建出来之后需要抢cpu时间片,抢不到就不能运行，如果主线程退出了,虚拟地址空间就被释放了,子线程就一并被销毁了。但是如果某一个子线程退出了,主线程仍在运行,虚拟地址空间依旧存在。在没有人为干预的情况下，虚拟地址空间的生命周期和主线程是一样的，与子线程无关。</p>
<h2 id="线程退出"><a href="#线程退出" class="headerlink" title="线程退出"></a>线程退出</h2><p>调用该函数，当前线程马上退出，并且不会影响到其他线程的正常运行，不管是子线程还是主线程中都可以使用。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">pthread_exit</span><span class="params">(<span class="type">void</span> *retval)</span></span><br></pre></td></tr></table></figure>
<p>参数：</p>
<ul>
<li>线程退出的时候携带的数据，当前子线程的主线程会得到该数据，如果不需要指定为NULL即可。</li>
</ul>
<h2 id="线程回收"><a href="#线程回收" class="headerlink" title="线程回收"></a>线程回收</h2><p>子线程退出的时候，其内核资源由主线程进行回收（非内核资源例如栈，代码区资源会在子线程退出的时候，自动释放）。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">pthread_join</span><span class="params">(<span class="type">pthread_t</span> thread, <span class="type">void</span> **retval)</span>;</span><br></pre></td></tr></table></figure>
<ol>
<li>阻塞函数<ul>
<li>如果还有子线程在运行，调用该函数就会阻塞，子线程退出函数解除阻塞进行资源回收，函数被调用一次，只能回收一个子线程，如果有多个子线程则需要循环进行回收。</li>
</ul>
</li>
<li>参数：<ul>
<li>thread：要回收的子线程的线程ID</li>
<li>retval：二级指针，指向一级指针的地址，是一个传出参数，这个地址中存储了pthread_exit()传递出的数据，如果不需要这个参数，可以指定为NULL</li>
</ul>
</li>
<li>返回值：<ul>
<li>线程回收成功返回0，回收失败返回错误码</li>
</ul>
</li>
<li>用法：<ul>
<li>在子线程退出的时候使用pthread_exit(void* retval)的参数将数据传出，在回收这个子线程的时候通过pthread_join(pthread_t thread, void **retval)的第二个参数来接收子线程传递出的数据。</li>
<li>需要注意的是，在进行数据回收的时候，不能回收子线程栈上的数据，因为如果多个线程共用同一个虚拟地址空间，每个线程在栈区都有一块属于自己的内存，相当于栈区被这几个线程平分了，当线程退出，线程在栈区的内存也就被回收了，因此随着子线程的退出，写入到栈区的数据也就被释放了。</li>
<li>可以使用全局变量</li>
<li>也可以使用主线程栈</li>
</ul>
</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Person</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">int</span> old;</span><br><span class="line">    <span class="type">char</span>* name;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="type">void</span>* <span class="title function_">callback</span><span class="params">(<span class="type">void</span> *arg)</span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;子线程id:%ld\n&quot;</span>, pthread_self());</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;子线程中i = %d\n&quot;</span>, i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Person</span>* <span class="title">p</span> =</span> (<span class="keyword">struct</span> Person*)arg;</span><br><span class="line">    p -&gt; old = <span class="number">10</span>;</span><br><span class="line">    p -&gt; name = <span class="string">&quot;zhang&quot;</span>;</span><br><span class="line">    pthread_exit(arg);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;主线程中i = %d\n&quot;</span>, i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;father_pthread_id:%ld\n&quot;</span>, pthread_self());</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> pRet = <span class="number">0</span>;</span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Person</span> <span class="title">p1</span>;</span>   <span class="comment">// 主线程栈</span></span><br><span class="line">    <span class="type">void</span>* ptr = <span class="literal">NULL</span>;   <span class="comment">// 传回参数</span></span><br><span class="line">    pRet = pthread_create(&amp;tid, <span class="literal">NULL</span>, callback, &amp;p1);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;son_pthread_id:%ld\n&quot;</span>, tid);</span><br><span class="line">    pthread_join(tid, &amp;ptr);</span><br><span class="line">    <span class="comment">// 因为ptr是一个void*类型，如果不想这么写，也可以通过类型转换，将ptr转成Person指针</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;name:%s\n&quot;</span>, p1.name);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;old:%d\n&quot;</span>, p1.old);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="线程分离"><a href="#线程分离" class="headerlink" title="线程分离"></a>线程分离</h2><p>线程分离之后，子线程退出的时候，其占用的内核资源就会被系统的其他进程接管并回收。优势在于，不需要在主线程进行阻塞，等待子线程结束，由主线程回收子线程资源。搭配pthread_exit()函数，可以实现主线程退出，子线程依然有其他线程进行资源的回收。<br>在某些情况下，程序中的主线程也有自己的业务流程，如果想让主线程负责子线程的资源回收，调用pthread_join()函数，子要子线程不退出，主线程就会一直被阻塞，主线程的任务也不能推进。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">pthread_detach</span><span class="params">(<span class="type">pthread_t</span> thread)</span>;</span><br></pre></td></tr></table></figure>
<p>参数：<br>    - thread：要分离的子线程ID</p>
<p>示例代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 子线程的处理代码</span></span><br><span class="line"><span class="type">void</span>* <span class="title function_">working</span><span class="params">(<span class="type">void</span>* arg)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;我是子线程, 线程ID: %ld\n&quot;</span>, pthread_self());</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>; i&lt;<span class="number">9</span>; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;child == i: = %d\n&quot;</span>, i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 1. 创建一个子线程</span></span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line">    pthread_create(&amp;tid, <span class="literal">NULL</span>, working, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;子线程创建成功, 线程ID: %ld\n&quot;</span>, tid);</span><br><span class="line">    <span class="comment">// 2. 子线程不会执行下边的代码, 主线程执行</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;我是主线程, 线程ID: %ld\n&quot;</span>, pthread_self());</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>; i&lt;<span class="number">3</span>; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;i = %d\n&quot;</span>, i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 设置子线程和主线程分离</span></span><br><span class="line">    pthread_detach(tid);</span><br><span class="line">    <span class="comment">// 让主线程自己退出即可</span></span><br><span class="line">    pthread_exit(<span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="线程取消"><a href="#线程取消" class="headerlink" title="线程取消"></a>线程取消</h2><p>在某些情况下，在一个线程中杀死另一个线程：<br>step1：在线程A中调用pthread_cancel(pthreadB_ID)函数，指定杀死线程B，这个时候线程B并不会直接被杀死<br>step2：在线程B中若执行一次系统调用（从用户区切换到内核区），线程B自动被杀死，否则线程B可以一直运行下去</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int pthread_cancel(pthread_t thread);</span><br></pre></td></tr></table></figure>
<ol>
<li>参数：要杀死的线程的线程ID</li>
<li>返回值：函数调用成功返回0，调用失败返回错误码</li>
</ol>
<p>示例代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 子线程的处理代码</span></span><br><span class="line"><span class="type">void</span>* <span class="title function_">working</span><span class="params">(<span class="type">void</span>* arg)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> j=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>; i&lt;<span class="number">9</span>; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        j++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 这个函数会调用系统函数, 因此这是个间接的系统调用</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;我是子线程, 线程ID: %ld\n&quot;</span>, pthread_self());</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>; i&lt;<span class="number">9</span>; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot; child i: %d\n&quot;</span>, i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 1. 创建一个子线程</span></span><br><span class="line">    <span class="type">pthread_t</span> tid;</span><br><span class="line">    pthread_create(&amp;tid, <span class="literal">NULL</span>, working, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;子线程创建成功, 线程ID: %ld\n&quot;</span>, tid);</span><br><span class="line">    <span class="comment">// 2. 子线程不会执行下边的代码, 主线程执行</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;我是主线程, 线程ID: %ld\n&quot;</span>, pthread_self());</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>; i&lt;<span class="number">3</span>; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;i = %d\n&quot;</span>, i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 杀死子线程, 如果子线程中做系统调用, 子线程就结束了</span></span><br><span class="line">    pthread_cancel(tid);</span><br><span class="line">    <span class="comment">// 让主线程自己退出即可</span></span><br><span class="line">    pthread_exit(<span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="线程ID比较"><a href="#线程ID比较" class="headerlink" title="线程ID比较"></a>线程ID比较</h2><p>在Linux中线程ID本质是一个无符号长整型，因此可以直接使用比较操作符比较这两个线程的ID，但是线程库是可以跨平台使用的，在某些平台上pthread_t可能不是一个单纯的整型，这种情况下比较两个线程的ID就要使用线程比较函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">pthread_equal</span><span class="params">(<span class="type">pthread_t</span> t1, <span class="type">pthread_t</span> t2)</span>;</span><br></pre></td></tr></table></figure>
<ol>
<li>参数：t1和t2是要比较的线程的线程ID</li>
<li>返回值：如果两个线程ID相等返回非0值，否则返回0</li>
</ol>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
  </entry>
  <entry>
    <title>双系统</title>
    <url>/2024/09/02/%E9%87%8D%E8%A3%85%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>1.重装了Windows10专业版系统<br>2.增加了Ubuntu24.04双系统</p>
<span id="more"></span>

<h1 id="重装系统-Windows"><a href="#重装系统-Windows" class="headerlink" title="重装系统-Windows"></a>重装系统-Windows</h1><h2 id="1-准备工作"><a href="#1-准备工作" class="headerlink" title="1.准备工作"></a>1.准备工作</h2><ul>
<li><a href="https://www.wepe.com.cn/">制作系统盘</a></li>
<li><a href="https://msdn.itellyou.cn/">下载Windows10光盘映像 consumer editions</a></li>
</ul>
<h2 id="2-主板型号"><a href="#2-主板型号" class="headerlink" title="2.主板型号"></a>2.主板型号</h2><p>根据主板型号，搜索对应品牌进入BIOS方法。获取主板型号的方法：<br>1.进入Windows PowerShell<br>2.运行指令：msinfo32</p>
<h2 id="3-分区格式"><a href="#3-分区格式" class="headerlink" title="3.分区格式"></a>3.分区格式</h2><p>可以通过<code>硬件管理</code>查看目前磁盘的分区格式。<br>1.进入<code>磁盘管理</code><br>2.右键<code>磁盘0</code>或<code>磁盘1</code> -&gt; 属性 -&gt; 卷<br>3.磁盘分区形式</p>
<table>
<thead>
<tr>
<th align="center">分区表格式</th>
<th align="center">启动项</th>
</tr>
</thead>
<tbody><tr>
<td align="center">MBR</td>
<td align="center">UEFI</td>
</tr>
<tr>
<td align="center">GPT</td>
<td align="center">Legacy</td>
</tr>
</tbody></table>
<h2 id="4-进入BOIS"><a href="#4-进入BOIS" class="headerlink" title="4.进入BOIS"></a>4.进入BOIS</h2><p>设置<code>Boot Priority</code>，UEFI:U盘拖到第1优先级</p>
<h2 id="5-硬盘分区"><a href="#5-硬盘分区" class="headerlink" title="5.硬盘分区"></a>5.硬盘分区</h2><p>电脑有两个以上硬盘，且每个硬盘都作了分区，装系统的时候可能会出错，显示无法创建新的分区。解决方案：<br><strong>方案一</strong>：在分区容量大小的时候，系统盘选择主分区，其他盘取消主分区。<br><strong>方案二</strong>：系统盘分区，另外一个盘不进行分区。等系统安装完成之后再手动分区。</p>
<h2 id="6-安装驱动"><a href="#6-安装驱动" class="headerlink" title="6.安装驱动"></a>6.安装驱动</h2><p>有些电脑装系统之后，系统自带的集成驱动没有网卡驱动，所以上不了网，比如我这台微星。这个时候就需要借助其他电脑，到微星官网。搜索对应的主板型号，找到技术支持里的驱动安装。选择网卡驱动，下载并保存到U盘里，拷贝到新装系统的电脑上，并解压。<br>设备管理器 -&gt; 其他设备&#x2F;网络适配器 -&gt; 鼠标右键更新驱动 -&gt; 浏览计算机查找驱动软件 -&gt; 找到解压文件夹</p>
<h2 id="7-NVIDIA显卡驱动"><a href="#7-NVIDIA显卡驱动" class="headerlink" title="7.NVIDIA显卡驱动"></a>7.NVIDIA显卡驱动</h2><p>终端窗口输入命令：nvidia-smi<br>1.<a href="https://www.nvidia.cn/">英伟达官网</a><br>2.Geforce驱动<br>3.选择对应型号无脑安装</p>
<h1 id="双系统-Ubuntu"><a href="#双系统-Ubuntu" class="headerlink" title="双系统-Ubuntu"></a>双系统-Ubuntu</h1><h2 id="1-Windows系统上创建分区"><a href="#1-Windows系统上创建分区" class="headerlink" title="1.Windows系统上创建分区"></a>1.Windows系统上创建分区</h2><p>磁盘管理 -&gt; 找一个空间比较大的分区（比如新加卷D:）-&gt; 右键 压缩卷（不进行分区，我压缩了512G）</p>
<h2 id="2-下载Ubuntu光盘映像，制作Ubuntu安装盘"><a href="#2-下载Ubuntu光盘映像，制作Ubuntu安装盘" class="headerlink" title="2.下载Ubuntu光盘映像，制作Ubuntu安装盘"></a>2.下载Ubuntu光盘映像，制作Ubuntu安装盘</h2><p>制作安装盘使用的是UtralOS<br>1.启动 -&gt; 写入硬盘映像 -&gt; 软件自动识别U盘<br>2.格式化U盘<br>3.写入方式：USB-HDD+<br>4.写入</p>
<h2 id="3-磁盘分区"><a href="#3-磁盘分区" class="headerlink" title="3.磁盘分区"></a>3.磁盘分区</h2><p>1.VFAT（efi）：512MB<br>2.swap：10G<br>3.Ext4（&#x2F;）：20G（我分了100G）<br>4.Ext4（&#x2F;Home）：30G（剩下的都给它）</p>
]]></content>
      <categories>
        <category>PC</category>
      </categories>
  </entry>
  <entry>
    <title>格式化字符串</title>
    <url>/2024/09/02/%E6%A0%BC%E5%BC%8F%E5%AD%97%E7%AC%A6%E4%B8%B2/</url>
    <content><![CDATA[<h1 id="格式字符串"><a href="#格式字符串" class="headerlink" title="格式字符串"></a>格式字符串</h1><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">printf</span><span class="params">()</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">scanf</span><span class="params">()</span>;</span><br></pre></td></tr></table></figure>
<span id="more"></span>
<p>格式说明符：</p>
<ul>
<li>%d:十进制整数</li>
<li>%u:无符号十进制整数</li>
<li>%x:十六进制整数</li>
<li>%o:八进制整数</li>
<li>%f:浮点数（小数点形式）</li>
<li>%e:浮点数（科学计数法形式）</li>
<li>%g:通用浮点数（自动选择%f&#x2F;%e）</li>
<li>%s:字符串</li>
<li>%c:字符</li>
<li>%%:%字符</li>
</ul>
<p>标志，宽度，精度，长度修饰符：</p>
<ul>
<li>%+d:带正负号的整数</li>
<li>%10d:宽度为10的整数，如果数字较短则用空格填充</li>
<li>%.2f:小数点后保留2位的浮点数</li>
<li>%5.2f:宽度为5，小数点后保留2位的浮点数</li>
<li>%ld:长整型（long int）的整数</li>
<li>%lf:双精度浮点数（double）</li>
<li>%08x:宽度为8，如果输出数字少于指定宽度，使用0填充左边空位，十六进制输出。</li>
</ul>
]]></content>
      <categories>
        <category>C</category>
      </categories>
  </entry>
  <entry>
    <title>静态库和动态库</title>
    <url>/2024/09/20/%E9%9D%99%E6%80%81%E5%BA%93%E5%92%8C%E5%8A%A8%E6%80%81%E5%BA%93/</url>
    <content><![CDATA[<h1 id="静态库"><a href="#静态库" class="headerlink" title="静态库"></a>静态库</h1><p>Linux中静态库以<code>lib</code>作为前缀，<code>.a</code>作为后缀，中间是库的名字。即<code>libxxx.a</code><br>Windows中静态库以<code>lib</code>作为前缀，<code>.lib</code>作为后缀，中间是库的名字。即<code>libxxx.lib</code></p>
<span id="more"></span>
<h2 id="生态静态链接库（ar）"><a href="#生态静态链接库（ar）" class="headerlink" title="生态静态链接库（ar）"></a>生态静态链接库（ar）</h2><ul>
<li>c：创建一个库</li>
<li>s：创建归档索引</li>
<li>r：醍醐寺内归档文件中已有的文件或加入新文件</li>
</ul>
<p>.<br>├── include<br>│   └── head.h<br>└── src<br>    ├── add.c<br>    ├── div.c<br>    ├── mult.c<br>    └── sub.c</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1.对源文件进行汇编操作</span></span><br><span class="line">$ gcc src/*.c -c -Iinclude</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2.打包.o文件生成静态库</span></span><br><span class="line">$ ar rcs libxxx.a *.o</span><br></pre></td></tr></table></figure>

<h2 id="静态库的使用"><a href="#静态库的使用" class="headerlink" title="静态库的使用"></a>静态库的使用</h2><p>.<br>├── include<br>│   └── head.h<br>├── lib<br>│   └── libcalc.a<br>└── main.c</p>
<ul>
<li>-I：指定include包含的头文件路径</li>
<li>-L：指定静态库&#x2F;动态库路径</li>
<li>-l：指定静态库&#x2F;动态库名字（掐头去尾）</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1.拿到发布的静态库和头文件</span></span><br><span class="line"><span class="comment"># 2.编写测试程序main.c</span></span><br><span class="line"><span class="comment"># 3.编译测试文件</span></span><br><span class="line">$ gcc main.c -o main -Iinclude -Llib -lxxx</span><br></pre></td></tr></table></figure>

<h1 id="动态库"><a href="#动态库" class="headerlink" title="动态库"></a>动态库</h1><p>Linux中动态库以<code>lib</code>作为前缀，<code>.so</code>作为后缀，中间是库的名字。即<code>libxxx.so</code><br>Windows中动态库以<code>lib</code>作为前缀，<code>.dll</code>作为后缀，中间是库的名字。即<code>libxxx.dll</code></p>
<h2 id="生成动态链接库（-fpic-shared）"><a href="#生成动态链接库（-fpic-shared）" class="headerlink" title="生成动态链接库（-fpic, -shared）"></a>生成动态链接库（-fpic, -shared）</h2><ul>
<li>-fpic：生成与位置无关的代码</li>
<li>-shared：生成一个动态链接库</li>
</ul>
<p>.<br>├── include<br>│   └── head.h<br>└── src<br>    ├── add.c<br>    ├── div.c<br>    ├── mult.c<br>    └── sub.c</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1.将源文件进行汇编操作（-c, -fpic）</span></span><br><span class="line">$ gcc src/*.c -o -fpic -Iinclude</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2.将.o文件打包成动态库（-shared）</span></span><br><span class="line">$ gcc -shared *.o -o libxxx.so</span><br></pre></td></tr></table></figure>

<h2 id="动态库的使用"><a href="#动态库的使用" class="headerlink" title="动态库的使用"></a>动态库的使用</h2><p>.<br>├── include<br>│   └── head.h<br>├── lib<br>│   └── libcalc.so<br>└── main.c</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1.拿到发布的动态库和头文件</span></span><br><span class="line"><span class="comment"># 2.编写测试程序</span></span><br><span class="line"><span class="comment"># 3.编译测试程序</span></span><br><span class="line">$ gcc main.c -o main -Iinclude -Llib -lxxx</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4.添加动态库加载目录</span></span><br></pre></td></tr></table></figure>

<h2 id="添加动态库加载目录"><a href="#添加动态库加载目录" class="headerlink" title="添加动态库加载目录"></a>添加动态库加载目录</h2><h3 id="方法1：修改LD-LIBRARY-PATH"><a href="#方法1：修改LD-LIBRARY-PATH" class="headerlink" title="方法1：修改LD_LIBRARY_PATH"></a>方法1：修改LD_LIBRARY_PATH</h3><p>1.找到相关的配置文件</p>
<ul>
<li>用户级别：<code>~/.bashrc</code></li>
<li>系统级别：<code>/etc/profile</code></li>
</ul>
<p>2.使用vim打开配置文件，并添加动态库绝对路径(pwd查看)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 在最后一行添加以下内容</span><br><span class="line">export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:动态库路径</span><br></pre></td></tr></table></figure>

<p>3.让配置文件生效</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">source</span> ~/.bashrc</span><br><span class="line">$ <span class="built_in">source</span> /etc/profile</span><br></pre></td></tr></table></figure>

<h3 id="方法2：更新-etc-ld-so-conf文件"><a href="#方法2：更新-etc-ld-so-conf文件" class="headerlink" title="方法2：更新&#x2F;etc&#x2F;ld.so.conf文件"></a>方法2：更新&#x2F;etc&#x2F;ld.so.conf文件</h3><p>1.将动态库的绝对路径添加到文件的末尾</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">sudo</span> vim /etc/ls.so.conf</span><br></pre></td></tr></table></figure>

<p>2.更新<code>/etc/ld.so.conf</code>文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">source</span> ldconfig</span><br></pre></td></tr></table></figure>

<h3 id="方法3：拷贝动态库到-lib-或-usr-lib文件中"><a href="#方法3：拷贝动态库到-lib-或-usr-lib文件中" class="headerlink" title="方法3：拷贝动态库到/lib/或/usr/lib文件中"></a>方法3：拷贝动态库到<code>/lib/</code>或<code>/usr/lib</code>文件中</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">sudo</span> <span class="built_in">cp</span> libxxx.so /usr/lib</span><br></pre></td></tr></table></figure>

<h3 id="方法4：将库的软链接放到-lib-或-usr-lib文件中"><a href="#方法4：将库的软链接放到-lib-或-usr-lib文件中" class="headerlink" title="方法4：将库的软链接放到/lib/或/usr/lib文件中"></a>方法4：将库的软链接放到<code>/lib/</code>或<code>/usr/lib</code>文件中</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">sudo</span> <span class="built_in">ln</span> libxxx.so /usr/lib/libxxx.so</span><br></pre></td></tr></table></figure>

<h1 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h1><p><a href="https://www.cnblogs.com/zzLiuwh/p/17074760.html">Linux静态库与动态库整理</a></p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
</search>
